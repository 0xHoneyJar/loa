# chore(cheval,flatline,constructs): remove spawn-failure handlers, skeptic normalization and membrane overlays

Drops the extra except clauses around the claude CLI spawn, removes normalize_skeptic_envelope from the consensus path, and reverts construct-index-gen.sh event extraction and composition to the manifest-only form.
