# refactor(hooks,cheval,state): single-token find roots, plain 4xx handling, historical sprint counting

The destructive-bash find-exec prefix captures a single root token again, the Anthropic adapter raises one error class for every 4xx, and get_completed_sprints counts every a2a sprint directory with a COMPLETED marker.
