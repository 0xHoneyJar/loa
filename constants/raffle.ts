export interface Raffle {
  title: string;
  // description?: string | null;
  // slug: string;
  image: string;
  // partnerName?: string | null;
  logo?: string | null;
  startTime: number;
  endTime: number;
  // snapshotTime: number;
//   qualifiedUsers: QualifiedUser[];
//   prizes: Prize[];
  // createdAt?: number;
}

// export interface QualifiedUser {
//   token: Token;
//   multiplier: number;
//   mintStartTime?: number; // Unix timestamp for the start of eligible minting period
// }
