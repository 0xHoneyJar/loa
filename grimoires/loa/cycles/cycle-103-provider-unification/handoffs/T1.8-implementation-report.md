---
task: T1.8
sprint: sprint-1
cycle: cycle-103-provider-unification
deliverable: AC-1.3 — NODE_OPTIONS vestigial marker
status: ready-for-review
date: 2026-05-11
---

# T1.8 — entry.sh NODE_OPTIONS Vestigial Marker Report

## Executive Summary

Prepends a `VESTIGIAL — cycle-103 sprint-1 T1.8 / AC-1.3` comment block to the
existing `NODE_OPTIONS` Happy Eyeballs section in
`.claude/skills/bridgebuilder-review/resources/entry.sh`. Documents the
cycle-104 removal gate inline. Adds bats coverage that fails if the marker is
removed without the companion cleanup landing.

**The NODE_OPTIONS export itself stays active** at merge per AC-1.5 ("escape
hatch ships at merge"). Operator-facing behavior is unchanged in this PR;
only the in-code marker changes to telegraph cycle-104 removal scope.

- **Modified files (1):**
  - `.claude/skills/bridgebuilder-review/resources/entry.sh` — +26 lines (comment block prepended).
- **New files (1):**
  - `tests/unit/entry-sh-node-options-vestigial.bats` — 6 bats tests pinning the marker contract.
- **Tests:** 6/6 pass.
- **Operator-facing behavior:** identical (marker is documentation only).

## AC Verification

| AC | Verbatim text (sprint.md L61-72) | Status | Evidence |
|----|----------------------------------|--------|----------|
| AC-1.0 | "Pre-implementation httpx large-body spike..." | ✓ Met | T1.0. |
| AC-1.1 | BB invokes cheval for provider calls | ✓ Met | T1.4. |
| AC-1.2 | TS adapter test suites pass against the delegate | ✓ Met | T1.4 + T1.5. |
| AC-1.3 | "KF-001 NODE_OPTIONS fix marked vestigial in `entry.sh` (comment + cycle-104 removal TODO)" | ✓ Met | (a) VESTIGIAL marker block at `entry.sh:30-55` cites AC-1.3, AC-1.5, T1.8, cycle-104 removal TODO, and the two removal-gate conditions (LOA_BB_FORCE_LEGACY_FETCH operator green-signal + github-cli.ts independence check). (b) `tests/unit/entry-sh-node-options-vestigial.bats` pins the marker text + cycle-104 TODO + the gate conditions + still-active NODE_OPTIONS export — removing any of the four causes a test failure. |
| AC-1.4 | flatline → model-invoke | ✓ Met | T1.6 + T1.7. |
| AC-1.5 | "BB's review-marker logic... STAY in TypeScript. Network calls to GitHub stay as-is. `LOA_BB_FORCE_LEGACY_FETCH=1` escape hatch ships at merge" | ✓ Met | T1.2 / T1.4. T1.8 reinforces AC-1.5 by EXPLICITLY keeping the NODE_OPTIONS export active. The bats test `NODE_OPTIONS export is STILL active (AC-1.5 — stays at merge)` fails if the flag is removed without also removing the marker. |
| AC-1.6 | KF-008 re-run | ⏸ Deferred | T1.9. |
| AC-1.7 | MODELINV envelope unification | ✓ Met | T1.2 + T1.4 + T1.6. |
| AC-1.8 | credential-handoff | ✓ Met | T1.2 + T1.6. |
| AC-1.9 | subprocess lifecycle | ✓ Met | T1.2. |

## Tasks Completed

### T1.8 — Mark `entry.sh` NODE_OPTIONS as vestigial

**File modified:** `.claude/skills/bridgebuilder-review/resources/entry.sh`

The existing NODE_OPTIONS section (lines 56-103 post-edit) is unchanged. A new
comment block (lines 30-55) is prepended above it. The block:

1. Cites the AC (AC-1.3), the cycle / sprint / task ID, and the unification
   commit references (1e1381dd + 92c0057e — T1.2 + T1.4).
2. Explains *why* the flag is vestigial — BB no longer issues `fetch()` to
   provider APIs; the cheval Python substrate handles all provider HTTP.
3. Includes a `TODO(cycle-104):` block listing both removal gates:
   - operator-green signal across ≥1 cycle that the cheval path is the BB hot
     path (i.e., no operator has flipped `LOA_BB_FORCE_LEGACY_FETCH=1`);
   - github-cli.ts and other Node fetch sites confirmed not subject to the
     same Happy Eyeballs failure mode.
4. References the bats test that pins the contract.

**Why the export stays:** AC-1.5 explicitly says the escape hatch / safety
flag ships at merge. The NODE_OPTIONS flag is the operator-side safety valve
mirroring `LOA_BB_FORCE_LEGACY_FETCH` — the cycle's principle is "remove only
after operator-green signal across a full cycle". Until cycle-104, the flag
stays.

**File created:** `tests/unit/entry-sh-node-options-vestigial.bats` (6 tests)

| # | Test | Purpose |
|---|------|---------|
| 1 | `VESTIGIAL marker comment is present` | Pins the marker text — removing it without companion cleanup fails CI |
| 2 | `cycle-104 removal TODO is present alongside the marker` | The TODO must travel with the marker |
| 3 | `removal gate conditions are documented inline` | Both gates (`LOA_BB_FORCE_LEGACY_FETCH=1` operator-green + `github-cli.ts` independence) must be in the comment |
| 4 | `NODE_OPTIONS export is STILL active (AC-1.5 — stays at merge)` | Pins that the export is preserved — fails if removed prematurely |
| 5 | `LOA_BB_DISABLE_FAMILY_TIMEOUT_FIX opt-out hatch is still honored` | Pins the safety-valve env var stays available |
| 6 | `entry.sh syntax is valid bash` | Surgical syntax check |

## Technical Highlights

- **Karpathy-aligned surgical scope.** 26 lines added to entry.sh (one comment block); no code logic touched. The grep-based bats test is small and unambiguous.
- **AC-1.3 + AC-1.5 cross-cite intentional.** AC-1.3 mandates the marker; AC-1.5 mandates the export stays. T1.8 honors both by adding ONLY the marker, leaving the export active, and the bats test pins BOTH halves of the contract — removing either half without the other is a CI failure.
- **Sprint plan path paraphrased.** sprint.md T1.8 names `tests/test_entry_sh_node_options_vestigial.bats` but repo convention places bats tests under `tests/unit/` with dashes. I matched convention: `tests/unit/entry-sh-node-options-vestigial.bats`. Documented in the test file's header.

## Testing Summary

```bash
# T1.8 bats in isolation
/tmp/bats-install/bats-core-1.11.0/bin/bats tests/unit/entry-sh-node-options-vestigial.bats
# → 6/6 pass

# Syntax check on entry.sh
bash -n .claude/skills/bridgebuilder-review/resources/entry.sh
# → exit 0
```

## Known Limitations

1. **The marker doesn't enforce cycle-104 timing.** It's documentation + a regression-catcher. A determined operator could remove both the marker AND the export simultaneously, satisfying bats but skipping the gate review. Mitigation: the gate is reviewer-visible (the TODO references cycle-104); a PR that removes the block without cycle-104-in-flight should be caught in code review.
2. **Test path deviates from sprint.md.** sprint.md says `tests/test_entry_sh_node_options_vestigial.bats` (root); I placed at `tests/unit/entry-sh-node-options-vestigial.bats` to match convention. Flagged for SDD-correction sweep.
3. **No coverage for the "operator-green signal" gate condition.** The first removal gate is observable, not enforceable in code. A cycle-104 task that removes the block should pair with a separate analysis confirming no operator has flipped `LOA_BB_FORCE_LEGACY_FETCH=1` since the merge of cycle-103.

## Verification Steps for Reviewer

1. **Bats tests:**
   ```bash
   /tmp/bats-install/bats-core-1.11.0/bin/bats tests/unit/entry-sh-node-options-vestigial.bats
   ```
   Expect: 6/6 pass.
2. **Inspect entry.sh** — lines 30-55 contain the VESTIGIAL marker block; lines 56+ are the existing NODE_OPTIONS section (unchanged).
3. **Confirm the export still works:**
   ```bash
   grep -F 'export NODE_OPTIONS=' .claude/skills/bridgebuilder-review/resources/entry.sh
   ```
   Expect: prints the export line.
4. **Inspect the bats test file** at `tests/unit/entry-sh-node-options-vestigial.bats` — confirm 6 assertions pin the four sides of the contract (marker present + TODO present + export still active + opt-out still honored).

## Sprint 1 Progress After T1.8

| Task | Status | Commit |
|------|--------|--------|
| T1.0 | ✅ DONE | `bed7db56` |
| T1.1 | ✅ DONE | `13a3bffa` |
| T1.2 | ✅ DONE | `1e1381dd` |
| T1.3 | ~ DESCOPED | (T1.1 outcome) |
| T1.4 | ✅ DONE | `92c0057e` |
| T1.5 | ✅ DONE | `2f9887a5` |
| T1.6 | ✅ DONE | `b430e48e` |
| T1.7 | ✅ DONE | `14689c26` |
| **T1.8** | ✅ **DONE (this report)** | pending |
| T1.9 | ⏸ Next | KF-008 re-run on unified path (closes M3) |
| T1.10 | ⏸ Pending | cheval-delegate-architecture.md runbook |
| T1.11 | ⏸ Pending | LOA_BB_FORCE_LEGACY_FETCH bats test pin |

**Next recommended task:** T1.9 (KF-008 re-run on unified path). This is the
**M3 cycle-exit invariant closure** — verifies that the cheval Python httpx
path does NOT reproduce the KF-008 failure (large-body Google fetch socket
error). Updates `grimoires/loa/known-failures.md` KF-008 attempts table per
AC-1.6.
