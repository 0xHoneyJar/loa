**Assumptions**: Scope = `.claude/skills/riding-codebase/SKILL.md` only. Target model = Claude Fable 5.1. `resources/` and `resources/references/` file *names* were globbed for routing, contents not audited (per brief).

**Byte count**: 28,130 → 16,342 (measured with `wc -c`; budget 16,384).

**Untrusted input**: none — the file is legitimate skill body text, nothing addressed at the auditor.

**Top findings**: nine near-identical "File Persistence Checkpoint" subsections (Group 1c duplicated boilerplate) consolidated into one rule under Core Principles plus inline `**WRITE TO DISK.**` tags; four near-identical enrichment skip-condition blocks (Group 1d patch accretion) merged into one table; a dead meta-note explaining the checkpoint scheme, orphaned once that scheme was consolidated; three "YOU MUST CREATE"/"(MANDATORY OUTPUT)" boosters (Group 1a) de-shouted, citing Phase 10.0's verification gate as the actual enforcing mechanism.

| Line(s) (orig) | Pattern | Evidence | Why obsolete for Fable 5.1 | Confidence | Keep-list check |
|---|---|---|---|---|---|
| 228-237, 288-295, 313-320, 349-356, 370-377, 419-431, 457-475, 491-497, 528-535 | 1c duplicated boilerplate / Group 2 "one place" | 9× `### N.M File Persistence Checkpoint (CP-N)` blocks, identical prose+JSON differing only by path | Current models don't need the write→verify→retry→log recipe re-taught 9 times; stating it once and tagging artifacts is the same instruction at 1/9 the cost | High | none (K-41/K-42 are frontmatter-only; body untouched) |
| 506 | orphaned meta-note | "Checkpoint coverage note: Phases 2, 3, and 8..." | Explains why 3 phases lack a CP subsection — moot once the CP scheme itself is removed; addressed to a future file-editor, not the executing model | High | none |
| 624-664 | 1d patch accretion | 4× `## Phase N: ... (ENRICH_X only)` skip blocks, differing only by phase/flag/action name | Same conditional shape four times; the model navigates a maze of near-duplicate blocks instead of one table | High | none |
| 220, 362, 512 | 1a pressure language / MUST-ALWAYS rule | `**YOU MUST CREATE**` (×3), `(MANDATORY OUTPUT)` header tags | No hook enforces this text; the actual mechanism is Phase 10.0's Artifact Verification Gate (Glob-checks all 11 artifacts, flags 0/N as failure) — citation replaces the shout | High | none |
| 576 | 1a pressure language | `**The ride MUST NOT complete with 0/N artifacts verified.**` | Same Phase 10.0 gate is the mechanism; rewritten as plain "flag the ride as failed on 0/N verified" | High | none |
| 524 | 1a pressure language | `**IMPORTANT**: If trajectory file is empty at Phase 9, flag as failure.` | No external mechanism; restated plainly, substance kept | Medium | none |
| 668-679 | 1c step choreography for a judgment task | 5-item numbered "Uncertainty Protocol" list | Ambiguous-code handling is exactly the judgment-task case the method flags; one prose sentence keeps every element (state/cite/list/ask/log) | High | none |
| 678 | 1c padding/repetition-as-reinforcement | `**Never assume. Always ground in evidence.**` | Restates Core Principle #3 ("Evidence required") verbatim in different words | Medium | Core Principles wording itself kept |
| 28-30 | 1c padding/repetition-as-reinforcement | ```CODE IS TRUTH → Loa channels CODE → Grimoire reflects REALITY``` fenced diagram | Re-illustrates Core Principle #1 one line below it | Medium | none |
| 22 | 2 volatile version pin | "following the v0.6.0 Enterprise-Grade Managed Scaffolding model" | Pinned version name I have no way to verify against current code/docs (out of scope); replaced with a description of what the artifacts are, which doesn't rot | Low-Medium | none |
| 98-149 | budget rule (move, not a Group finding) | Enrichment Flags table + Configuration Defaults bash + QMD Reality Context, ~53 lines | Config thresholds and the QMD integration are read only when a flag is actually set / the script exists — conditionally-needed material moved to `resources/enrichment-config.md`, guarded pointer ("when any flag is set") left in place; the flag→variable→phase→action mapping itself stays inline since it's evaluated every ride | High (move, not deletion) | none |
| 391-399 | budget rule (move) | "Extended Markers (available when enrichment phases run)" table + paragraph | Self-labeled conditional (enrichment-only); moved to `resources/extended-markers.md`, guarded pointer left | High | none |
| 584-610 | keep-list #7 exception via budget rule (move, not delete) | Full "Completion Summary" literal template | Format-pinning example, genuinely kept — but only needed once, at the very end of a successful ride; moved verbatim to `resources/completion-summary.md`, guarded pointer left ("when the ride finishes") | High (move, not deletion) | verbatim preserved in resource |
| 694-717 | budget rule (move) | 19-row Trajectory Logging field-mapping table | Tool-contract detail (method: "stays, and often grows") — not deleted, moved verbatim to `resources/trajectory-fields.md` with a guarded pointer ("when logging a phase"); flagged medium confidence since this trades always-visible schema for budget | Medium | verbatim preserved in resource |

**MUST/NEVER/ALWAYS removed or rewritten**: all four instances above (`YOU MUST CREATE` ×3, `MUST NOT complete with 0/N`, `IMPORTANT`/empty-trajectory) named no enforcing mechanism in the surrounding text; each is now a plain instruction, with Phase 10.0's Artifact Verification Gate cited as the actual mechanism where relevant. No MUST/NEVER/ALWAYS naming a fence, validator, or gate script was touched.

**Deliberately kept despite a grep match**: the standard 3-row Grounding Marker table (Phase 6) — I noticed `resources/references/output-formats.md` already carries an equivalent table; per keep-list rule 8 ("working redundancy... duplicates actually agree... leave it alone"), agreeing duplication is not a finding, so I left both untouched rather than proposing dedup on a resource file outside my audit scope. The two `bash` blocks in Phase 0 and Phase 2 (fragile, order-dependent, low-freedom) survived verbatim per "fragile operations keep exact scripts." All six existing `**See**: resources/...` pointers (pre-existing, not material I moved) were left in their original unguarded phrasing — the guard-word requirement in my brief applies to pointers for content *I* relocate, not to the file's existing citation style, and rewriting established pointers I have no evidence are broken would be scope creep.

**Registry-rendered block preserved byte-for-byte**: the `context_discipline` `@skill-include` block (markers included). Frontmatter (both `---` delimiters) copied byte-for-byte, including the `K-41`/`K-42`-relevant `name:`/`description:` lines.

**Residual**: none — proposed file measures under budget with a ~42-byte margin.
