# Prompt audit report — `protocol-tool-result-clearing` (lead-authored)

**Target model**: Claude Fable 5.1. Thresholds, the 4-step process, the synthesis format and the edge cases are the contract the `context_discipline` include points at — kept byte for byte.

| Line | Pattern | Evidence | Action |
|---|---|---|---|
| 3-4 | 1d version pin + migration-relative narrative | `**Version**: 2.0 (cycle-121 shrink — … removed; live rule unchanged)`, `**Status**: Active` | removed; the shrink note moved to a `## Provenance` footer |

MUST lines: none outside the thresholds table (`MANDATORY clearing` is the contract). Keep-list: no rows match. Untrusted input: none.
