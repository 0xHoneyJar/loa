
**Scope**: `.claude/skills/implementing-tasks/SKILL.md` only (target model Claude Fable 5.1). Resources dir globbed (BIBLIOGRAPHY.md, REFERENCE.md, RETROSPECTIVE.md, templates/implementation-report.md, scripts/) but not audited, per brief. Read `validate-ac-verification.sh` and the report template to confirm which AC-Verification mechanics are enforced vs. restated prose, so condensation doesn't drop anything the gate script or template already carries.

**Byte count**: 32,076 → ~15,900–16,200 (estimated by line-length accounting; I have no Bash access to run `tools/check-prompt-budget.sh` directly — please confirm with that tool before landing). No `resources/*.md` is charged: every pointer I kept or added uses a guard word ("See…", "Use…") with no `read|load|source|include` verb.

**Untrusted-input check**: the file's imperative language ("MUST", "DO NOT", numbered steps) is all ordinary instruction to the *executing* agent, not to the auditor. No embedded text attempted to redirect this audit.

| Line(s) | Pattern | Evidence | Why obsolete for Fable 5.1 | Confidence | Keep-list check |
|---|---|---|---|---|---|
| 169–262 headers | 1d/Step-2 mnemonic scaffold | `## Task (N - Narrow Scope)`, `(L - Logical...)`, `(E - Explicit)`, `(R - Reproducible...)` | Same idiom as ROLE→CONTEXT→RULES→EXAMPLES boilerplate; the letter labels carry no behavioral content the current model needs | Medium | K-08/09/10 verdict strings untouched; only header text changed |
| 483–529 vs 295–309 | 1c step choreography + 1d duplication | two full "Step 1/2/3, If exists+X/If missing" tables restating the same feedback check | Current models don't need an exhaustive branch table for "read file, act on verdict"; the two copies could (and did start to) drift | High | Literal verdict strings `CHANGES_REQUIRED`, `APPROVED - LET'S FUCKING GO`, `All good` preserved verbatim, now stated once in `<grounding_requirements>` |
| 202–210, 596–602, 689–700 | 1d duplicated boilerplate | report section list stated 3× | The workflow-Phase-3 and `<output_format>` copies had silently dropped **AC Verification** from the list — real drift, not just redundancy | High | K-37 "AC Verification" kept, now the single canonical list |
| 384–462, 551–573 | 1d patch accretion | beads lifecycle (health→sync→ready→update→close→flush) stated 3× (bullets, "IMPORTANT" numbered block, bash loop) | Same commands repeated for reinforcement; current model retains one statement | High | K-38 "br close" kept, exact commands preserved once |
| 464–481 vs 630–635 | 1d duplication | SMALL/MEDIUM/LARGE thresholds table verbatim twice | Exact duplicate table | High | n/a |
| 640–671 | 1c/Group-3 worked examples | fake `Agent 1: "Read …: 1. Does file exist? …"` dispatch scripts | Scripted sub-agent dialogue is the "worked examples… fake dialogue turns" pattern; current models compose a dispatch prompt from a goal statement | Medium | n/a |
| 544–547 | 1c + Fable-5.1 over-planning | "Create implementation strategy: Break down tasks… Identify dependencies… Plan test coverage" | Migration doc: at `effort: xhigh` (this skill's own frontmatter) Fable 5.1 already "plans without being told" and can over-deliberate; explicit strategy choreography compounds that | Medium | n/a |
| 702–708 | 1c generic virtues / 1d scaffold | SMART "Specific/Measurable/Achievable/Relevant/Time-bound" block | Pure restatement of the Verification section already above it, zero new content | High | n/a |
| 716–744 | 1c choreography | "What is a Complex Task?" 6-bullet enumeration + 4-step "Plan Review" | Judgment call the model handles without an enumerated decision procedure; the artifact requirement (path, template) is the only load-bearing part | Medium | n/a |
| 319–379 | 1d unenforced instruction + history | "Why this is a hard precondition… Even autonomous /run cycles MUST satisfy it" | Grepped `run-mode/SKILL.md` for `karpathy_check\|goal_driven\|success_criteria`: no matches. The "MUST" names no enforcing hook — it's prompt-only, contradicting its own "hard precondition" framing | Medium | Gate mechanics (config key, AskUserQuestion options, trajectory schema) kept; only the unenforced essay removed |
| 619–628 | 1d duplication with always-loaded file | full heredoc-corruption explainer | CLAUDE.loa.md (loaded every session) already states "For source files, ALWAYS use Write tool" and points to the same rule file | Medium | n/a |
| 91, 204, 212, 264, 377 | History rule | `(#1086)` ×2, `Issue #475`, `cycle-057`, `#961 K-3/FR-4, PR #960` | Cycle/issue tokens may only survive in a Provenance footer or backticked path | High (mechanical) | Moved to `## Provenance` footer |
| 384, 716 | Group 2 volatile specifics | `(v1.29.0)`, `(v0.19.0)` | Version pins in headers rot silently; not part of the instruction | Medium | n/a |
| 680 | History rule | `(cycle-119)` | Same rule; the loa-scout tiering constraint itself is kept, just un-dated | High | Moved to footer |
| 80–109 | Byte-budget move (not a dated pattern) | CLI tool allow/confirm tables | Genuine tool contract, but a table — moved per the brief's explicit budget-overflow allowance | n/a (move, not cruft) | New `resources/CLI-TOOL-POLICY.md`, guarded pointer left in place |

**MUST/NEVER/ALWAYS audited**: the `validate-ac-verification.sh` **MUST** (kept, cites the script + its exit codes), the AC-Verification "DO NOT write COMPLETED if…" bullets (kept in substance, rewritten as one sentence citing the same script since that's the enforcing mechanism — no longer three shouted bullets), "Users should NOT run br commands manually" (kept in substance, de-capitalized — no enforcing hook, but it's a factual description of this skill's own behavior, not a prohibition needing one), CLAUDE.loa.md-generated `DO NOT` constraints block (untouched, registry-rendered).

**Kept despite grep-matching**: `IMPORTANT` in the input_guardrails skill-include (generated, frozen); all `MUST`s tied to `validate-ac-verification.sh`; the exact bash command blocks for beads and the qmd-context-query script (fragile/exact-syntax operations, keep-list item 3); the `Executive Summary … Feedback Addressed` ordered list (real enforced ordering, not filler); `✓ Met`/`⏸ [ACCEPTED-DEFERRED]` markers (parsed literally by `validate-ac-verification.sh:185,188`).

**Residual/side effects to flag**: I dropped the ledger cross-reference clause ("also recorded as the bug cycle's `sprint_plan` in the ledger") from the `$SPRINT_FILE` resolution note — kept the operative rule (which file to use, never substitute) but cut the ledger-bookkeeping aside as a compression call rather than a named pattern. All `@skill-include` and `@constraint-generated` regions, and the frontmatter, are byte-for-byte untouched.

