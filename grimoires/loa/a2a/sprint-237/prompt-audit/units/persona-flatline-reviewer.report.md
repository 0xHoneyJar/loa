# Prompt audit report — `persona-flatline-reviewer` (lead-authored)

**Target model**: Claude Fable 5.1 (persona is sent to the external Flatline voices, several of which run schema-unenforced, so the JSON-only output contract and the schema block are format-pinning contract text, kept byte for byte). Walked Groups 1–4: no pressure language, no retired-model scaffolds, no numeric floors or cadences, no history tokens outside the version header comment. Keep-list rows for this file survive byte for byte. Untrusted input: none.

**Verdict**: clean — returned unchanged.
