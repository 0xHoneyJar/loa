
**Scope**: `.claude/skills/auditing-security/SKILL.md` only (32,538 B → estimated ~14,600 B; I have no Bash, so this is a careful manual estimate, not a `check-prompt-budget.sh` run — the lead should confirm). **Target model**: Claude Fable 5.1, per the brief.

**Untrusted-input note**: the audited file's own text ("MANDATORY", "CRITICAL — DO THIS FIRST", "MUST self-check") reads only as prompt content for the eventual audit-executing model, not as instructions to me; I did not treat any of it as directives.

**Frontmatter** (lines 1–46) and the six `@skill-include` blocks (`input_guardrails`, `integrity_precheck`, `factual_grounding`, `context_discipline`, `trajectory_logging`, `retrospective_postlude`) are copied byte-for-byte, unchanged, per the registry-rendered rule. All keep-list rows for this file — K-31 `APPROVED - LET'S FUCKING GO`, K-32 `LOA-VERDICT`, K-33 `verdict-derive.sh`, K-34 `auditor-sprint-feedback.md`, K-35 `^disallowed-tools:`, and K-41/K-42 `^name:`/`^description:` — survive verbatim in Phase 3 and the frontmatter, which I left untouched.

**Findings** (highest confidence first):

| Line(s) | Pattern | Evidence | Why obsolete for Fable 5.1 | Confidence | Keep-list check |
|---|---|---|---|---|---|
| 155–198 `<kernel_framework>` | 1c/1d ROLE→CONTEXT→RULES→EXAMPLES-style scaffold ("Task(N)/Context(L)/Constraints(E)/Verification(E)/Reproducibility(R)") | "## Task (N - Narrow Scope)" … full NCEVR template | Every fact in it (output paths, 5 categories, DO/DON'T list, "Success =" checklist, the two verdict strings) is restated verbatim or near-verbatim in Phase 2's file tree, Phase 1's category list, `<output_format>`, `<citation_requirements>`, and Phase 3 — pure duplicate scaffold | High | No K-row touches this block; verified by re-checking every fact appears elsewhere before deleting |
| 618–664 `<rubric_scoring>` | 1c inline arithmetic rubric ("weighted average", per-dimension point list) | "Security: 30% / Architecture: 20% / …" | `resources/RUBRICS.md` already contains the full SEC-IV/SEC-AZ/… dimension list (confirmed by name-check) — the model computing a weighted average inline is exactly the "arithmetic the model must compute" pattern; data belongs in the file | High | none |
| 666–725 `<structured_output>` | 2 duplicated info across SKILL.md and reference file | full JSON schema + example reproduced inline right under "**Reference**: `resources/OUTPUT-SCHEMA.md`" | The file already points at `resources/OUTPUT-SCHEMA.md` for this; restating the whole schema inline is the "information lives in exactly one place" violation | High | none |
| 275–346 (Phase 1A) / 348–395 (Phase 1B) | 1c/2 step choreography + "model already knows this" (SQLi/XSS/SSRF source-sink mappings, ID-tracking bureaucracy SRC-*/SINK-*/PATH-*, per-repo-size trace-count/time-budget table) | Source/Sink Taxonomy tables, Trust Decision Tree, 30s/90s/180s/300s×20/50/100/150-trace table | A frontier security-focused model already owns standard taint-source/sink taxonomy; the exact 2-pass ID-tagged bureaucracy over-constrains a judgment task current models already do well, and the numeric time/trace ceilings duplicate the Phase -1 size/`timeout_minutes: 60` controls already present | Medium-High | none; REFERENCE.md §Security backstops any coverage gap |
| 548–602 `<parallel_execution>` | 2 duplicated info (repeats Phase -1's SMALL/MEDIUM/LARGE table) + 1c over-specification (5 fully worked "Agent N" fake-dialogue blocks with hardcoded file globs) | "Files: [auth/, api/, middleware/, config/]" ×5 | Duplicate threshold table; the exact agent scripts are conditionally-needed (LARGE only) — moved to `resources/PARALLEL-SPLIT.md` per the byte-budget rule, guarded pointer left ("...if you want a starting split") | Medium | none; content preserved verbatim in the new resource, not deleted |
| 727–733 `<success_criteria>` | 1d/2 dated S-M-A-R-T-style scaffold, duplicate | "Specific/Measurable/Achievable/Relevant/Time-bound" | Every line duplicates citation_requirements, Phase -1/timeout_minutes, or Phase 1B's budget; the one non-duplicate fact ("Zero false positives for CRITICAL severity") is folded into `## Coverage` instead of deleted | High | none |
| 208–223 `<grounding_requirements>`+`<citation_requirements>` | 1c padding/repetition | "Quote source code…" / "Quote vulnerable code directly…" (near-duplicate of each other and of the generated `factual_grounding` block just above) | Restates the generated `factual_grounding` block's "extract quotes / cite file:line / flag assumptions" a second and third time | Medium | merged into one non-duplicative block; unique facts (technical-debt cross-reference, CWE/OWASP citation) kept |
| 784–790 vs 776–781 | 2 duplicated info | "Audit Checklist Addition" is the same 5 items as "Cannot Approve If" restated as checkboxes | Say it once | Medium | merged into one list |
| 226 | 1a pressure language | `CRITICAL—DO THIS FIRST` | One of five "CRITICAL/MANDATORY" markers in the file; markers stop carrying information when repeated | Medium | rewritten "(do this first)" |
| 399, 756 | 1a pressure language | `**MANDATORY when enabled.**`, `**MANDATORY**: For sprint audits…` | Same over-marking; the Phase 1C rule already names its own enforcing hook, so the shout is redundant with the citation | Medium | rule + mechanism (`.claude/hooks/safety/adversarial-review-gate.sh`) kept, shout removed |
| 503 | MUST without adjacent mechanism | `## Phase 2.5: Severity Tally (MUST — before Verdict)` | No mechanism was named at the point of the MUST; I added the citation (`verdict-derive.sh` checks this against the trailer) in place of the shout | Medium | rule kept, mechanism now cited inline |
| 79 | History rule | `### Review Scope Filtering (#303)` | Bare issue number in rule text, not in a backticked path or Provenance footer | High | moved to new `## Provenance` footer |
| 401, 422 | History rule (FR-ID variant) | `(per FR-2.5)`, `(per FR-6.4)` | Same species of patch-accretion citation; the reasons are already stated in the same sentence, so the ID is decoration | Medium | noted in Provenance footer |
| 754 | History rule (version-pin variant) | `## Documentation Audit (Required) (v0.19.0)` | Pinned skill-version tag in rule text | Medium | noted in Provenance footer |

**MUST/NEVER/ALWAYS removed or rewritten**: Phase -1's and the old parallel_execution's `MUST split into parallel` had no cited enforcing mechanism (no hook forces the split) — softened to plain strategy language, duplicate copy deleted outright. Phase 2.5's and the two `MANDATORY` lines above are rewritten with their mechanism cited inline rather than shouted. Phase 3's `Prose and trailer MUST agree…` and `MUST self-check before finishing…` are untouched — verdict-gate contract, self-naming mechanism, never delete per the brief.

**Deliberately kept although a grep would flag it**: `<communication_style>` (735–751) matches Group 1a's "pressure language" grep on its surface ("Be direct and blunt", contrastive NOT-examples) but this is exactly the Fable 5.1 migration doc's endorsed pattern — invest in an explicit communication-style section rather than fighting output style downstream — so I left it verbatim. The three worked severity-classification examples in Phase 2.5 are several, deliberately varied, and labeled illustrative (the keep-list's example-over-indexing exception), so I kept them, lightly trimmed. Phase 0's "All good" / gate-check chain and the beads-workflow exact commands are fragile-operation exact scripts, kept.

**Residual / caveats**:
1. **Byte estimate, not a measured result.** I have Read/Grep/Glob only — no way to run `tools/check-prompt-budget.sh` myself. My line-by-line accounting puts the proposed file comfortably under 16,384 B (roughly 14,000–15,000 B), but the lead should run the script before trusting the number.
2. **`cycle-119` survives inside the `input_guardrails` `@skill-include` block** ("mechanized — cycle-119", "pre-cycle-119 semantics"). This will keep `tests/unit/no-history-in-rule-text.bats` (NH-1) red for this file even after my patch, because that test's exemptions (Provenance footer / provenance comment / KF pointer / backticked path) don't cover `@skill-include` regions the way the byte-budget script does. Per this brief's explicit rule ("Registry-rendered regions… copied through byte for byte"), I did not touch it — the real fix is in the generator source, `.claude/data/skill-includes/input_guardrails.md`, which is outside this unit's scope.
3. I did not open `resources/REFERENCE.md`, `RUBRICS.md`, or `OUTPUT-SCHEMA.md` beyond their headings (per instructions to skim, not audit) — I'm trusting their existing content covers what I trimmed from SKILL.md's inline duplicates; the lead may want a spot-check.

