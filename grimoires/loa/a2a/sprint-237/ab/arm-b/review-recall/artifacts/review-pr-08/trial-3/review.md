## Changes Required

- **HIGH** (confidence: medium) `head/.claude/adapters/loa_cheval/providers/agy_headless_adapter.py:163` — removing the `except ValueError` handler reintroduces a crash-on-embedded-NUL bug. CPython's subprocess/exec machinery raises a raw `ValueError: embedded null byte` (not an `OSError` subclass) when argv contains a NUL byte, which can happen for a corrupted/binary prompt built by `self._build_prompt(request.messages)`. With this handler gone, that `ValueError` propagates straight out of the surrounding `try` — none of the remaining `except` clauses (`TimeoutExpired`, `SubprocessOutputCapExceeded`, `FileNotFoundError`, `OSError`) catch it — so the process crashes instead of being converted to `ProviderUnavailableError` and letting the fallback chain advance. This directly contradicts the invariant stated two clauses above it, at `head/.claude/adapters/loa_cheval/providers/agy_headless_adapter.py:156-162` ("never crash with a raw OSError... WALK the chain"), which the removed handler existed to extend to the NUL-byte case. The PR description gives no justification (e.g. that the underlying `run_subprocess_pgkill` helper now pre-validates argv) for why this failure mode can no longer occur.

- **HIGH** (confidence: medium) `head/tools/check-no-swallowed-jq.sh:119-149` — the heredoc handling was simplified to unconditionally skip every heredoc body, removing the old `_hd_command`/`hd_exec` logic that distinguished an *executed* heredoc (e.g. `bash <<EOF … EOF`, an unquoted terminator subject to expansion, or a heredoc piped into an interpreter) from an inert one (e.g. `cat <<'EOF' > file`, doc/fixture text). The scanner's whole purpose, per its own header comment, is to tripwire the "output-swallowing jq" shape on gate-critical scripts (KF-004/KF-015). After this change, a gate-critical script containing e.g. `bash <<EOF\n... jq ... || echo default ...\nEOF` — the exact shape the tool exists to catch — is now silently invisible to the scanner, because the whole heredoc body is skipped regardless of whether it is executed. The PR title frames this as "simpler heredoc skipping," but it collapses a security-relevant distinction (executable vs. documentation heredoc) rather than simplifying incidental complexity, weakening the tripwire for the exact class of gate-critical scripts it protects.

## Observations

- **MEDIUM** (confidence: medium) `head/.claude/scripts/ledger-lib.sh:157` — the content guard was relaxed from `jq -es 'length == 1 and (.[0] | type == "object")'` to a bare `jq empty`. `jq empty` (without `-s`) validates a *stream* of whitespace-separated JSON values, not a single top-level value — content consisting of two or more concatenated JSON objects (e.g. `{"a":1}{"b":2}`) now passes this check. The subsequent stamp at `head/.claude/scripts/ledger-lib.sh:179` (`jq --arg ts ... '.last_updated = $ts'`, also non-`-s`) then applies the filter per-value in the stream and emits multiple JSON objects back-to-back into `updated_content`, which gets written to the ledger file as multi-document JSON instead of the single object every other consumer of `ledger.json` expects. This is a narrower regression than the original 20260808-a008c6 bug (empty content is still explicitly checked at the same line), but it does silently drop the "and exactly one object" guarantee the removed `-es` clause enforced, without the comment or error message acknowledging that the object-shape guarantee is gone (the error text at `head/.claude/scripts/ledger-lib.sh:158` was in fact edited to drop the words "or non-object").

- **LOW** (confidence: low) `head/.claude/scripts/ledger-lib.sh:157,179` — `printf '%s' "$content"` was replaced with `echo "$content"` in both the validation and the stamping pipeline. `echo`'s handling of leading-dash arguments (`-n`, `-e`, `-E`) and backslash escapes is shell/build dependent (varies with `xpg_echo`, `sh` vs `bash`, coreutils vs. POSIX builtins), whereas `printf '%s'` is the standard safe idiom for emitting an arbitrary string literally. Practical risk here is low since ledger content is expected to be a JSON object starting with `{`, but the substitution buys nothing and quietly drops a defensive idiom.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":2,"medium":1,"low":1},"excluded":0,"sprint_id":"sprint-N","ts":"2026-09-22T00:00:00Z"} -->
