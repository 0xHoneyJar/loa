## Changes Required

- **CRITICAL** (confidence: high) `head/.claude/scripts/ledger-lib.sh:146-193` and `head/.claude/scripts/ledger-lib.sh:601` — `_write_ledger()` no longer validates that `$content` is non-empty/valid JSON before writing (the `if [[ -z "$content" ]] || ! echo "$content" | jq empty ...` guard at old `ledger-lib.sh:152-155` was deleted), and the check that `updated_content` came out non-empty after the timestamp stamp (old lines ~171-176) was deleted too. Combined with the removal of the numeric-id guard in `update_sprint_status` (see next finding), a single call with a malformed `global_id` now silently truncates `ledger.json` to an empty file instead of refusing the write. Concretely: `update_sprint_status "abc" "completed"` → `jq --argjson id "abc" ...` fails (not valid JSON) → `ledger_content=""` → `_write_ledger ""` → `updated_content=$(echo "" | jq ...)` also fails/empty → `echo "" > "$tmp_file"; mv "$tmp_file" "$ledger_path"` succeeds and overwrites the real ledger with an empty file — and the function still `return $LEDGER_OK`. A backup is taken via `ensure_ledger_backup` earlier in the same call, so recovery is possible via `recover_from_backup`, but the corruption itself is silent and unflagged. This directly undoes the "SECURITY (HIGH-001): Atomic write via temp file + mv" protection whose comment is still present at `head/.claude/scripts/ledger-lib.sh:171`.

- **HIGH** (confidence: high) `head/.claude/scripts/ledger-lib.sh:576-603` — `update_sprint_status()` dropped the `if [[ ! "$global_id" =~ ^[0-9]+$ ]]; then ... return $LEDGER_SPRINT_NOT_FOUND; fi` guard that used to reject non-numeric `global_id` before it ever reached `jq --argjson`. Removing it is what makes the ledger-wipe above reachable from ordinary caller error (e.g. an unset/blank variable, or a string sprint id) instead of requiring deliberately crafted input.

- **HIGH** (confidence: high) `head/.claude/scripts/ledger-lib.sh:379,418,459,511,601,822` — every caller of `_write_ledger` lost its `|| return $LEDGER_ERROR`. `_write_ledger` still returns 1 on lock-acquisition timeout or temp-file/mv failure, but callers now ignore that return value, `echo` their result (`cycle_id`, `current`, `global_id`, etc.) and unconditionally `return $LEDGER_OK`. Any lock contention (two ledger writers running concurrently, which `LEDGER_LOCK_TIMEOUT=5` exists specifically to guard against) now looks like a successful commit to every caller even though the write never happened.

- **HIGH** (confidence: medium) `head/.claude/scripts/mount-submodule.sh:370-405` — `validate_symlink_target()` dropped the `source` parameter and the branch that resolved a relative `target` against `dirname(source)` (the symlink's own location), and now resolves relative targets against the process's current working directory instead (`if [[ -e "$target" ]]; then resolved_target=$(cd "$(dirname "$target")" && pwd)/...`, with no `source`-relative fallback). But `ln -sf "$target" "$source"` at line 418 still creates the actual symlink with the literal relative `$target`, which the OS resolves relative to the symlink's own directory, not the caller's cwd. `create_symlinks()` calls `safe_symlink` for entries whose `link_path` is nested below the repo root (e.g. `head/.claude/scripts/mount-submodule.sh:459-465` explicitly does `parent_dir=$(dirname "$link_path"); mkdir -p "$parent_dir"` before symlinking), so for any such entry the validator now checks a boundary-escape candidate computed from the wrong base directory — it no longer verifies the path that will actually be created. This is a real accuracy regression in a check whose own docstring says "Returns: 0 if safe, 1 if escapes bounds"; a manifest/target combination that looks safe under cwd-relative resolution can still produce a real symlink that escapes the repo once the OS resolves it relative to the link's own directory (or vice versa, breaking legitimate nested links). I can't fully verify the concrete depth math without `lib/symlink-manifest.sh` (not part of this diff), hence medium confidence, but the logic error itself is unambiguous from the visible code.

## Observations

- **MEDIUM** (confidence: medium) `head/.claude/scripts/classify-pr-type.sh:42-72` — the rule matching `"from [^ ]+/release/|^[Rr]elease(\([^)]*\))?:"` (base `classify-pr-type.sh:66-69`) was deleted outright, with no replacement and no change to the function's docstring (which already didn't mention it, so there's no fresh doc/code drift). A PR titled e.g. `Release: v2.3.0` or `Merge branch 'release/v2.3' into main` now falls through to `other` instead of `cycle`, which per the docstring means the post-merge Full Pipeline (CHANGELOG, GT, RTFM, Release run) is skipped for what may still be genuine release-merge PRs. This may be intentional (e.g. release merges are expected to always carry a `cycle` label so rule 1 already covers them) but nothing in the diff or PR description establishes that, and no test coverage for this path is included in the change.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":1,"high":3,"medium":1,"low":0},"sprint_id":"pr-review","ts":"2026-09-22T00:00:00Z"} -->
