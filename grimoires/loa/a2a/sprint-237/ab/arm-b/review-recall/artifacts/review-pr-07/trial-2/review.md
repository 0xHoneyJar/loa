## Changes Required

- **HIGH** (confidence: high) `head/.claude/scripts/constructs-lib.sh:565-597` — `find_local_source` no longer validates that the discovered directory actually corresponds to the requested `slug`, so with a multi-entry `.constructs.local_source_paths` config it can silently return the wrong construct's local source. The function's own docstring says `Args: $1 = pack slug`, and the base version enforced that contract via `_local_source_matches_slug` (matching by directory name `construct-<slug>`/`<slug>`, or by the declared `name`/`slug` inside `construct.yaml`/`manifest.json`). The diff deletes that helper and its call site (`base/.claude/scripts/constructs-lib.sh:589-596` vs. `head` lines 589-594), so the loop now does:
  ```
  for path in "${search_paths[@]}"; do
      if [[ -d "$path" && ( -f "$path/construct.yaml" || -f "$path/manifest.json" ) ]]; then
          echo "$path"
          return 0
      fi
  done
  ```
  Failure scenario: a user configures `constructs.local_source_paths` with several generic local clone directories (this is exactly the use case that setting exists for — the "search multiple constructs" case, as opposed to the default slug-templated `$HOME/Documents/GitHub/construct-$slug` paths, which happen to dodge this bug only because the slug is baked into the path string itself). If they request `find_local_source foo` and the *first* configured path is actually the local clone of a *different* pack `bar` (which also has a `manifest.json`), the function now returns `bar`'s directory as if it were `foo`'s source. Any caller that installs/copies/symlinks from this path (install/update flow) will silently pull the wrong pack's content under the `foo` slug — a correctness bug with supply-chain-flavored impact (wrong, and potentially attacker-influenced if any of the configured local paths is less trusted, code getting installed under a different pack's identity) rather than a mere ergonomics fix. The PR description frames this as "returns the first existing local source path" as if it were the intended new contract, but nothing in the diff updates the function's slug-based docstring/contract or its one call site to compensate, and no caller code is included in this diff to show the slug check moved elsewhere.

## Observations

- **MEDIUM** (confidence: medium) `head/.claude/scripts/lib/symlink-manifest.sh:258-262` — Validation 2 now rejects any `link` or `target` containing a literal `..` segment outright, replacing the previous logic that resolved a relative `target` against the link's parent directory and only rejected it if the *resolved* path escaped the repo root (`base/.claude/scripts/lib/symlink-manifest.sh:279-288`, via the deleted `_normalize_rel_path`). This is stricter than before: a construct manifest with a relative symlink target that uses `..` but still resolves inside the repo (e.g. `link: .claude/skills/foo/bar`, `target: ../../shared/lib.sh` resolving to `.claude/shared/lib.sh`) is now unconditionally rejected where it previously would have been allowed. This matches the PR's stated intent ("rejects any construct link or target containing a dot-dot segment") and is arguably a reasonable simplification (it's stricter, not weaker, from a security standpoint), but it is a behavior-breaking change for any existing construct manifest relying on that pattern — worth confirming no in-repo or shipped construct actually does this before merging, since the failure mode is a silent `REJECTED` log line rather than an install error surfaced to the user.
- **LOW** (confidence: low) `head/.claude/hooks/safety/block-destructive-bash.sh:856-857` — The new hidden-dir allow branch (`\./\.[A-Za-z0-9_-][^*]*`) and the widened named exclusion list are a clear improvement over the old bare `\.` alternative that blocked every hidden path outright. One residual gap: the exclusion entries (`\.git`, `\.ssh`, `\.env`, etc.) only protect the *first* path segment after `./`; a nested sensitive directory like `./sub/.git/` or `./sub/.env` still falls through to the pre-existing visible-dir allow branch (`\./[^/*.][^*]*`, unchanged by this diff) and is allowed. This isn't a new regression introduced by this PR — the visible-dir branch already had this property before the change — but since the PR explicitly reworks the "bounded hidden dirs" exclusion semantics, it may be worth extending the exclusion coverage to nested sensitive dirs in a follow-up rather than only root-level ones.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":1,"medium":0,"low":0},"excluded":0,"sprint_id":"pr-review","ts":"2026-09-22T00:00:00Z"} -->
