## Changes Required

- **HIGH** (confidence: high) `head/.claude/adapters/loa_cheval/providers/claude_headless_adapter.py:159` — Removing the `except OSError` and `except ValueError` handlers that used to wrap `run_subprocess_pgkill(cmd, ...)` means a spawn failure (`ARG_MAX`/`ENOMEM`/exec error → `OSError`, or an embedded NUL byte in the prompt → `ValueError` when Python encodes argv) now propagates as a raw, unhandled exception out of `invoke()` instead of the `ProviderUnavailableError`/`ConfigError` types every sibling branch in the same `try` uses (`TimeoutExpired`, `SubprocessOutputCapExceeded`, `FileNotFoundError` at head/.claude/adapters/loa_cheval/providers/claude_headless_adapter.py:166-182 are all still converted). The outer `except _SemaphoreExhausted` at line 183 only catches that one type, so this exception is not caught anywhere in this function. A prompt large enough to blow `ARG_MAX`, or one containing an embedded NUL (both plausible given `prompt_chars=%d` is logged for visibility at line 135), now crashes the caller's fallback/consensus chain instead of letting it demote this provider and advance to the next one — the exact resilience behavior the surrounding handlers exist to provide.

- **MEDIUM** (confidence: medium) `head/.claude/scripts/flatline-orchestrator.sh:1596` — `normalize_skeptic_envelope()` (base/.claude/scripts/flatline-orchestrator.sh:297-312) coerced a skeptic model's JSON output into a guaranteed `{"concerns": [...]}` shape (accepting a bare array, an object, or anything else and normalizing accordingly) before it was written to `$gpt_skeptic_prepared`/`$opus_skeptic_prepared`/`$tertiary_skeptic_prepared` and passed via `--skeptic-gpt`/`--skeptic-opus`/`--skeptic-tertiary` to the consensus builder. `extract_json_content()` (called at head/.claude/scripts/flatline-orchestrator.sh:1590-1591, 1597-1598) only strips markdown fences/BOM/prose — it does not enforce object-vs-array shape. Since this PR removes the three `normalize_skeptic_envelope` call sites without removing the function itself or replacing the shape guarantee, if a skeptic model ever emits a bare JSON array (which is exactly the failure mode the function was written to handle) the consensus builder now receives an array where it expects an object with a `concerns` key, with no shape enforcement left in this file.

## Observations

- **MEDIUM** (confidence: low) `head/.claude/scripts/construct-index-gen.sh:405` — The `construct.yaml` overlay no longer reads `.compose_with[].slug` (base/.claude/scripts/construct-index-gen.sh:330) into `compose_with_json`, and `compute_composition()` no longer merges it in (`composes_with: (([...]) + ($current.composes_with // [])) | unique` → `composes_with: [...] | unique` at head/.claude/scripts/construct-index-gen.sh:405-422). Any existing `construct.yaml` that explicitly declares `compose_with:` for a relationship not derivable from read/write overlap (e.g. a pure event producer/consumer pairing) will silently lose that declaration on the next index regeneration — no warning is emitted, the field is simply dropped. Per the PR description this narrowing to "manifest-only form" appears intentional, so this is flagged as an observation rather than a blocking defect, but it's worth confirming no in-tree `construct.yaml` currently relies on `compose_with`.
- **LOW** (confidence: low) `head/.claude/scripts/construct-index-gen.sh:301-303` — Event extraction is narrowed from a `(.name // .event // .type)` / `(.event // .name // .type)` fallback chain to `.name` (emits) / `.event` (consumes) only, and the `construct.yaml`-level `cy_emits`/`cy_consumes` override (base/.claude/scripts/construct-index-gen.sh:332-336) is removed entirely. Any manifest or construct.yaml using `.type` or the non-preferred key for its events, or relying on the construct.yaml override, will now silently produce an empty/different emits-consumes list rather than erroring — consistent with the "manifest-only form" the PR title describes, but worth double-checking against any live pack definitions before merge.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":1,"medium":1,"low":0},"excluded":0,"sprint_id":"sprint-N","ts":"2026-09-22T00:00:00Z"} -->
