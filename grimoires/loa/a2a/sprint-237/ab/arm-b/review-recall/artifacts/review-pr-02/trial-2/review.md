## Changes Required

- **CRITICAL** (confidence: high) `head/.claude/adapters/loa_cheval/audit_envelope.py:136` — Removing the `verify_for_merge`/`LOA_AUDIT_STRICT_VERIFY` plumbing deletes the only mechanism that refused an unsigned local pubkey during merge-gate verification. `_resolve_pubkey_pem` (head/.claude/adapters/loa_cheval/audit_envelope.py:136-160) now unconditionally falls back to `<key-dir>/<key_id>.pub` at line 156-159 — the "refused in strict verify" branch (`allow_local_fallback=False`) is gone entirely, and no code path in the file can request that refusal anymore. Concrete failure: anyone (or any compromised CI checkout) who can drop a self-generated `<key_id>.pub` into the key directory can forge a `signing_key_id`/`signature` pair that `audit_verify_chain` will now report as VERIFIED even for the merge-gate use case the removed `verify_for_merge` parameter existed to protect — there is no longer any "strict" mode to opt into. The docstring for `_check_trust_store` still references "issue #690" and the module still carries F1/strip-attack remediation comments (lines 481-482, 520-523) describing exactly the threat class (ATK-3/ATK-4 in the removed code) this change reopens. This is a security regression, not a simplification: the PR removes the enforcement path without removing (or replacing) the threat it defended against.

- **CRITICAL** (confidence: high) `head/.claude/adapters/loa_cheval/audit_envelope.py:462` — `_check_trust_store` now returns success for `BOOTSTRAP-PENDING` unconditionally (line 468: `if status in ("BOOTSTRAP-PENDING", "VERIFIED"): return`). The removed strict branch raised `[TRUST-STORE-BOOTSTRAP-PENDING]` specifically to stop an unsigned trust-store from passing verification "as a merge gate (ATK-3)" (base/.claude/adapters/loa_cheval/audit_envelope.py:477-482). After this change, any caller that previously relied on `audit_verify_chain(log_path, verify_for_merge=True)` to reject an unsigned/bootstrap trust-store at merge time gets a silent pass instead — and since the `verify_for_merge` keyword no longer exists on `audit_verify_chain` (head/.claude/adapters/loa_cheval/audit_envelope.py:508), any external caller still passing it (e.g. a merge orchestrator) will hit a `TypeError` rather than getting the strict behavior it asked for. Either the strict caller breaks loudly (best case) or, if it was updated to drop the kwarg, it now silently accepts an unsigned trust-store during a merge gate (worst case). No compensating control was added.

- **HIGH** (confidence: high) `head/.claude/scripts/spiral-evidence.sh:700` — Dropping the `content_bare` backtick-masking step before Pattern 2 reintroduces the exact false-positive class the surrounding comments (still present, unchanged, at lines 692-697) describe as issue #1175: "backtick prose also wraps shell commands (`bash tools/x.sh`, `yq -o=json ... file.yaml`) and CLI flags (`--inputs @/tmp/f.json`) that contain `/`+extension and matched as deliverable paths, failing cycles with IMPL_EVIDENCE_MISSING." Pattern 1 (line 679) filters those out via its whitespace/glob/leading-dash guard, but Pattern 2 (line 700) now scans the *raw* `$content` instead of the previously-masked `$content_bare`, so a sprint.md line such as `` `bash deploy.sh --config src/settings.yaml` `` will have `src/settings.yaml` extracted as a bare match (the guard clauses on Pattern 1 don't apply to Pattern 2 at all) even though it's a CLI argument inside prose, not a declared deliverable. `_pre_check_implementation_evidence` then fails the sprint with a spurious `IMPL_EVIDENCE_MISSING` for a path that was never a deliverable, which per the file's own header comment (lines 604-609) is meant to invite exactly the "narrower fix-loop re-dispatch" that burns cycle time — this is a functional regression to a bug this file was already patched for, not the "prose scanning" simplification the PR title implies.

## Observations

- **MEDIUM** (confidence: medium) `head/.claude/scripts/semver-bump.sh:57` — `get_version_from_tag`'s glob `v[0-9]*.[0-9]*.[0-9]*` still matches prerelease-suffixed tags (e.g. `v1.2.3-rc.1`) because `*` is an unrestricted glob wildcard, not a digit class — the base version relied on a separate `grep -E` pass to strip anything that didn't match a strict release-or-prerelease shape before returning it. That filter is gone. If any such tag exists in history and sorts newest under `--sort=-v:refname`, `get_version_from_tag` returns e.g. `1.2.3-rc.1`, and `bump_version` (line 80) now rejects it outright via the `^[0-9]+\.[0-9]+\.[0-9]+$` check (line 83). The caller at line 333 (`next=$(bump_version "$current" "$bump")`) does not check `bump_version`'s exit status, so the error message goes to stderr while `$next` becomes empty and the script proceeds to emit `jq` output with `"next": ""` instead of failing the run — a pre-existing gap in the caller, but one this diff makes much easier to trigger since prerelease tags are no longer filtered out before reaching `bump_version`.
- **LOW** (confidence: low) `head/.claude/scripts/semver-bump.sh:79` — `bump_version` no longer bumps prerelease tags (alpha/beta/rc), matching the PR's stated "release-only version parsing" intent; this is presumably deliberate given the PR description, but if any release process still cuts prerelease tags via `semver-bump.sh` (per CLAUDE.loa.md's "always use semver-bump.sh for version computation" rule) that workflow now has no supported path and will error instead of falling back.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":2,"high":1,"medium":1,"low":1},"sprint_id":"sprint-N","ts":"2026-09-22T00:00:00Z"} -->
