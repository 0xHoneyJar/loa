## Changes Required

- **HIGH** (confidence: high) `head/.claude/scripts/butterfreezone-validate.sh:294` — The skip heuristic (`"$file" == /* && "$file" != *.*`) is not "route vs. real file," it's "has a dot vs. doesn't." Any real absolute-path file reference that happens to lack a filename extension is now silently exempted from the missing-file check, not just Express/Fastify route params. Concrete failure scenario: a BFZ document contains `` `/opt/tools/deploy-script:main` `` (or `` `/usr/local/bin/migrate:run` ``, or any reference to an extensionless script/binary/`Makefile`/`Dockerfile`/`LICENSE` at an absolute path) pointing at a file that does not exist. Verified directly against the extraction regex used two lines above (`grep -oE '`[a-zA-Z0-9_./-]+:[a-zA-Z_L][a-zA-Z0-9_]*`'`): `` `/opt/tools/deploy-script:main` `` extracts to `file=/opt/tools/deploy-script`, `symbol=main`, which now matches the new skip condition and is silently dropped — `checked` isn't even incremented, and no `log_fail`/`log_warn` is emitted. Before this patch, that missing reference would have been correctly reported at line 301. The comment's own justification ("Real absolute paths would have extensions") is the load-bearing assumption and it's false in general — extensionless absolute-path references are common (CLI tools, scripts with shebangs, `Makefile`, `Dockerfile`, `LICENSE`). This silently weakens a validation gate for an entire class of real references, with no test covering it (both added tests that exercise "still validated" paths use `.sh` extensions).

## Observations

- **MEDIUM** (confidence: high) `head/tests/unit/butterfreezone-validate-route-false-positive.bats:77` — The second assertion in the first test (`! [[ "$output" == *"Referenced file missing: /users/"* ]]`) doesn't exercise the fix at all. `` `/users/:userId/sessions` `` never matches the extraction regex on line 272 of the script in the first place (verified: `grep -oE '`[a-zA-Z0-9_./-]+:[a-zA-Z_L][a-zA-Z0-9_]*`'` produces no match for that string, because the symbol character class stops at the `/` before `sessions` and the pattern requires a literal closing backtick immediately after the symbol). This means the multi-segment "trailing path after the route param" case was never actually broken pre-fix, and the test doesn't demonstrate what its own comment and the PR description claim (`/users/:userId/sessions` "covering the shapes with a regression suite"). Worth either dropping the misleading assertion or replacing it with a case that genuinely exercises a multi-param/trailing-segment route through the regex.
- **LOW** (confidence: medium) `head/.claude/scripts/butterfreezone-validate.sh:294` — The heuristic also has a symmetric false-negative in the other direction: a route segment that itself contains a literal dot (e.g. `/v1.2/users/:id`, or a route serving versioned/file-like paths) would fail the "no extension" test and fall through to the real existence check, reintroducing the original false positive for that narrower case. Not covered by any test; likely rare in practice but worth a one-line acknowledgment given the heuristic is explicitly dot-based.

## Overall Assessment

The regression tests are well-constructed for the literal `/factors/:factorId`-style case the issue reports, and the fix does resolve that exact false positive. However the heuristic chosen ("absolute path with no dot in it") is broader than "route pattern" and silently disables the file-existence check for a real, plausible class of legitimate absolute-path references (extensionless scripts/binaries), which is exactly the kind of silent gate-weakening this validator exists to prevent. That needs to be tightened (e.g., require a `:` followed by digits for line refs and treat everything else conservatively, or specifically require a path segment starting with `:` as in `/factors/:id` rather than testing the whole path for "any dot anywhere") or explicitly justified and covered by a test asserting the extensionless-absolute-path-is-still-checked case, before this merges.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":1,"medium":1,"low":1},"excluded":0,"sprint_id":"sprint-N","ts":"2026-09-22T00:00:00Z"} -->
