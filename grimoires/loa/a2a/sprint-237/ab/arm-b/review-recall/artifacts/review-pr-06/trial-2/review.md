## Changes Required

- **HIGH** (confidence: medium) `head/.claude/adapters/loa_cheval/providers/claude_headless_adapter.py:151` — Removing the `except OSError` / `except ValueError` handlers that used to wrap `run_subprocess_pgkill(...)` (previously at base lines 183-192) breaks the documented fallback-chain contract for this adapter. Every other failure mode in this same `try` block (`TimeoutExpired` at :166, `SubprocessOutputCapExceeded` at :171, `FileNotFoundError` at :178, `_SemaphoreExhausted` at :183) is deliberately re-raised as `ProviderUnavailableError` so the caller's model-fallback chain advances to the next provider instead of crashing (see the file's own module docstring and the `#982` / C12 comments: "on timeout the whole CLI tree dies and the fallback chain advances instead of hanging"). The removed handlers existed specifically for `OSError` (ARG_MAX / ENOMEM / exec errors from spawning `claude -p` with a very large prompt — the function logs `prompt_chars=%d` right before the call, so oversized prompts are an expected input, not a hypothetical) and `ValueError` (embedded NUL byte in the prompt breaking argv construction). After this change, either of those two — plausible, previously-anticipated — failures now propagates as a raw unclassified exception instead of `ProviderUnavailableError`, so the multi-provider fallback chain no longer advances on this failure path; it now depends on whatever catches at the caller, which is outside this file and not demonstrably a catch-all for `OSError`/`ValueError`. This is a behavior regression, not a pure cleanup, unless the PR author confirms upstream now handles these exit classes generically (please make that explicit in the PR description if so).

## Observations

- **MEDIUM** (confidence: low) `head/.claude/scripts/flatline-orchestrator.sh:1597` — `normalize_skeptic_envelope` (previously defined at base `flatline-orchestrator.sh:297-312`, called at base lines 1616/1617/1646) guaranteed the skeptic-prepared files were always a JSON *object* with a `concerns` key, converting bare arrays (`{concerns: .}`) and any other JSON shape to `{"concerns":[]}`. `extract_json_content` (this file's :261-300) only strips markdown fences/BOM/prose wrapping via `normalize_json_response` — it does not enforce the object shape. If a skeptic model ever emits a bare JSON array (which the removed function was explicitly written to handle) instead of `{"concerns":[...]}`, the prepared file fed to `scoring-engine.sh --skeptic-gpt/--skeptic-opus/--skeptic-tertiary` (:1636-1637) would be a raw array rather than an object. `scoring-engine.sh` isn't in this PR's file set, so I can't confirm how it fails, but a `jq '.concerns'` on an array either yields `null` (concerns silently dropped) or errors (`jq: error: Cannot index array with "concerns"`, aborting consensus). Worth a one-line confirmation that `scoring-engine.sh` already tolerates a bare-array skeptic file before dropping the normalization step, since the PR removes the specific code that used to prevent this shape mismatch.
- **MEDIUM** (confidence: low) `head/.claude/scripts/construct-index-gen.sh:396` — `composes_with` is now always initialized to `[]` and then fully overwritten by `compute_composition()` (:404-426) using only the automatic read/write-overlap inference; the removed code (base lines 330-333, `+ ($current.composes_with // [])`) used to preserve any construct-level `compose_with:` override from `construct.yaml` by merging it in. Any pack that declares a manual `compose_with` relationship not derivable from `writes`/`reads` overlap (e.g., two constructs that compose via a shared event rather than shared file paths) will silently lose that entry from the generated index — no warning, no error, just absence. Same pattern for events: emits/consumes extraction reverted to reading only `.name` for emits and `.event` for consumes off `manifest.json` (:302-303), and the whole `construct.yaml`-overlay block for events (base lines 330-334, `cy_emits`/`cy_consumes`) is gone, so any pack whose events are declared only in `construct.yaml` rather than `manifest.json` now gets empty `emits`/`consumes` in the index. Since the PR description frames this as an intentional revert to a "manifest-only form," this may well be correct if no shipped `construct.yaml` currently relies on the removed overlay/override behavior — but that's not verifiable from the files in this PR alone, so flagging for confirmation rather than blocking.

## Overall Assessment

The two flatline/construct-index changes read as deliberate simplifications back toward a "manifest is the single source of truth" design, consistent with the PR title, and I can't point to a concrete manifest/construct.yaml in this repo that depends on the removed behavior — so those are non-blocking observations pending confirmation. The adapter change is different in kind: it removes exception handling for two specific, previously-anticipated OS-level failure modes (oversized-argv spawn failure, embedded-NUL prompt) whose entire purpose was to keep the provider-fallback chain moving instead of raising an unclassified exception, and nothing in the diff or surrounding code demonstrates an equivalent classification now happens elsewhere. Please confirm the caller-side handling (or restore the two `except` clauses) before merging.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":1,"medium":2,"low":0},"sprint_id":"sprint-N","ts":"2026-09-22T00:00:00Z"} -->
