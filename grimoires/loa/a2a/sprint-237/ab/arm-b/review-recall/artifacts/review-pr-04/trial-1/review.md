## Changes Required

- **HIGH** (confidence: high) `head/.claude/adapters/loa_cheval/providers/base.py:472-478` — `_HEADLESS_STRIPPED_AUTH_VARS` drops `GOOGLE_GENAI_USE_VERTEXAI`, `GOOGLE_GENAI_USE_GCA`, `GOOGLE_GEMINI_BASE_URL`, and `GEMINI_CLI_USE_COMPUTE_ADC`, so `build_headless_subprocess_env()` no longer strips them. The block comment directly above the tuple (`head/.claude/adapters/loa_cheval/providers/base.py:466-471`, unchanged by this PR) still explains why this matters: "The CLI tools (claude / codex / gemini) prefer their OAuth-subscription auth path, but if any of these vars are exported in the parent process the CLI falls back to API mode — defeating the headless adapter's purpose." Those four vars are exactly the Gemini CLI's auth-mode selectors (route to Vertex AI, GCA, a custom base URL, or Compute ADC instead of the OAuth-subscription path) — the old code's own docstring called this out by name: "both the credential sub-class (e.g., GOOGLE_API_KEY) and the auth-mode-selector sub-class (e.g., GOOGLE_GENAI_USE_VERTEXAI)." With this change, if any of those four vars is set in the parent environment (e.g. an operator's shell profile, or CI env), a headless Gemini subprocess will silently route through Vertex/GCA/a custom endpoint instead of the intended OAuth-subscription path — reintroducing the class of bug that issues #879/#880 were opened for. The docstring at `head/.claude/adapters/loa_cheval/providers/base.py:493` still claims "Closes issues #879 / #880 (and symmetric for codex / gemini)," which is no longer accurate for the Gemini mode-selector case. The PR description ("shorten the headless env scrub … to credential variables only") frames this as a simplification, but it's a behavior change with security consequences that isn't called out or justified anywhere in the PR.

## Observations

- **MEDIUM** (confidence: medium) `head/.claude/scripts/flatline-orchestrator.sh:577` — in `aggregate_and_write_final_consensus()`, the `mktemp` guard was removed inside the `for f in "${input_files[@]}"` loop (`head/.claude/scripts/flatline-orchestrator.sh:572`). The function has `set -euo pipefail` in effect (`head/.claude/scripts/flatline-orchestrator.sh:47`), and `tmp=$(mktemp ...)` is a bare (non-conditional) command substitution, so a `mktemp` failure (e.g. `$TMPDIR` full or unwritable) now aborts the whole orchestrator process instead of just skipping that one voice's `verdict_quality` envelope and continuing to aggregate the rest, as the removed code did (`log "WARNING: ... skipping verdict-quality envelope for $f"` + `continue`). Low-probability trigger (mktemp essentially never fails in normal operation), but it converts a per-item graceful-degradation path into a hard failure for the whole consensus run.
- **LOW** (confidence: low) `head/.claude/scripts/flatline-orchestrator.sh:2282` — the analogous guard removed around the arbiter prompt file's `mktemp` call is likely inert: this code sits in `main()` and is not inside any enclosing loop (the function is invoked once per `--phase`), so the removed `continue` on `mktemp` failure was already unreachable/meaningless outside a loop context and `set -e` would have aborted the script either way. Flagging only for completeness since it's the same edit pattern as the finding above, not because it changes behavior.
- **MEDIUM** (confidence: medium) `head/.claude/scripts/loa-status.sh:29-35` — the `NO_COLOR`/`[[ -t 1 ]]` guard around the color variable definitions was removed; `RED`/`GREEN`/`YELLOW`/`CYAN`/`BOLD`/`NC` are now always set to their ANSI escape sequences. The human-readable output path (`head/.claude/scripts/loa-status.sh:501-548` etc.) unconditionally emits these via `echo -e`, so raw escape codes will now leak into output whenever `loa-status` is piped/redirected (non-tty) or run with `NO_COLOR=1` set, e.g. in CI logs or when another tool captures/parses its human-readable output. The `--json` path is unaffected (it does not reference the color vars), so this is scoped to the human-readable branch.
- **LOW** (confidence: medium) `head/.claude/scripts/loa-status.sh:43-67` — unknown-flag validation was removed entirely (the old `UNKNOWN_ARGS` tracking, `USAGE_LINE`, and the `dx_unknown_flag`/`exit 2` block are gone). Outside `--economy` mode, any unrecognized flag (e.g. a typo like `--josn`) is now silently swallowed into `ECONOMY_ARGS`, which is never read since `ECONOMY_MODE` is false, and the script proceeds as if no flag were given rather than erroring with a usage message. This trades a loud, actionable error for a silent no-op on user typos; not a security issue, but a real UX regression for a CLI-ergonomics-facing script.

## Overall Assessment

The `flatline-orchestrator.sh` and `loa-status.sh` trims are reasonable simplifications individually, but the `loa-status.sh` NO_COLOR/tty regression and the unknown-flag swallowing are real (if non-blocking) correctness losses worth a follow-up. The blocking issue is the `base.py` change: it is described as a scrub of "credential variables only," but it silently removes Gemini's auth-mode-selector vars from the strip list, undoing part of the fix from issues #879/#880 that the surrounding (unedited) docstring still describes as intact. This needs to either restore those four vars to `_HEADLESS_STRIPPED_AUTH_VARS`, or the PR needs to explicitly justify and document dropping mode-selector stripping as an intentional scope change (and correct the now-stale "Closes issues #879 / #880" claim).

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":1,"medium":2,"low":2},"sprint_id":"pr-review","ts":"2026-09-22T00:00:00Z"} -->
