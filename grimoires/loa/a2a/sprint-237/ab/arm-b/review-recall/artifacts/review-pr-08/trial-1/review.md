## Changes Required

- **HIGH** (confidence: high) `head/.claude/scripts/ledger-lib.sh:157` — the content guard was relaxed from "valid JSON *and* a single top-level object" (`jq -es 'length == 1 and (.[0] | type == "object")'`) to `jq empty` (valid JSON of *any* type). `jq empty` accepts the literal string `null`. `null` then flows into `updated_content=$(echo "$content" | jq --arg ts "$(now_iso)" '.last_updated = $ts')` at line 179 — jq auto-vivifies `null | .last_updated = $ts` into `{"last_updated": "<ts>"}`, a non-empty object, so the second guard (`[[ -z "$updated_content" ]]`) does not catch it either. The atomic write then proceeds and replaces the entire ledger with a single-field object, discarding every cycle/sprint record. This is exactly the failure mode the function's own comment describes fixing (bug 20260808-a008c6: "passed through the last_updated jq stamp … and truncated the ledger to a 1-byte newline while every caller reported success") — the PR reopens it via a different input (`null` instead of empty string) while leaving the bug-reference comment in place, giving false confidence that the guard is unchanged. Restore the object-type check (or at minimum reject `null`/non-object top-level values) before removing it.

- **HIGH** (confidence: medium) `head/tools/check-no-swallowed-jq.sh:138` — `in_heredoc { … next }` now unconditionally skips scanning of every heredoc body, whereas the base version computed `hd_exec` (base `tools/check-no-swallowed-jq.sh:161-163`) and scanned heredoc bodies that are actually executed — piped to an interpreter (`<<EOF | bash`, `ssh host <<EOF`, `python3 <<EOF`) or left unquoted — for the same swallowed-jq shape this tool exists to catch. This tool is described in its own header as a tripwire against KF-004/KF-015 ("zero-findings canonical verdicts masking real findings" / "silent-clean red-team gate pass"); gate-critical and red-team-* scripts are exactly the kind of code that assembles remote/piped shell via heredocs (e.g. `ssh $host <<'REMOTE' … jq … || echo fallback … REMOTE`). After this change, a swallowed-jq shape written inside any heredoc — executable or not — is silently invisible to the scanner, regardless of whether it will actually run. The new "Step 1" comment justifies this by only citing the fixture/bats-planted-example false-positive case, but the fix for that (only skip *non-executed* heredocs) was already what `hd_exec` provided; discarding the whole distinction trades a narrow false-positive fix for a much broader false-negative hole in a security lint tool. The header's "Tripwire scope" section is not updated to disclose this new blind spot either.

## Observations

- **MEDIUM** (confidence: medium) `head/.claude/scripts/ledger-lib.sh:157` and `:179` — `printf '%s' "$content"` was replaced with `echo "$content"` in both places. Not currently exploitable (a JSON value consisting solely of `n`/`e`/`E` characters after a leading `-`, e.g. `-n`/`-e`, which bash's `echo` builtin would swallow as an option string, is never valid JSON, and the `jq empty` gate blocks anything else that isn't valid JSON), but it's a needless reintroduction of the `echo`-vs-`printf` class of bug for no stated benefit, and it removes a layer of defense-in-depth that would otherwise still be safe if the content check above it is ever loosened further.

- **LOW** (confidence: low) `head/.claude/adapters/loa_cheval/providers/agy_headless_adapter.py:157` — the `except ValueError` handler around `run_subprocess_pgkill` (removed; was directly below the retained `except OSError` at line 157) converted a real, previously-anticipated failure mode ("un-execable argv (embedded NUL in the prompt?)" — CPython's subprocess/exec path raises `ValueError` for embedded null bytes in argv) into a graceful `ProviderUnavailableError` that lets the provider chain fall back to the next model. With the handler gone, a prompt/diff containing an embedded NUL byte now propagates an uncaught `ValueError` out of `_execute`, past the retained `except _SemaphoreExhausted` (line 166, a sibling `except` on the outer `try`, so it does not catch this), instead of degrading to the next provider like every sibling failure mode in this same block (`TimeoutExpired`, `SubprocessOutputCapExceeded`, `FileNotFoundError`, `OSError`) is designed to. Marked speculative/low because the outer caller (chain orchestrator) is not in the diff and may itself catch bare exceptions and fail over — but that isn't visible from this file, and the surrounding code's own stated intent ("WALK the chain, never crash with a raw OSError", line ~152) is now inconsistent for this one exception type with no removal rationale given in the PR description.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":2,"medium":1,"low":1},"excluded":0,"sprint_id":"sprint-N","ts":"2026-09-22T00:00:00Z"} -->
