## Changes Required

- **HIGH** (confidence: medium) `head/.claude/adapters/loa_cheval/providers/claude_headless_adapter.py:151-182` — Removing the `except OSError` / `except ValueError` clauses around `run_subprocess_pgkill(...)` re-opens a failure path this same class already treats as real: `health_check()` at `head/.claude/adapters/loa_cheval/providers/claude_headless_adapter.py:262` still catches `(subprocess.TimeoutExpired, OSError)` for the exact same CLI invocation family, which is direct evidence `OSError` is an observed, not speculative, failure mode for spawning `claude -p` (ARG_MAX exceeded, ENOMEM, exec errors). With the two clauses removed, an `OSError`/`ValueError` raised inside `run_subprocess_pgkill` now propagates past the `with _acquire_slot(...)` block uncaught — the outer `try/except _SemaphoreExhausted` at line 150/183 does not catch it — instead of being converted into `ProviderUnavailableError`. That breaks the fallback-chain contract this file otherwise goes out of its way to preserve (see the neighboring `subprocess.TimeoutExpired` and `SubprocessOutputCapExceeded` handlers, and the code comment at line 153-155 explicitly framing timeout-handling around "the fallback chain advances instead of hanging"). Concretely: an oversized prompt that trips the OS argv-length limit (ARG_MAX) or a prompt containing an embedded NUL byte will now crash the whole cheval consensus run instead of letting flatline fall back to the next model/provider. If `run_subprocess_pgkill` itself already normalizes these exceptions internally (its source isn't in this diff), this finding doesn't apply — but nothing in the diff or PR description states that, and the PR description just says "drops the extra except clauses," treating them as dead weight without addressing where those exceptions now go.

## Observations

- **MEDIUM** (confidence: medium) `head/.claude/scripts/flatline-orchestrator.sh:1596-1598,1626` — `normalize_skeptic_envelope` is dropped from the consensus path, but its two behaviors (wrap a bare JSON array into `{"concerns": [...]}`, and coerce anything else to `{"concerns":[]}`) are not obviously subsumed by `extract_json_content`/`normalize_json_response` (line 261-295), whose documented job is BOM/fence/prose stripping, not shape normalization. If a skeptic model legitimately returns a top-level JSON array of concern objects (which is exactly the case the removed function special-cased), the scoring engine now receives a bare array where it previously always received `{"concerns": [...]}`. Whether the Python scoring engine (`$SCORING_ENGINE`, not in this diff) already tolerates both shapes can't be confirmed from the files here; if it expects a dict and calls `.get("concerns", ...)` this becomes a hard crash instead of the graceful `{"concerns":[]}` default. Worth confirming against the scoring engine's actual input handling before merging.

- **MEDIUM** (confidence: medium) `head/.claude/scripts/construct-index-gen.sh:301-303` — Reverting `emits`/`consumes` extraction from `(.name // .event // .type)` / `(.event // .name // .type)` to bare `.name` / `.event` silently drops any manifest.json event entries that use the alternate key spellings — `[.[].name // empty]` produces `null`→filtered-`empty` for an entry shaped `{"event": "foo"}`, with no warning emitted. If any existing pack manifests in the repo (not present in this diff's file set) use the `.event`/`.type` spellings, their emitted/consumed events will silently vanish from the generated construct index after this change, rather than failing loudly.

- **MEDIUM** (confidence: medium) `head/.claude/scripts/construct-index-gen.sh:310-336,404-426` — `construct.yaml`'s `compose_with` field and its `events.emits`/`events.consumes` overlay are no longer read at all (the `cy_emits`/`cy_consumes`/`compose_with_json` extraction block, and the `+ ($current.composes_with // [])` merge in `compute_composition`, are both removed). Any pack author who hand-authored `compose_with:` entries in `construct.yaml` to express a composition relationship the automatic write/read-overlap heuristic can't infer (e.g. event-based rather than storage-based coupling) will have those manual entries silently discarded on regeneration, with `composes_with` now purely computed. This is presented as an intentional "revert to manifest-only form" in the PR description, so it may be a deliberate, accepted scope reduction — but it's a silent behavior change for any construct.yaml currently relying on the overridden fields, and no deprecation notice or migration path is visible in the diff.

## Overall Assessment

This is a defensible cleanup in principle (removing over-general fallback code paths), but the adapter change removes exception handling that this same file, two methods down, treats as a real and expected failure mode for the identical subprocess invocation — that inconsistency is the blocking issue. The two shell-script observations are lower-confidence because the actual consumers (`$SCORING_ENGINE`, pack `manifest.json`/`construct.yaml` files) aren't part of this diff, so I can't prove they're exercised, but they represent silent-failure risk worth a sentence of confirmation from the author before merge.

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":1,"medium":3,"low":0},"excluded":0,"sprint_id":"pr-review","ts":"2026-09-22T00:00:00Z"} -->
