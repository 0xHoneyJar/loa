## Changes Required

- **HIGH** (confidence: high) `head/.claude/adapters/loa_cheval/providers/claude_headless_adapter.py:159-182` — removing the `except OSError` / `except ValueError` handlers around `run_subprocess_pgkill(...)` lets those exceptions escape uncaught instead of being converted to `ProviderUnavailableError`. The inner `try` (line 152) now only catches `subprocess.TimeoutExpired`, `SubprocessOutputCapExceeded`, and `FileNotFoundError`; the enclosing `try` (line 150) only catches `_SemaphoreExhausted` (line 183). A prompt large enough to blow `ARG_MAX` (E2BIG) or a fork failure under memory pressure (ENOMEM) raises `OSError` from the subprocess spawn — this is exactly the case the deleted comment documented ("claude -p spawn failed (ARG_MAX / ENOMEM / exec error?)"). Likewise an embedded NUL byte in the prompt raises `ValueError` from argv construction ("un-executable argv (embedded NUL in the prompt?)"). Both now propagate as raw stdlib exceptions out of `invoke()` instead of the classified `ProviderUnavailableError` that every other failure mode in this same function (timeout, output-cap, missing binary, semaphore exhaustion) is deliberately normalized into so the fallback chain can route around the failure. Every sibling handler in this function exists specifically to preserve that chain-routing contract; removing just these two silently reintroduces the crash-instead-of-fallback behavior the contract was built to prevent, with no replacement handling added anywhere in the diff.

- **HIGH** (confidence: medium) `head/.claude/scripts/flatline-orchestrator.sh:1597-1598,1626` — `normalize_skeptic_envelope` is deleted and its three call sites (gpt/opus/tertiary skeptic prepared files) removed, but `extract_json_content` (line 261) only strips markdown fences/BOM/prose via `normalize_json_response` — it does not reshape the JSON's top-level type. If a skeptic model responds with a bare JSON array (e.g. `[{"concern": "..."}]`) instead of `{"concerns": [...]}`, that array now flows straight into `$gpt_skeptic_prepared` / `$opus_skeptic_prepared` / `$tertiary_skeptic_prepared` and is passed to `$SCORING_ENGINE --skeptic-gpt/--skeptic-opus/--skeptic-tertiary` unchanged. The deleted function's own branching (`elif type == "array" then {concerns: .}`) is direct evidence this exact shape was previously observed and had to be coerced; every other extraction in this file defaults to an object (`'{"scores":[]}'`, `'{"concerns":[]}'`, `'{"improvements":[]}'`), implying the scoring engine expects an object and will fail (or silently misbehave) on `.concerns`-style field access against a bare array. No equivalent guard was added elsewhere in the diff.

- **HIGH** (confidence: high) `head/.claude/scripts/construct-index-gen.sh:302-303,306-308,396,414-422` — `compute_composition` (414-422) now builds `composes_with` purely from the auto-computed writes/reads overlap and no longer merges it with any value carried on `$current.composes_with`; `build_construct_entry` (396) always seeds `composes_with: []`, and the `compose_with_json` extraction from `construct.yaml`'s `.compose_with[].slug` (previously read via `yq` in the overlay block) is gone entirely along with its `--argjson compose_with` wiring. Concretely: a pack that declares an explicit `compose_with:` relationship in its `construct.yaml` (e.g. to force a composition that has no read/write overlap, or to state a relationship the write/read heuristic can't infer) will have that declaration silently dropped from the generated index — no warning, no error, just a smaller `composes_with` array than the pack author configured. The same overlay block (325-336) also lost its `cy_emits`/`cy_consumes` extraction, so `construct.yaml` can no longer override or supplement a pack's declared events at all, and the manifest-level extraction itself (302-303) narrowed from `(.name // .event // .type)` / `(.event // .name // .type)` to single-key lookups (`.name` for emits, `.event` for consumes) — a manifest using `.type` (or `.event` for an emits entry) now silently yields an empty events list for that entry instead of falling back. The PR description frames this as intentionally "reverting to the manifest-only form," but nothing in the diff updates docs, warns operators, or demonstrates that no in-tree `construct.yaml` currently relies on either dropped override, so this lands as a silent, undocumented feature/data loss for any pack that does.

## Observations

None beyond the above — all three defects found are concrete regressions with clear failure scenarios; no independent minor/style issues stood out in the diff.

## Overall Assessment

All three hunks in this PR delete previously-added defensive/override behavior (spawn-error classification, skeptic-envelope shape normalization, construct.yaml event/compose_with overlays) without any accompanying evidence that the removed paths are unreachable or that downstream consumers were updated to tolerate the new behavior. Each has a concrete, nameable failure input (an oversized prompt, a skeptic model returning a bare array, a construct.yaml with an explicit `compose_with:` or non-`name`/`event` keyed event). Recommend restoring the three handlers/normalizations, or — if this really is an intentional revert — landing it with evidence (e.g. a grep of all in-tree `construct.yaml` files showing none use `compose_with`/event overrides) and a CHANGELOG note, rather than as a silent "chore".

<!-- LOA-VERDICT {"gate":"review","verdict":"CHANGES_REQUIRED","counts":{"critical":0,"high":3,"medium":0,"low":0},"excluded":0,"sprint_id":"sprint-N","ts":"2026-09-22T00:00:00Z"} -->
