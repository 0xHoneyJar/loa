# Sprint 1 implementation report — cycle-124 "model-generation floor" (global sprint-235)

Branch `feature/cycle-124-model-generation-floor`, base `origin/main` 80be4b0f. Sprint goal (sprint.md): every Anthropic HTTP request cheval builds is shaped for the current generation and observable, the catalog names the current models, and the review/audit gates and production ledgers are honest — with a live-check scaffold committed for the credentialed operator step.

## Commits (one per unit, no squash — SDD §7)

| Unit | Commit | Task |
|---|---|---|
| U1a catalog | `f5ea53f4` feat(catalog) | 1.3 |
| U3 ledger isolation | `f4375abc` feat(ledger) | 1.1 |
| U0 MODELINV schema | `6164feb4` feat(modelinv-schema) | 1.2 |
| FR-5 gates | `37b0ee80` fix(gates) | 1.7 |
| U1b defaults | `e14198a1` feat(defaults) | 1.4 |
| U2 adapter | `a421334a` feat(adapter) | 1.5 + 1.6 |
| live scaffold | `fff40901` test(live) | 1.8 |
| repo map | `f3dcf310` chore(repo-map) | 1.10 |
| AC-gate scoping | `96387449` fix(validate-ac) | 1.10 |
| docs / catalog-suite fix | `789a0d0b` docs(changelog), `206c6000` docs(cheval), `19ffd850` test(catalog) | 1.10 / 1.3 |
| round-1 adapter fixes (H1 H2 H3 M1 M2 M4 L1 L2 L3 L4) | `0f8c1df6` fix(adapter) | review round 1 |
| round-1 ledger fixes (H5 M3 M5 M6 L6) | `fcc31c5e` fix(ledger) | review round 1 |
| round-1 gate fix (H4) | `77f9180f` fix(gates) | review round 1 |
| round-1 tooling fixes (M7 M8 M9 L7 L8) | `16b88d56` fix(tooling) | review round 1 |
| repo map + changelog | `632d0961` chore(repo-map) | review round 1 |
| round-2 dissent DISS-001 (readers fail closed) | `12b6067e` fix(readers) | review round 2 |
| prototype-safe budget lookup (round-2 concern 10) + repo map | `e14b3e7c` fix(tooling) | review round 2 |
| audit dissent AUD-D1 (live workflow manual-only + environment) | `44e1feab` fix(ci) | audit |
| KF-004 recurrence + attempt row | `f740be39` docs(kf) | audit |
| audit dissent residual + environment-protection precondition | `7665775b` docs(ci) | audit |
| audit slice B fixes (isolation, sidecar, readers) | `2b43baf4` fix(ledger) | audit |
| audit slices A/D fixes (timeout, breaker, tests) | `1991390f` fix(adapter) | audit |
| audit slice D fixes (BB retry budget, pricing schema) + repo map | `4670b22f` fix(tooling) | audit |
| changelog | `087fc39e` docs(changelog) | audit |

Local-only artifacts (a2a is gitignored): this report, `flatline-prd.md`, `rollback-proof.md`. Tracked evidence: `grimoires/loa/reports/2026-09-17-cycle-124-catalog-evidence.md`.

## AC Verification

### AC-6.1 … AC-6.4 (ledgers byte-identical across the suites; tripwire self-tests; rotation executed; discovery test)
- **Status**: ✓ Met
- **Evidence**: `.claude/adapters/loa_cheval/metering/ledger.py:49` (`resolve_cost_ledger_path`, env > config > default, realpath, symlink rejected; since review round 1: an existing non-regular target — directory, `/dev/null` — is `INVALID_CONFIG` at resolve time, and a relative config/default path anchors at the project root like the MODELINV twin while a relative env path stays CWD-relative), `.claude/adapters/loa_cheval/metering/ledger.py:206` (`O_NOFOLLOW` writer); `.claude/adapters/tests/conftest.py:17` + `tests/replay/conftest.py:1` (autouse set-if-unset); `.claude/adapters/tests/test_ledger_isolation.py:234` (`test_mock_run_leaves_repo_ledgers_byte_identical` — sha256 of both ledgers unchanged by a mock cheval run), `:219` (the invariant itself: both ledger env vars set and neither resolving under the repo's `.run/` — holds under an operator-level export too), `:90` (config path anchored at the project root from a foreign CWD; env path CWD-relative), `:116` (subprocess twin: fresh interpreter, no env, foreign CWD ⇒ `<repo>/.run/cost-ledger.jsonl`), `:160` (directory and `/dev/null` refused with `not a regular file`), `:66` (round-2 DISS-001: the reader default raises the same `ConfigError` — a refused path is never a silent read of another file; rollup CLI exit 2, explicit `--ledger` bypasses the resolver); `tools/check-ledger-hygiene.sh:158` (exit 1 on a violation, SKIP on absent, sentinel fixtures under `tests/fixtures/ledger-hygiene/`; the MODELINV rule matches `cheval-e2e-` without the `/tmp/` root since round 1 — macOS mkdtemp lives under `/var/folders/`); `tests/integration/ledger-hygiene-tripwire.bats:18` (LH-1…LH-7, incl. seal + move keeps `verify-chain` green and restarts at GENESIS); `tests/unit/ledger-isolation-discovery.bats:35` (DS-1: every spawner sets both vars; `KNOWN_UNCOVERED` is empty); `.github/workflows/bats-tests.yml:262` (post-test tripwire step, ordering comment) + `.claude/scripts/git-hooks/pre-push-audit`. Rotation executed on this host 2026-09-17T09:24Z per `grimoires/loa/runbooks/ledger-hygiene-rotation.md`: `verify-chain` OK 1347 entries → `seal MODELINV` → `.run/archive/model-invoke-20260917T092404Z.jsonl` (verify-chain OK on the archive) and `.run/archive/cost-ledger-20260917T092405Z.jsonl` (precondition: 0 priced mock rows); `tools/check-ledger-hygiene.sh` now exits 0 with two SKIP lines and no fresh test rows appeared after the full suites ran.

### AC-3.1 … AC-3.6 (`cheval --dry-run --model opus` → `claude-opus-5`, `fable` → `claude-fable-5-1`; drift gates; TS twin 1M budgets + dist parity; advisor tier via live config; AC-3.5 consumer matrix; v3 smoke flip + `cheval-input-gate.bats` in CI)
- **Status**: ✓ Met
- **Evidence**: `tests/unit/cycle-124-anthropic-catalog.bats:60` (c124-1.3-1…5: `opus` → `claude-opus-5`, `fable` → `claude-fable-5-1`, self-maps, 4-8 still pinnable), `tests/unit/cycle-124-anthropic-catalog.bats:129` (c124-1.3-11: `tools/regen-model-artifacts.sh --check` = gen-adapter-maps + gen-bb-registry + check-bb-dist-fresh + checksum, all green — skipped, not failed, without npm / BB `node_modules`; c124-1.3-12 idempotence), `:170` (c124-1.3-14: a missing toolchain is exit 3 `TOOLCHAIN`, never exit 1 drift — `tools/regen-model-artifacts.sh:55` `precheck_toolchain`); `tests/unit/cycle-124-anthropic-catalog.bats:95` (c124-1.3-8 TS twin budgets) and `:110` (c124-1.3-9 dist twin parity) — **the twin carries 160 000 per Anthropic HTTP id, not 1M**: SDD §2.1 (Flatline SKP-003) superseded the sprint bullet's "1M budgets" with `effective_input_ceiling − 20 000`, so BB never prepares more than cheval's pre-flight accepts; `.claude/adapters/tests/test_anthropic_catalog_floor.py:225` (AC-3.4 through the loader cheval uses: `load_advisor_strategy(REPO_ROOT).resolve(role='review', skill='reviewing-code', provider='anthropic')` ⇒ tier `advisor`, `claude-opus-5`) and `:237` (the yaml + example text), `:123` (ceiling is the computed `min(180000, ctx − default)` per entry); AC-3.5 matrix: `.claude/adapters/tests/test_input_size_consumers.py:173` (120K/160K/180K/200K/900K × gate × streaming; pre-flight owns >180K, the 36K wall owns the band below under the kill switch, nothing above the ceiling dispatched; CWD-independent since round 1 — the `_no_persona` fixture at `:113` stubs `_load_persona_parts`, the loader the Anthropic path actually calls, so the matrix is green from the repo root and from `.claude/adapters`; the same stub landed in the four sibling suites) + `tests/integration/input-size-consumers.bats:59` (BB `getTokenBudget` = 160K, `progressiveTruncate` on a 900K-token fixture with a 300K operator budget prepares ≤ 160K, adversarial figure = 160K); `.github/workflows/cycle099-sprint-1e-tests.yml:73` (`--to-v3` + v3 schema smoke), `.github/workflows/bats-tests.yml:214` (`cheval-input-gate.bats` in the maintenance run list; G1/G8/G7 re-baselined, 8/8 green).

### AC-2.1 … AC-2.6 (defaults table incl. non-streaming cap and catalog clamp; effort on chain walk; ceiling + default ≤ window; `--max-tokens 0` rejected; per-model effort validity)
- **Status**: ✓ Met
- **Evidence**: `.claude/adapters/loa_cheval/providers/base.py:168` (`default_max_tokens`: Anthropic min(64K | 16K non-streaming, catalog `max_output_tokens`); 4096 for other providers, missing clamp source, or `LOA_CHEVAL_LEGACY_WIRE`); `.claude/adapters/cheval.py:712` (`_hop_max_tokens`: per-hop default, explicit value clamped with a warning — on every provider hop, disclosed in the PR body), `.claude/adapters/cheval.py:600` (`_LEGACY_TRANSPORT_INPUT_WALL = 36_000`); `.claude/adapters/tests/test_max_tokens_defaults.py:61` (16K non-streaming), `:119` (`effective_input_ceiling + default ≤ context_window` for every HTTP entry), `:163` (`--max-tokens 0`/negative ⇒ `INVALID_INPUT` via the CLI); `.claude/adapters/tests/test_chain_walk_audit_envelope.py:548` (each hop gets its own default; explicit 200K clamps to 128K on opus-5 and passes through on headless; effort travels on every hop and into the MODELINV payload); the CLI hop honours it too since round 1 — `.claude/adapters/loa_cheval/providers/claude_headless_adapter.py:255` (`request.effort` is the first `_resolve_effort` candidate) pinned by `.claude/adapters/tests/test_claude_headless_adapter.py:231` (argv capture: `--effort xhigh` reaches `claude -p`, outranking metadata and model-config); `.claude/adapters/tests/test_anthropic_effort_families.py:89` (xhigh → high on the 4.6 generation; omitted on Sonnet 4.5 / Haiku 4.5; pass-through elsewhere), `:115` (falsifiable totality: every catalog HTTP id falls in exactly one of the three prefix tuples — `_EFFORT_FULL_PREFIXES` at `.claude/adapters/loa_cheval/providers/anthropic_adapter.py:58`); `tests/unit/cycle-124-effort-flag.bats:88` (CLI surface incl. clamp, `--max-tokens 0`, invalid effort). AC-2.5 caller grep: bounded dispatchers pass explicit budgets — `flatline-orchestrator.sh call_model` 16 000 / 16 000 (the scorer moved from 4 000 in round 1: it runs on the retargeted `opus` voice with adaptive thinking sharing the budget — `.claude/scripts/flatline-orchestrator.sh:128`, B3 re-pinned at `tests/unit/cycle-124-dispatch-budgets.bats:82`), `model-adapter.sh --max-tokens` forward, `adversarial-review.sh` dissent 16 000, BB `cheval-delegate.ts` `request.maxOutputTokens` (config default 16 000) — pinned by `tests/unit/cycle-124-dispatch-budgets.bats` B1–B9.

### AC-1.1, AC-1.2 (per-family body tests; schema-typed flag; thinking ⇒ no temperature; Bedrock unaffected); AC-1.3 committed as a skipped live scaffold with the operator command
- **Status**: ✓ Met
- **Evidence**: `.claude/adapters/loa_cheval/providers/anthropic_adapter.py:191` (`body["thinking"] = {"type": "adaptive"}` iff `params.thinking_adaptive is True` and not legacy wire); `.claude/adapters/tests/test_anthropic_thinking.py:92` (six adaptive ids emit it with no `temperature`/`top_p`/`top_k`, never `budget_tokens`/`disabled`; four non-adaptive ids omit it; streaming inherits; byte-identical when the flag is absent or the kill switch is set; thinking-block-first fixtures parse on both transports; non-default temperature dropped with a logged warning), `:273` (Bedrock body identical with and without the flag), `:132` (scoped legacy-wire claim: under `LOA_CHEVAL_LEGACY_WIRE` every HTTP family in the LIVE catalog sends only model/messages/max_tokens plus temperature where the catalog still allows it — the flag removes what this cycle added; the temperature omission is catalog-driven and is not restored on entries that gained `temperature_supported: false` this cycle), `:174` (the drop warning is once per model per process, INFO afterwards — `anthropic_adapter.py:178`); the truncation flag keys on the thinking CLASS: `.claude/adapters/cheval.py:737` (`_entry_thinking_class`: `thinking_adaptive` OR `temperature_supported: false` — Fable's always-on shape — OR a returned thinking trace), `.claude/adapters/tests/test_anthropic_cache_control.py:307` (opus-5 / fable-5-1 / trace-only cases set `operator_visible_warn` on `stop_reason: max_tokens`), `:332` (a plain stop is not flagged); `.claude/data/schemas/model-config-v3.schema.json:151` (typed `thinking_adaptive` boolean, `params.additionalProperties: false`); AC-1.3 scaffold: `tests/replay/test_cycle124_live_floor.py:129` (`test_thinking_shape_accepted` ×4 models, skipped without `LOA_RUN_LIVE_TESTS=1` + key; operator command and red-result rule in the module docstring), pinned skip count `tests/unit/cycle-124-live-scaffold.bats:26`.

### AC-4.1, AC-4.2, AC-4.4 (block shape; telemetry end to end; other adapters byte-identical); AC-4.3 committed as a skipped live scaffold
- **Status**: ✓ Met
- **Evidence**: `.claude/adapters/loa_cheval/providers/anthropic_adapter.py:529` (string when no marker; text blocks with exactly one `cache_control` when marked; legacy wire ignores markers); `.claude/adapters/tests/test_anthropic_cache_control.py:104` (block shape, one breakpoint, identical visible text), `:74` (PRD FR-4, corrected in round 1 / SDD-R1-1: with no persona the WHOLE `--system` payload is the marked stable prefix — `.claude/adapters/cheval.py:219` `_persona_messages`), `:84` (the real Bridgebuilder prefix under `--agent reviewing-code`, which has no persona.md, through `_persona_messages` → `_transform_messages` yields exactly one breakpoint), `.claude/adapters/tests/test_claude_headless_adapter.py:358` (AC-4.1 on the CLI hop: `Usage.cache_creation_input_tokens` / `cache_read_input_tokens` on the result, not only metadata), `:255` (`Usage.cache_*` → MODELINV `tokens_cache_read`/`tokens_cache_creation` + `pricing_snapshot.cache_read_per_mtok`/`cache_write_per_mtok` + CLI JSON `usage` fields), `:152` (OpenAI Responses body identical for split vs merged system messages; Google and headless twins follow), `tests/unit/cycle-124-cache-telemetry.bats:1` (streaming + non-streaming fixtures, payload-schema validation of the emitted envelope); pricing: `.claude/adapters/tests/test_cache_read_pricing.py:54`; AC-4.2 mixed fixture: `tests/fixtures/metering/mixed-cost-ledger.jsonl` (three pre-cache rows without the fields, three post-cache rows with them; hand-computed total 113 750 micro-USD over 6 calls) runs clean through `metering/rollup.py` (`tests/unit/cycle-124-cache-telemetry.bats:170`), `cost-report.sh` (`:184`) and `tools/model-economy-roll-up.sh` over the mixed-writer MODELINV fixture (`:194`); the readers resolve their default through the writer's resolver over the MERGED config since round 1 and fail closed like it since round 2 (`.claude/adapters/loa_cheval/metering/rollup.py:44`, `.claude/scripts/cost-report.sh:36` resolver + `:93` exit-2 gate after argument parsing, pinned by `tests/unit/cheval-cost-rollup.bats:74` and `:101` — the old reader read only the system-defaults yaml and pointed at a file the writer never wrote; a resolver-refused path used to fall back silently); AC-4.3 scaffold: `tests/replay/test_cycle124_live_floor.py:149` (`test_second_identical_call_reads_the_cache`, ≥ 4096-token prefix, skipped without a key; since round 1 the body is built by the production path — `cheval._persona_messages` under `--agent reviewing-code` + `AnthropicAdapter._transform_messages` on the real BB prefix — so the FR-4 shape is what gets probed).

### AC-5.1, AC-5.2 (seven new bats cases incl. fail-closed; doc-lock; consumer-inventory and `test_golden_path.bats` green unmodified)
- **Status**: ✓ Met
- **Evidence**: `.claude/scripts/golden-path.sh:94` (`_gp_verdict_gate`: rc ≠ 0 or `.consistent ≠ true` denies with violations on stderr; pass only on `APPROVED`; audit cross-check `excluded` ⇔ `excluded_confirmed`); `tests/unit/golden-path-c8-verdict-trailer.bats:206` (exit-2-empty-stdout fail-closed; 17 new cases, 11 + 3 red before), `:319` (round 1: the audit path also runs the review trailer through `verdict-derive.sh` — an audit APPROVED over a review APPROVED with `counts.critical=1` denies both gates; `.claude/scripts/golden-path.sh:184`), `:338` / `:354` (a non-integer `excluded` / `excluded_confirmed` — `1.0`, `"1"` — denies instead of reading as 0; `.claude/scripts/golden-path.sh:117` `_gp_trailer_int` extracts the LAST whitespace-tolerant trailer the way `verdict-derive.sh` does), `:324` (doc-lock: derived-verdict hash, `grep -A 100` only as the labelled fallback); `tests/unit/c119-c8-consumer-inventory.bats` 12/12 unmodified; `tests/integration/test_golden_path.bats` 47/47 — **modified by one line**: its setup() did not copy `compat-lib.sh` (sourced by golden-path.sh since PR #1028) and the suite was 47/47 red on `main`; the fix touches the harness copy list only, no expectation changed (NOTES Decision Log 2026-09-17). All 14 trailer-bearing a2a files re-derive to the same gate result under old and new logic.

### Adapter pytest ≥ 1937 passed, 0 failed; all three `model-registry-drift` checks green locally; REPO-MAP regenerated; `known-failures.md` KF-002 row added via `kf-write-lib.sh`
- **Status**: ✓ Met
- **Evidence**: `cd .claude/adapters && python3 -m pytest tests -q -p no:cacheprovider` → `2190 passed, 6 skipped, 175 subtests passed` after round 1 (2174 before; baseline 1937); `bash tools/regen-model-artifacts.sh --check` → gen-adapter-maps, gen-bb-registry, check-bb-dist-fresh, checksum all OK (`tests/unit/cycle-124-anthropic-catalog.bats:129`); `grimoires/loa/REPO-MAP.md` regenerated in `f3dcf310` (`tests/unit/repo-map*.bats` 6/6); `grimoires/loa/known-failures.md:241` (KF-002 attempt row, evidence `f5ea53f4 + e14198a1`, written with `kf-write-lib.sh attempt` after a `--file` sandbox run; KF-006 recurrence 5 → 6 + attempt row likewise).

## Tasks completed

- **1.1 (subagent, finished by the lead)** — resolver, `O_NOFOLLOW` writer, reader precedence (rollup, cost-report), pytest conftests, eight bats suites + TS e2e exports, tripwire script + CI step + pre-push hook, runbook; the subagent hit its session cap at ~90 %.
- **1.2 (subagent)** — four optional payload fields, 11-row chained mixed-writer fixture, 33 pytest cases proving every consumer is invariant to the new fields (none aggregates them yet), backcompat V14 replays the fixture.
- **1.3** — catalog rewrite (Opus 5 / Fable 5.1 new; 1M/128K family; v2 input fields removed; ceiling + calibration; typed params; `structured_json`; cache rates; Sonnet 5 $2/$10; aliases), v3 schema `params` typing, BB codegen derives `maxInput` from the ceiling, `tools/regen-model-artifacts.sh`, 404 chain-walkable, CI grafts, evidence file. Pins re-baselined: cycle-114 registry, gen-adapter-maps, input-gate G1/G7/G8 (G1/G8 pinned the pre-cycle-109 `[input-gate]` marker; G7 the pre-split `max_input_tokens`), migrate M19.3 → `--to-v3`, overlay-source C1/D1/F1 (red on main since cycle-114), gen-bb-registry-codegen T3/T10.
- **1.4** — `default_max_tokens`, `_lookup_max_output_tokens`/`_hop_max_tokens`, `--effort`, `--max-tokens` None, dry-run reports budget + effort, MODELINV `effort`, `LOA_CHEVAL_LEGACY_WIRE`, per-family effort table, bounded dispatchers, `adversarial-review.sh` 160K company-aware input budget + pre-dispatch log + `--rawfile` prompts (the 160K budget exceeded `jq --arg`'s 128 KiB cap — found by test B9), BB `getTokenBudget` now serves the generated twin and `progressiveTruncate` clamps to it (the T1.3 codegen change had no runtime consumer before this), `live-floor-check.yml`.
- **1.5 / 1.6** — adaptive thinking per catalog flag, temperature-drop warning, persona/context split with one cache breakpoint (byte-identical text on every transport; headless prompt merges consecutive system sections), `Usage.cache_*` on non-streaming + headless, MODELINV + CLI JSON + ledger + pricing snapshot cache fields, cache read/write pricing, `operator_visible_warn` on thinking + `max_tokens`, `claude-headless` context 1M (bug-881-1b invariant), cache-eligibility table in `context-engineering.md`.
- **1.7 (subagent)** — verdict gate helper + cross-check, run-mode / sprint-completion docs, 14 bats cases, `test_golden_path.bats` harness fix.
- **1.8** — live scaffold + `tools/ceiling-probe.py` + pinned skip count.
- **1.9** — rollback proof: `grimoires/loa/a2a/sprint-235/rollback-proof.md` (worktree-per-unit reverts, drift gates + adapter suite + golden-path bats after each). Summary: U2 → U2+U1 → U2+U1+U0 → U2+U1+U0+U3 and FR-5 alone all revert cleanly (no conflict); after the full chain the adapter suite is back at the pre-cycle 1937 passed / 0 failed; "drift red" after reverting U1 only means the regen wrapper is itself part of U1; the single "1 failed" in the intermediate rows is the proof harness exporting the ledger env vars (reproduces on HEAD with the same exports), not a rollback defect; the FR-5 row's 47 red bats are `test_golden_path.bats`' pre-cycle state on main. Rollback of the ledger rotation is data: the two archive paths above.
- **1.10** — this report, `flatline-prd.md`, REPO-MAP regen. Framework fix found while running the AC gate: `validate-ac-verification.sh` walked every sprint's criteria in a multi-sprint plan (a Sprint 1 report had to quote Sprint 4's bullets); `--sprint-id sprint-N` now scopes it, with a bats case and the implementing-tasks MUST line updated (NOTES Decision Log 2026-09-17).

## Review round 1 — findings → fixes

Round-1 verdict: CHANGES_REQUIRED (0 critical / 5 high / 9 medium / 9 low; `engineer-feedback.md` 2026-09-17). Every finding was accepted. One commit per concern class (table in "Commits"); each fix landed with a test that is red on the pre-fix HEAD `19ffd850` (RED line above).

| # | Finding | Fix | Pin |
|---|---|---|---|
| H1 | persona-less `--system` payload unmarked (PRD FR-4 not held for Bridgebuilder) | `cheval.py:219` marks the whole payload when no persona; SDD §2.2 + SDD-R1-1; `context-engineering.md` BB row; live scaffold through the production path | `test_anthropic_cache_control.py:74`, `:84`; `test_cycle124_live_floor.py:149` |
| H2 | `max_tokens` stop flag never fired for Fable | `cheval.py:737` `_entry_thinking_class` (flag OR `temperature_supported: false` OR returned thinking) | `test_anthropic_cache_control.py:307`, `:332` |
| H3 | `--effort` dropped on the claude-headless hop | `claude_headless_adapter.py:255` `request.effort` first | `test_claude_headless_adapter.py:231` |
| H4 | audit cross-check fail-open shapes; review trailer never validated once an audit exists | `golden-path.sh:117` strict last-trailer parse (`invalid` denies), `:184` review gate on the audit path | `golden-path-c8-verdict-trailer.bats:319`, `:338`, `:354` |
| H5 | ledger resolver accepted directories / `/dev/null`; CWD-relative config path | `ledger.py:49` `S_ISREG` refusal + project-root anchoring (env stays CWD-relative) | `test_ledger_isolation.py:90`, `:116`, `:160` |
| M1 | every 404 chain-walked | `anthropic_adapter.py:61` `_is_model_not_found` (`model:` signature) | `test_anthropic_404_chain_walkable.py:103` |
| M2 | matrix + 4 suites stubbed a loader the Anthropic path no longer calls (CWD-dependent) | `_no_persona` fixtures stub `_load_persona_parts` | `test_input_size_consumers.py:113` (+ chain-walk, preflight, cli-cost, live-chain siblings) |
| M3 | isolation test asserted the mechanism | invariant: neither ledger env under the repo `.run/` | `test_ledger_isolation.py:219` |
| M4 | headless `Usage.cache_*` unasserted | test added | `test_claude_headless_adapter.py:358` |
| M5 | AC-4.2 mixed fixture missing | `tests/fixtures/metering/mixed-cost-ledger.jsonl` + three consumers | `cycle-124-cache-telemetry.bats:170`, `:184`, `:194` |
| M6 | readers resolved their default outside the merged config | `rollup.py:44`, `cost-report.sh:36` run the writer's resolver | `cheval-cost-rollup.bats:74` |
| DISS-001 (round-2 dissent, BLOCKING) | the reader swallowed the resolver's `ConfigError` and silently read `.run/cost-ledger.jsonl` | `default_ledger_path()` raises; rollup CLI exit 2; `cost-report.sh:93` exit 2 after argument parsing (explicit `--ledger` never consults the resolver; literal fallback only when the substrate is unavailable) | `test_ledger_isolation.py:66`, `cheval-cost-rollup.bats:101` |
| AUD-D1 (audit dissent, schema-rejected → recovered from the sidecar; HIGH) | `live-floor-check.yml` ran on `pull_request` with `ANTHROPIC_API_KEY` in a pytest step over PR-controlled code (`HAS_KEY` proves the secret exists, not that the checkout is trusted) | `workflow_dispatch` only + `environment: live-floor` (secret scoped to a reviewable environment); operator runbook line updated | `cycle-124-live-scaffold.bats` c124-1.8-5 |
| AUD-B1 (audit slice B, MEDIUM) | conftests set the ledger redirect only if unset — an operator's own export collected test rows and the tripwire never looked there | unconditional redirect in both conftests (`.claude/adapters/tests/conftest.py:25`) | `test_ledger_isolation.py:219` (tmp_path assertion restored) |
| AUD-B2 (audit slice B, MEDIUM) | readers swallowed every `load_config` exception and guessed `.run/` | `rollup.py:71` re-types as `ConfigError`; `cost-report.sh:102` exits 2 when the substrate is unavailable unless the env names the ledger | `test_ledger_isolation.py:196` |
| AUD-B3 (audit slice B, MEDIUM) | daily-spend sidecar opened symlink-following and truncated | `O_NOFOLLOW` in `ledger.py:284` and `budget.py:160` | `test_ledger_isolation.py:180` |
| AUD-B4 (audit slice B, LOW) | `cost-report.sh` `cd`'d into `.claude/adapters`, so a relative env path resolved differently for reader and writer | `PYTHONPATH` instead of `cd` (`cost-report.sh:36`) | `cheval-cost-rollup.bats:117` |
| AUD-A1 (audit slice A, MEDIUM) | non-streaming path: 16K default + thinking vs a flat 120 s read timeout ⇒ `ReadTimeout` retried ×4, each billed | `anthropic_adapter.py:72` `_nonstreaming_read_timeout` (lengthened only, 25 tok/s, cap 600 s) | `test_anthropic_nonstreaming_timeout.py` |
| AUD-A2 (audit slice A, MEDIUM, accepted with documentation) | `LOA_CHEVAL_LEGACY_WIRE` cannot turn thinking off on Opus 5 / Sonnet 5 / Fable (server default-on), so the 4096 default can truncate | documented in `multi-model-reference.md` and the env table (`cheval-delegate-architecture.md`); the `max_tokens` stop warning still fires; a catalog flag distinguishing default-on from explicit would be a new config surface — not added | — |
| AUD-A3 (audit slice A, LOW) | model-not-found 404 counted against the provider-wide circuit breaker | `types.py:205` `ModelNotFoundError`; `retry.py:414` skips `_record_failure` for it | `test_anthropic_404_chain_walkable.py:102` |
| AUD-A4 (audit slice A, LOW, known) | 36K legacy wall applied in the walk gate, not pre-flight (exit 12 vs 7) | follow-up bead (report §Known limitations 3) | — |
| AUD-D1 (audit slice D, MEDIUM) | BB adaptive retry derived from the operator budget, not the clamped one ⇒ identical retry payload | `truncation.ts:686` `effectiveInputBudget`; `reviewer.ts:477`, `:950` | `progressive-truncation.test.ts:308` |
| AUD-D2 (audit slice D, LOW) | `"default"` row counted as a known id | folded into `effectiveInputBudget` | same |
| AUD-D3 (audit slice D, LOW) | effort totality test could not fail | expected per-family table (`test_anthropic_effort_families.py:137`) | itself |
| AUD-D4 (audit slice D, LOW) | `val in (None, True, False)` accepted `1` | `is None or isinstance(bool)` (`test_anthropic_catalog_floor.py`) | itself |
| AUD-D5 (audit slice D, LOW) | ceiling test mirrored `default_max_tokens` locally | imports the real helper (`test_anthropic_catalog_floor.py:67` env fixture) | itself |
| AUD-D6 (audit slice D, LOW) | `pricing` untyped in the v3 schema | four pricing fields typed non-negative integer (`model-config-v3.schema.json:163`) | `model-config-v3-schema.bats:421` |
| R2-10 (round-2 lead concern) | `getTokenBudget`/`progressiveTruncate` resolved `constructor`/`__proto__` through inherited keys (NaN targets) | own-key lookup (`truncation.ts:674`) | `progressive-truncation.test.ts:307` |
| M7 | ceiling probe counted "no text block" as a size failure at `max_tokens: 16` | `tools/ceiling-probe.py:44`: OK = `message_stop`, 1024 tokens, `stop_reason` recorded | live-only tool; docstring + `test_ceiling_probe_writes_evidence` (live) |
| M8 | `FLATLINE_SCORE_MAX_TOKENS=4000` under adaptive thinking | 16 000 (`flatline-orchestrator.sh:128`) | `cycle-124-dispatch-budgets.bats:82` |
| M9 | regen bats ran `npm`/`tsx` unguarded | skips without the toolchain; `regen-model-artifacts.sh:55` exit 3 ≠ drift | `cycle-124-anthropic-catalog.bats:129`, `:170` |
| L1 | temperature-drop WARNING on essentially every call | once per model per process (`anthropic_adapter.py:178`) | `test_anthropic_thinking.py:174` |
| L2 | legacy-wire "byte-identical for every family" over-stated | claim scoped; per-family live-catalog test | `test_anthropic_thinking.py:132` |
| L3 | effort totality test could not fail | `_EFFORT_FULL_PREFIXES` + exactly-one-tuple test | `test_anthropic_effort_families.py:115` |
| L4 | AC-3.4 read the yaml, not the loader | loader test | `test_anthropic_catalog_floor.py:225` |
| L5 | report's catalog RED figure from a draft | restated (RED line above) | — |
| L6 | hygiene rule anchored on `/tmp/` | `cheval-e2e-` prefix (`check-ledger-hygiene.sh:158`) | `ledger-hygiene-tripwire.bats` (unchanged, green) |
| L7 | `progressiveTruncate` clamped unknown ids to the 100K default row | clamp only ids the tables know (`truncation.ts:900`); dist rebuilt | `progressive-truncation.test.ts:314` |
| L8 | regen script had no toolchain precheck | see M9 | `cycle-124-anthropic-catalog.bats:170` |
| L9 | explicit `--max-tokens` clamp on every provider | kept; disclosed in the PR body | — |

## Testing summary

| Suite | Command | Result |
|---|---|---|
| Adapter pytest | `cd .claude/adapters && python3 -m pytest tests -q -p no:cacheprovider` | 2203 passed, 6 skipped, 175 subtests (after the audit fixes; 2190 after round 1; 2174 before) |
| Cycle-124 bats + golden path | `npx --no-install bats tests/unit/cycle-124-*.bats tests/integration/input-size-consumers.bats tests/unit/golden-path-c8-verdict-trailer.bats tests/integration/ledger-hygiene-tripwire.bats tests/unit/ledger-isolation-discovery.bats tests/unit/cheval-cost-rollup.bats tests/integration/test_golden_path.bats` | 142/142 green after round 1 |
| Re-baselined suites | `cheval-input-gate`, `migrate-model-config`, `model-adapter-overlay-source`, `gen-bb-registry-codegen`, `cycle-114-opus-4-8-registry`, `gen-adapter-maps`, `model-config-v3-schema`, `test_golden_path` | all green |
| Bridgebuilder TS | `cd .claude/skills/bridgebuilder-review && npm test` | 739 / 740 — the one failure is `__tests__/persona.test.ts`, which fails on `main` too without `ANTHROPIC_API_KEY` (a subtest reaches the CLI's fatal credential check) |
| Drift gates | `bash tools/regen-model-artifacts.sh --check` | OK |
| Full `tests/unit/` + `.claude/scripts/tests/` sweep | `npx --no-install bats tests/unit/ .claude/scripts/tests/` | every failure is either pre-existing on `origin/main` (`aleph-release-ingestion.bats` 7, `post-merge-publication.bats` 19 — verified in an `origin/main` worktree) or was an in-flight artifact of running the sweep while the adapter unit was still being edited (`cheval-calling-primitive-attribution`, `cheval-tokens-postflight`, `cheval-cost-rollup`, `bug-881`, T3/T10, repo-map — all green on rerun after the final commits) |

RED-first evidence: every new pytest/bats file was run against the previous unit's commit in a scratch worktree before the implementation landed (catalog: the committed `test_anthropic_catalog_floor.py` is a collection error on 80be4b0f — it imports `_LEGACY_TRANSPORT_INPUT_WALL`, which did not exist; the '24/26 failed' figure in the first draft of this report came from a pre-import draft of the file, corrected in round 1; defaults: 2 collection errors + 16/18 bats red on f5ea53f4; adapter: 32/52 failed + 3 bats red on e14198a1); Task 1.7's subagent reported 11/14 red before its change, Task 1.2's 13/33. Round-2 fixes: `test_reader_default_fails_closed_like_the_writer` and the DISS-001 bats case fail on `632d0961` (the reader returned the literal fallback), the prototype-safe case fails on the `in`-based lookup. Round-1 fixes: the changed test files were run against the pre-fix HEAD `19ffd850` in a scratch worktree (`scratchpad/red-proof-r1.sh`): 16 pytest failures (persona-less + BB-shape breakpoint, three thinking-class stop cases, endpoint-404 ×2, headless effort argv, ledger anchoring ×3 + subprocess default + non-regular ×2, once-per-model warning, effort tuple totality), 6 bats red (three golden-path cases, regen exit 3, scorer B3, cost-rollup default), 1 BB suite red (unknown-id clamp); the remaining new cases (AC-4.2 fixture through the three consumers, per-family legacy-wire pin, advisor loader, headless `Usage` counts, isolation invariant) are evidence pins that were green on the old code by design — they close missing-evidence findings, not defects.

## Cost exposure of the new default (Task 1.4 "per-call cost-ceiling" table)

Worst case = `max_tokens` × output rate, from `test_max_tokens_defaults.py::test_worst_case_output_cost_per_call_is_bounded`:

| Entry | default max_tokens | worst-case at default | worst-case at the bounded 16K |
|---|---|---|---|
| claude-fable-5-1 / claude-fable-5 | 64 000 | $3.20 | $0.80 |
| claude-opus-5 / 4-8 / 4-7 / 4-6 | 64 000 | $1.60 | $0.40 |
| claude-sonnet-5 | 64 000 | $0.64 | $0.16 |
| claude-sonnet-4-6 | 64 000 | $0.96 | $0.24 |
| claude-sonnet-4-5-20250929 | 4 096 | $0.06 | $0.24 |
| claude-haiku-4-5-20251001 | 4 096 | $0.02 | $0.08 |

The 64K default is reached only by callers that pass no budget (ad-hoc `cheval --prompt`, future open-ended callers); every framework dispatcher passes an explicit bounded value.

## Timeout audit (SDD §3.2)

Observed output rates from the archived MODELINV log (CLI-reported latencies; medians, n in brackets): codex-headless 44 tok/s (171), gpt-5.5 43 (5), claude-headless p10 55 (122 — the median is not usable, the CLI reports sub-second latencies on cached calls), gemini-3.1-pro 4.2 (12), gpt-5.5-pro 13.8 (1). Worst-case generation time at 44 tok/s:

| Call | budget | time at 44 tok/s | timeout | verdict |
|---|---|---|---|---|
| Flatline review / skeptic | 16 000 | 364 s | `flatline_protocol.*.timeout_seconds: 600` (live config) | fits |
| Flatline review / skeptic | 16 000 | 364 s | `DEFAULT_MODEL_TIMEOUT=120` (orchestrator default when config unset) | does not fit — pre-existing: the same 120 s already could not cover a 4 096-token answer on gemini (975 s) |
| Flatline score | 16 000 (was 4 000; raised in round 1 — thinking shares the budget) | 364 s | 600 s / 120 s | fits under the live config; same 120 s caveat as review/skeptic under the orchestrator default |
| Adversarial dissent | 16 000 | 364 s | `CONF_TIMEOUT` (see `adversarial-review.sh` default) | tight; pre-existing (the 4 096 default already needed 93 s) |
| BB delegate | 16 000 (unchanged config default) | 364 s | `DEFAULT_TIMEOUT_MS = 120 000`; tier ladder 300 s above 100K input | does not fit at the floor — unchanged by this cycle (BB passed 16K before) |
| Open-ended cheval CLI (no budget) | 64 000 | 1 454 s | none by default | operator-visible: `--timeout` remains the caller's choice |

No timeout was changed this sprint (out of scope); the table is the input for a follow-up bead.

## Known limitations / operator items

1. **Live ACs (AC-1.3, AC-4.3)** need an Anthropic HTTP credential this host does not have. Run from the repo root:
   ```
   LOA_RUN_LIVE_TESTS=1 ANTHROPIC_API_KEY=sk-ant-... python3 -m pytest tests/replay/test_cycle124_live_floor.py -rs -q
   ```
   or store `ANTHROPIC_API_KEY` as an **environment secret** on the `live-floor` environment — configured FIRST with required reviewers and a deployment-branch rule (audit dissent, accepted residual: a dispatched run executes the candidate branch's test code with the key; the reviewer approving the run is the control) — and dispatch `live-floor-check.yml` manually on the candidate branch so it records the pass (the draft PR's merge precondition). The workflow is `workflow_dispatch`-only since the Sprint 1 audit: a `pull_request` trigger would have handed the key to a pytest run over PR-controlled code (audit dissent finding, recovered from the KF-004 sidecar — `tests/unit/cycle-124-live-scaffold.bats` c124-1.8-5 pins the trigger set and the environment). Red-result rule: a 400 on the thinking/cache shape is a defect; a 404 on `claude-opus-5`/`claude-fable-5-1` means the account does not serve the id yet (chain-walk covers runtime; record `absent` in the evidence file); `cache_read == 0` with a ≥ 4096-token prefix is a defect.
2. Every catalog value is `reference` (claude-api table cached 2026-06-24) until probed; Sonnet 5 pricing in particular ($2/$10 per the reference vs the cycle-120 entry's $3/$15) is flagged in the evidence file.
3. Two exit codes for one refusal class: the pre-flight gate exits 7 (`CONTEXT_TOO_LARGE`) while the legacy wall walks the chain and exits 12 (`CHAIN_EXHAUSTED`) — observed by the AC-3.5 matrix, not changed.
4. `MODE_TO_AGENT["dissent"]` still names a non-existent skill directory (PRD Q6) — untouched this sprint.
5. Pre-existing failures on `origin/main` left alone: `aleph-release-ingestion.bats` (7), `post-merge-publication.bats` (19), BB `persona.test.ts` without a key.

## Verification steps for the reviewer

1. `bash tools/regen-model-artifacts.sh --check` — four OK lines.
2. `cd .claude/adapters && python3 -m pytest tests -q -p no:cacheprovider` — 0 failed.
3. `npx --no-install bats tests/unit/cycle-124-*.bats tests/integration/input-size-consumers.bats tests/unit/golden-path-c8-verdict-trailer.bats tests/integration/ledger-hygiene-tripwire.bats tests/unit/ledger-isolation-discovery.bats tests/unit/cheval-cost-rollup.bats tests/integration/test_golden_path.bats` — 142 ok.
4. `env -u ANTHROPIC_API_KEY python3 .claude/adapters/cheval.py --agent reviewing-code --model opus --effort xhigh --prompt x --dry-run` — `claude-opus-5`, `max_tokens: 64000`, `effort: xhigh`.
5. `LOA_CHEVAL_LEGACY_WIRE=1` with the same command — `max_tokens: 4096`; `tests/unit/cycle-124-cache-telemetry.bats` case 4 shows the pre-cycle request body.
6. `bash tools/check-ledger-hygiene.sh` after the suites — exit 0.
