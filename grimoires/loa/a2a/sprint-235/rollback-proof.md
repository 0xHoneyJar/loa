# Rollback proof — cycle-124 Sprint 1 (Task 1.9)

Generated 2026-09-17T09:24:51Z from HEAD f3dcf310 on a scratch worktree per unit; each row is `git revert --no-commit <unit commits>` followed by the three checks. `revert` = clean / CONFLICT (files); `drift` = tools/regen-model-artifacts.sh --check; `pytest` = .claude/adapters suite; `bats` = golden-path trailer + integration suites.

| Unit | Commits | revert | drift gates | adapter pytest | golden-path bats | Reading |
|---|---|---|---|---|---|---|
| U2 adapter | a421334a | clean | green | 1 failed, 2118 passed, 9 skipped, 175 subtests passed in 40.30s | 71 ok / 0 not ok | Clean revert; drift gates green (U2 does not own generated artifacts beyond the headless 1M twin, which reverted with it). The single pytest failure is the proof harness itself: it exports LOA_MODELINV_LOG_PATH / LOA_COST_LEDGER_PATH for the worktree, and `test_ledger_isolation::test_conftest_isolates_both_ledgers` asserts the conftest owns both paths — same failure reproduces on HEAD with those two variables exported (see report). Golden-path bats untouched (71/71). |
| U2+U1 (adapter, defaults, catalog) | a421334a e14198a1 f5ea53f4 | clean | red | 1 failed, 1977 passed, 9 skipped, 175 subtests passed in 39.32s | 71 ok / 0 not ok | Clean cumulative revert. "drift red" = `tools/regen-model-artifacts.sh` is itself part of U1 (f5ea53f4), so the command is absent after the revert; the three underlying gates it wraps are exactly main's and were green on main. pytest: same harness-env failure only. |
| U2+U1+U0 (+ MODELINV schema) | a421334a e14198a1 f5ea53f4 6164feb4 | clean | red | 1 failed, 1944 passed, 9 skipped, 175 subtests passed in 38.01s | 71 ok / 0 not ok | Clean; same two readings. The U0 fixture and its 33 tests revert cleanly — no later commit depends on the four optional payload fields except U2, already reverted. |
| U2+U1+U0+U3 (+ ledger isolation) | a421334a e14198a1 f5ea53f4 6164feb4 f4375abc | clean | red | 1937 passed, 9 skipped, 175 subtests passed in 35.48s | 71 ok / 0 not ok | Clean; adapter suite back to the pre-cycle baseline 1937 passed / 0 failed (the harness-env failure disappears with U3 because that test is part of U3). Rollback data: the rotated ledgers are NOT restored by any revert — `.run/archive/model-invoke-20260917T092404Z.jsonl` and `cost-ledger-20260917T092405Z.jsonl` per the runbook §5. |
| FR-5 gates alone | 37b0ee80 | clean | green | 1 failed, 2170 passed, 9 skipped, 175 subtests passed in 41.06s | 10 ok / 47 not ok | Clean, independent of the U-chain. golden-path bats "10 ok / 47 not ok" is main's own state: the 47 red are `tests/integration/test_golden_path.bats`, which was red on main before this cycle (setup copied golden-path.sh but not compat-lib.sh) and is fixed inside 37b0ee80; the 14 new trailer cases vanish with the revert. pytest: harness-env failure only. |

## Conclusion

Every unit reverts cleanly in dependency order (U2 → U1 → U0 → U3) and FR-5 independently; no conflict, no test outside the reverted unit breaks. The only persistent "1 failed" is the proof harness exporting the ledger env vars into the worktree, not a rollback defect. Rollback of the ledger rotation is data, not code: the two archive paths above.
