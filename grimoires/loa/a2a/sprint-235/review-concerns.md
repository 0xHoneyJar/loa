# Reviewer concern notes — Sprint 1 (cycle-124), Phase 2 input to the cross-model dissenter

Scope: commits 80be4b0f..HEAD on feature/cycle-124-model-generation-floor (catalog, ledger isolation, MODELINV schema, golden-path verdict gate, cheval defaults/effort, Anthropic adapter thinking + cache, live scaffold). The implementer's report with AC evidence is grimoires/loa/a2a/sprint-235/reviewer.md.

Concerns the reviewer wants an independent opinion on (each with the anchor to check):

1. `_hop_max_tokens` (.claude/adapters/cheval.py:704) clamps an EXPLICIT `--max-tokens` to the catalog `max_output_tokens` for every provider, not only Anthropic. FR-2 scoped the *default* to Anthropic; the clamp of explicit values silently lowers an OpenAI/Google caller's request (e.g. 64000 → 32000 on gpt-5.5) with only a logger warning. Is that a regression for any dispatcher, or the right guard against a 400?
2. Persona/context split (cheval.py `_persona_messages`, anthropic_adapter.py `_transform_messages` ~:530-560): the second system message starts with `---\n\n…`; the Anthropic block path folds the `\n\n` joiner into block 2. Confirm no path emits a `cache_control` marker on more than one block, and that a chain whose primary is Anthropic HTTP but whose surviving hop is `claude-headless` renders the identical prompt (headless_cli.py `_build_prompt` merge of consecutive system sections).
3. `LOA_CHEVAL_LEGACY_WIRE` is consulted in three places (base.default_max_tokens, anthropic_adapter thinking emission, `_transform_messages` marker handling, cheval assembly). Is the pre-cycle body truly byte-identical under the switch, including `max_tokens` and the absence of `thinking`/`output_config`? Any place that forgot the switch?
4. HTTP 404 → `ProviderUnavailableError` (anthropic_adapter.py ~:200-225 and ~:340-350): a 404 from a wrong endpoint URL would now walk the chain and end as CHAIN_EXHAUSTED instead of a terminal InvalidInputError. Acceptable trade for model-not-found, or does it mask misconfiguration?
5. Legacy transport wall (cheval.py:592 `_LEGACY_TRANSPORT_INPUT_WALL`) applies to `provider == "anthropic"` regardless of `kind` — a `claude-headless` hop has no `effective_input_ceiling` so it is unaffected today; if a ceiling were ever added to it, the 36K HTTP wall would wrongly bind a CLI hop.
6. Gate-order observation from the AC-3.5 matrix: the pre-flight gate refuses with exit 7 (CONTEXT_TOO_LARGE) while the legacy wall walks the chain and exits 12 (CHAIN_EXHAUSTED) for the same class of refusal. Does any caller key on exit 7?
7. `adversarial-review.sh` `_adv_input_budget_for_model` runs `bash -c 'source "$1"; id="${MODEL_IDS[$2]:-$2}"; …' _ "$maps" "$model"` — operator-controlled model string used as an associative-array subscript. Any injection or word-splitting path?
8. `golden-path.sh` `_gp_verdict_gate` / `_gp_trailer_int` (:94-135): can a crafted trailer (two trailers, trailer not last, `excluded` present without counts) make the review or audit gate return 0 while `verdict-derive.sh` would not approve?
9. `resolve_cost_ledger_path` (metering/ledger.py:47-100) + O_NOFOLLOW append: TOCTOU between validation and open, /dev/stdout or FIFO targets, a path inside a tracked directory.
10. `live-floor-check.yml`: secret only under HAS_KEY; `set +x`; artifact upload of the pytest log — could a failing assertion print the request headers? (The tests print response payloads on failure, not headers.)
11. Test quality: the in-process matrix (test_input_size_consumers.py) patches `get_adapter`, `resolve_execution`, `_build_provider_config`; confirm the pre-flight gate and legacy wall are still exercised for real (they are before dispatch), and that the re-baselined pins (cheval-input-gate.bats G1/G7/G8, model-adapter-overlay-source.bats C1/D1/F1, gen-bb-registry-codegen.bats T3/T10, BB truncation tests) track real behavior changes rather than weaken invariants.
12. `claude-headless` `context_window` raised to 1M for the bug-881 invariant: BB's generated budget for the headless voice becomes 1M (no ceiling on a CLI entry) — bounded in practice by BB's `maxInputTokens` config default 128K; is that enough?
