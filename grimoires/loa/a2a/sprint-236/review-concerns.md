# Reviewer concern notes — Sprint 2 (cycle-124, structured outputs), Phase 2 input to the cross-model dissenter

Scope: commits 087fc39e..3b4c03b1 (wire schemas, --json-schema, adapter emission, dissent enforced branch, Flatline plumbing, OpenAI pass-through, ledger-lib guard, isolation fix). Concerns the reviewer wants an independent opinion on (anchor to check in brackets):

1. Telemetry truth on a chain walk: cheval records `schema_enforced` from the FINAL hop's result metadata (cheval.py `_modelinv_state["schema_enforced"]`). If the primary (structured_json) hop fails and a non-capable fallback answers, is the envelope `false` (correct) — and is `output_schema_sha256` still emitted (yes, by design: "a schema was requested")? Any path where `schema_enforced` is missing when a schema was requested?
2. claude_headless_adapter `_run_subprocess` retry: a CLI schema rejection re-runs once WITHOUT `--json-schema`. Can the first (rejected) attempt have consumed model tokens? Is the rejection signature (`not a valid JSON Schema`) specific enough that a model answer containing that phrase in `stdout` of a FAILED run cannot trigger a retry? (returncode != 0 is required.)
3. `_read_output_schema` (cheval.py): `os.path.getsize` then `open` — TOCTOU on a FIFO or a growing file; the 64 KB cap is on file size, not on the canonical JSON size; error messages embed the path only, never content — confirm.
4. OpenAI strict json_schema: the wire subset uses `anyOf: [{type: string}, {type: null}]` for nullable fields, `enum` on strings, `additionalProperties: false`, all properties required. OpenAI strict mode additionally requires… (does it reject `anyOf` with `null`? it accepts `type: ["string","null"]` — verify against your knowledge of the /v1/responses strict rules; the repo avoided type arrays because Anthropic's subset documentation preferred anyOf).
5. adversarial-review.sh enforced branch: `schema_enforced` is read with `if .schema_enforced == true` (boolean only) — a string "true" from a foreign envelope falls to the unenforced path (safe direction). `stop_reason` absent ⇒ strict parse proceeds. Any envelope shape that reaches the enforced branch with `content` valid JSON but not an object (e.g. `[]`) — `.findings // empty` then routes to malformed_response; confirm no `clean` alias.
6. Repair loop now unconditional on unenforced voices: per rejected finding ONE extra model call (`_repair_finding_via_model`). With `codex-headless` as the only live dissenter on this host (schema never forwarded), every rejected finding costs a round-trip. Is the depth bounded (yes: one per finding) and is a repair failure still fail-safe (rejected + sidecar)?
7. flatline-orchestrator.sh: the 7th positional is appended after the D3 `if/else`; `WIRE_*` constants derive from `SCRIPT_DIR` — when the orchestrator is SOURCED by a bats file, `SCRIPT_DIR` is the caller's; the constants are evaluated at source time. Any production entry point that sources rather than execs it?
8. ledger-lib.sh: `(.cycles[].sprints[]? | select(type == "object") | select(.global_id == $id)) |= …` — with jq 1.6/1.7 an update through `select` in a path is valid; confirm the `exists` check and the update agree (both guarded) so an id present only inside a string-sprint cycle cannot pass `exists` and then no-op the update.
9. `tool_choice` `required` now raises in the Anthropic adapter: grep the BB TypeScript (`cheval-delegate.ts`) and cheval callers for `tool_choice` — none pass it today; confirm no config surface sets it.
10. The wire schemas dropped `$schema`; W3 also forbids `format` although the SDD allowed string `format` — none of the five schemas use it; is forbidding it wrong for either provider?
11. Isolation fix: `adversarial-review-e2e.bats` and `adversarial-review.bats` now export the two ledger paths under `BATS_TEST_TMPDIR`; `BATS_TEST_TMPDIR` is per-test — is the export evaluated once per test (setup) so each test gets its own dir? Any other suite that runs `flatline-orchestrator.sh` for real without the exports (the discovery test should list them — DS-1 is green)?
12. Test quality: `flatline-call-model-schema.bats` evals the `call_model` body with every collaborator stubbed — does `awk "/^call_model\(\)/,/^}/"` still capture the whole function (the function contains nested `}` lines at column 0?), i.e. could the harness be testing a truncated body?
