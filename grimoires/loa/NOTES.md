# Loa Project Notes

## Session Continuity — 2026-09-24 (2.0.0-rc.2 PUBLISHED — cycle-125 merged; maintainer said "proceed as you suggest")

- **State**: PR #1269 merged to `main` with a merge commit `5f4a58a5` (`--admin`, sole maintainer; feature branch deleted; local `main` = `origin/main`). Post-merge run 35941548443 prepared `v2.0.0-rc.2` (`prerelease: true`, rc increment from the `v2.0.0-rc.1` tag, changelog phase skipped because the `[2.0.0-rc.2]` heading was already on the branch, one generated commit `chore(ledger): archive cycle-125-friction-floor after merge` → target `764d80f8`). Published from an isolated clone with `--publish … --approve-sha256 a28d1cd8…` → tag `v2.0.0-rc.2` (object `81a3b7bf` → `764d80f8`), release id 395209640 `prerelease: true`, PR comment 5805643509, state `DONE`; rich notes installed from `grimoires/loa/reports/release-notes-2.0.0-rc.2.md`; "latest" release stays `v1.200.0`. Record on PR #1269 (release comment) and in `reports/release-2.0.0-rc.2-operator-steps.md` (plan of record). Run state JACKED_OUT; zone marker deleted; `record/cycle-125-a2a` refreshed after this block.
- **Deliverables commit `2812af93`** (on the branch before the merge): CHANGELOG `[Unreleased]` → `## [2.0.0-rc.2] — 2026-09-24 — Friction floor (release candidate)` with a fresh empty `[Unreleased]` above, corpus counts corrected (49/60/5), Bridgebuilder fixes under `### Fixed`; `update-loa-bump-version.sh --target 2.0.0-rc.2`; `sync-readme-version.sh --apply` (it does NOT move the install pin — hand-edit `--tag vX`); README what's-new restructured (rc.2 paragraph, rc.1 bullets kept), Release badge renamed; `semver-bump.sh --from-tag` → `2.0.0-rc.2` with no heading warning; `sync-readme-version.bats` + `semver-bump.bats` 62/62; REPO-MAP unchanged (the CLAUDE.loa.md header is not code).
- **Operator next**: valid `ANTHROPIC_API_KEY` → `live-floor` environment secret → `gh workflow run live-floor-check.yml --ref main`; soak with `2.0.0-rc` issues; promotion PR with a `## [2.0.0]` heading when the exit criteria hold. bd-b4ji recurs (the ledger-archive commit is only on the tag; `main`'s ledger still says cycle-125 active — fold the archive row into the next PR). Follow-ups bd-ypbg (re-pricing pass) and bd-n7v3 (check-permissions scope) as `/bug` cycles.
- **Gotchas**: after `repo-map-gen.sh` stage REPO-MAP.md + its `.checksum` sidecar + `.claude/checksums.json` together (two red CI rounds this close-out); `gh release view` has no `isLatest` field (use `gh release list` / `releases/latest`); the publisher's `gh: Not Found (HTTP 404)` line is its existence probe before the release POST, not an error; `gh pr merge --delete-branch` switches the local checkout to `main` (gitignored a2a/NOTES survive because `main` never tracked them).

## Decision Log — 2026-09-23 (cycle-125 "Friction floor" close-out — draft PR #1269, run JACKED_OUT)

- **State (cycle CLOSED, run JACKED_OUT)**: all four sprints (global 241–244) COMPLETED with review + audit approvals and cross-model dissents; ledger cycle `cycle-125-friction-floor` all `completed`; draft PR **#1269** (`feat(cycle-125): friction floor …`, base `main`) at `85347811`; Bridgebuilder pass (codex-headless) triaged in the PR comment: FIND-001/002 refuted (notes-guard warns on stderr; LSA-2 / PF-5 cover), FIND-003/004 fixed in `20082397` (checkpoint discarded when `br` is unavailable; pre-metadata priced rows are `unclassified_rows`, not unpriced), FIND-005/006 praise. `85347811` made `budget.py` stub-safe (the one red CI job), and `0f2b582f` regenerated REPO-MAP + checksums for its two new helper symbols (repo-map-gen.bats drift was the follow-on red) and `a3ddc311` committed the forgotten `REPO-MAP.md.checksum` sidecar (blocking CI check) — PR tip `a3ddc311`. Lesson: after `repo-map-gen.sh`, stage REPO-MAP.md, its `.checksum` sidecar AND `.claude/checksums.json` together. `.run/state.json` + `.run/sprint-plan-state.json` = JACKED_OUT with the PR URL; `.run/zone-guard-authorization.json` deleted after the last `.claude/` edit; a2a record refreshed on `record/cycle-125-a2a` (built from a temporary index, no checkout — never merge). GitHub refuses a self-review request (PR author = `deep-name`), so no reviewer is attached.
- **Operator next**: review/merge #1269 (lands as `2.0.0-rc.2` through the untagged-rc CHANGELOG rule; no tag/publish here); follow-ups bd-n7v3 (check-permissions settings scope) and bd-ypbg (explicit re-pricing pass — this repo's enforcer halts at 74 % historical unpriced rows until then); carry-over from rc.1 unchanged (live-floor key/dispatch, soak, `## [2.0.0]` promotion). Local-only: `tests/test_streaming_transport.py` (22), `test_modelinv_optional_fields.py` (6), `test_ledger_isolation.py` (1) fail identically on `origin/main` in a clean worktree on this host — environment, not the PR (CI only ever showed the 3 budget cases).
- **Gotchas for the next session**: Bridgebuilder constructs an Anthropic adapter unless `BRIDGEBUILDER_MODEL` is a `*-headless` alias — with no `ANTHROPIC_API_KEY` on this host run it as `BRIDGEBUILDER_MODEL=codex-headless`; refresh a record branch with a temporary index (`GIT_INDEX_FILE=… git read-tree HEAD && git add -f <paths> && git write-tree | commit-tree | update-ref`) — `git checkout record/…` and back DELETES the gitignored files from the working tree (restore with `git archive record/… <paths> | tar -x`); full `tests/unit/` run needs `GIT_CONFIG_GLOBAL=/dev/null` PLUS a git identity in env; regenerate the licence fixtures (8 cases) before the next full run (`tests/fixtures/ensure_license_fixtures.sh`); `.claude/cache/index.json` (gitignored) is in `checksums.json` and drifts whenever an index runs — regenerate checksums last; a relative `$PY` breaks once you `cd` into a worktree — use the absolute venv path.

## Decision Log — 2026-09-23 (cycle-125 Sprint 4 — provider health, cost accounting, docs, E2E; global 244, unattended `/run sprint-plan`)

- **Implemented** in `49dd7b2a` (FR-4/FR-5 + docs), `56e4900e` (fence: rebinding builtins count in command position only), `6c9d77e2` (enforcer schemas + hermetic seam), `a954bcc1` (legacy content-key de-dup), `13009350` (dotenv presence parity, `reset_bucket` never fabricates), `aeac6100` (hook header residual). Review r1 APPROVED (dissent 1 ADVISORY → fixed); audit dissent clean. a2a record `grimoires/loa/a2a/sprint-244/`.
- **Decisions**: warning-free breaker CLI is `python3 -m loa_cheval.routing.breaker_cli` (the package imports `circuit_breaker` eagerly, so `-m …circuit_breaker` prints runpy's RuntimeWarning); the chain-walk proof is the MODELINV trace from a mocked-adapter `cmd_invoke` with a REAL breaker file (`--dry-run` never reaches the chain; `retry.py` skips an OPEN bucket and the exhausted entry walks as `FALLBACK_EXHAUSTED`); the pricing ladder never re-prices history — this repo's ledger is 74 % unpriced from pre-ladder rows, so `budget_verdict` now halts (`halt-uncertainty:unpriced_share`) until bd-ypbg designs an explicit opt-in re-pricing pass; the enforcer's share is all-time, not windowed; credential presence in `/loa` follows the preflight rule (env → `.env.local` → `.env`) with the source named.
- **Caught in-sprint**: the enforcer's new `unpriced_share` payload field and reason failed the audit payload SCHEMAS (`additionalProperties: false`, enum) → both schemas extended; under bats the enforcer read this repo's LIVE ledger and flipped every `allow` → the seam is hermetic under the bats marker; two migrations in one second overwrote the receipt → `-N` suffix; legacy rows without `request_id` re-migrated every run → content key; `reset_bucket google:http_api` on a bucket that never existed fabricated healthy state (dissent DISS-001) → existence check; the framework's own `notes-guard.sh read` inside a command voided the mktemp proof (`read` matched anywhere) → command-position rule.
- **Full `tests/unit/` run**: needs `GIT_CONFIG_GLOBAL=/dev/null` (KF-034) PLUS `GIT_AUTHOR_*`/`GIT_COMMITTER_*` — the isolated config drops the identity and 113 bridge tests go red with "Please tell me who you are". Ledger hashes recorded before/after (KF-033) — result in `a2a/sprint-244/reviewer.md` §Close-out.
- **Gotchas**: `grep -E "^(not ok|1\.\.)|^#" | head -40` hid 15 real failures behind one test's JSON dump — grep `^not ok` alone first, then look at output; `bats` `run` merges stderr into `$output` (parse JSON with `grep -v '^prefix:'` or `tail -n1`); `yq eval "{model, fallback_chain}"` is jq syntax (KF-027) — read keys one at a time; zsh globs abort a whole command on no match (quote `--include=*.py`).

## Decision Log — 2026-09-23 (cycle-125 Sprint 3 — run preflight and resume, global 243, unattended `/run sprint-plan`)

- **Closed** with review r1 + audit r1 APPROVED (both dissents clean); commit `d6ff411a`. a2a record `grimoires/loa/a2a/sprint-243/`. `run-preflight.sh` P1–P8 (`--unattended`, `--resume`, `--json`), `run-checkpoint.sh` (schema v2, flock + tmp/mv, beads-validated read), `loa-run-state-surface.sh` SessionStart line behind hook-guard in both settings files, `/loa` `Run:` line, `workflow-state.sh` suggests `/run-resume` first; run-mode pre-flight collapsed (15,761 → 15,372 B); re-entry proven with the real `br` (`tests/integration/implement-reentry.bats`). Follow-up bead bd-n7v3: `check-permissions.sh` should merge settings.local/home and honour `deny` (P1/P2 scope).
- **Decisions**: P4 reads breaker files with jq (same schema as `list_buckets`) rather than starting Python on every `/run`; the per-task checkpoint write point lives in `implementing-tasks` (the process that closes beads), phase/sprint points in run-mode resources; a fresh `RUNNING` is refused as "already in progress" and is NOT surfaced at session start (the title hook covers a live run); `--resume` inverts P6.
- **Caught by the live smoke test, not by the suite**: `beads-health.sh` exits 4 for DEGRADED by design, and `cmd | jq || echo UNKNOWN` under `pipefail` yielded `DEGRADED\nUNKNOWN` → FAIL. Capture-then-parse (memory TRAP confirmed again); the stub now mirrors the real exit codes. Also: `/usr/bin/yq` on this host is the PYTHON jq-wrapper (`yq 3.4.3`) while `~/.local/bin/yq` is Mike Farah v4 — a minimal test PATH picked the wrong one and `yq eval` silently returned nothing, making every flatline stage look disabled → P3 passed vacuously. The script now detects the flavour; tests symlink the tools they need into `$T/bin` instead of adding whole directories (adding `~/.local/bin` also brought `claude` along and made every voice "usable").
- **Gotchas**: a `VAR=x fn` prefix on a bash FUNCTION call is not exported to the function's child processes — `export` before `run`; `br show <id> --json` returns an ARRAY; `br list --json` returns `{issues:[…]}`; `br init` works in any temp git dir (prefix from the dir name).

## Decision Log — 2026-09-23 (cycle-125 Sprint 2 — sectioned artefacts, global 242, unattended `/run sprint-plan`)

- **Closed** with review r3 + audit r2 APPROVED; commits `161ccd9a` (impl), `5791f92f` (review r1 fixes), `70cef5d8` (audit r1 fix). a2a record `grimoires/loa/a2a/sprint-242/`. `notes-guard.sh read --file F --section <Sprint N | N. | substring> | --index`; non-NOTES default = index; `emit_capped` shared with the NOTES default; `/implement` reads its sprint block by section; `/loa` `Artefacts:` line; `update-loa.sh rotate_oversized_notes` (submodule AND vendored paths); migration rc.2 addendum.
- **Decision — include budget**: the SDD's "≤ 100 B include line" was implemented as a net +6 B include (body tightened while adding the routing sentence) because eight of the ten carrying skills sit within 100 B of the 16,384 B cap; a literal +92 B would have broken six budgets. The three planned trims were still applied for headroom (implementing-tasks 16,342, reviewing-code 16,252, auditing-security 16,239).
- **Review r1 caught**: vendored mode `exec`ed `update.sh` inside `main()`'s `case`, so the new post-refresh step was unreachable for half the install shapes — audit any `main()` path that `exec`s whenever a post-step is added; `Sprint 2` matched `## Sprint 2.5` (boundary now `([^0-9.]|$)`); a TAB in a heading shifted awk's tab-separated fields (free text goes LAST; rebuild after the fifth TAB); `awk -v` processes backslashes (pass free text via `ENVIRON`). Audit r1 caught the test seam I added for the vendored test being honoured in production — test-mode overrides are bats-gated in this repo (`BATS_TEST_FILENAME`), no exceptions.
- **Gotchas**: `br close -r` text beginning with `--` is parsed as a flag (clap) — start reasons with a word; a bats `run` with an env prefix on `source` does not carry the variable into the following function call — `export` first; `make-large-notes.sh … under` yields ~1 KB (985 B), not 1.3 KB; `loa-status.sh` takes ~20 s per invocation — keep status tests few.

## Decision Log — 2026-09-23 (cycle-125 Sprint 1 — fence precision, global 241, unattended `/run sprint-plan`)

- **Closed** with review r3 + audit r2 APPROVED; commits `223012f0` (implementation), `87b7522c` (review r1 fixes + dissent DISS-001), `52ff4830` (audit r1 fixes). a2a record in `grimoires/loa/a2a/sprint-241/` (gitignored; goes to `record/cycle-125-a2a` at cycle end). Corpus `tests/fixtures/fence-corpus/corpus.jsonl`: 47 benign (14/46 → 47/47), 55 dangerous (55/55), 5 residual; suites 304/304 (`block-destructive-bash` 279, `hook-wiring` 10, `git-branch-prune` 15).
- **The review loop earned its keep**: the corpus-gated implementation passed its own gates, and the review then found six HIGH predicate holes by hand-built twins (newline `"\n"`-as-letter-n in a double-quoted regex; `+=`/`readonly`/`NAME[`/`getopts` rebinding; `$TMPDIR` mutation; per-segment cwd via `%%` first-occurrence instead of the loop cursor; `pushd`/`eval`/inline shell; `head -1` in FR-1.1/FR-1.3). The audit dissent then returned 0 schema-valid findings but 2 rejected payloads — one was a TRUE bypass (`if T=/; then rm -rf "$T"`), so **rejected dissent payloads must be triaged by hand, never dropped** (KF-004 class). Fix shape: count every `NAME=` token anywhere, not only statement-initial ones.
- **Design rule that held**: relax only on a positively established predicate; the hook's own `$PWD` is never evidence (a project under `/tmp` keeps `rm -rf src` blocked — the harness itself runs the hook under `mktemp -d`, which is how the corpus caught it); no network in the hook (the `gh` probe lives in `git-branch-prune.sh`, bounded by `timeout 5`, opt-out `LOA_FENCE_NO_NETWORK=1`, and requires `headRefOid == local head`).
- **Accepted FP classes recorded as residual rows** R01–R03 (remote payloads), R04 (whole-command rebind denylist: `source`/`read`/`declare`/`unset` anywhere), R05 (a second `NAME=` token even as `echo` text). `rm -rf /tmp/x/*` (prefixed glob under a temp root) stays allowed as before; a bare `/tmp/*` is now FR-2-BLOCK.
- **Gotchas**: the fence blocks the LEAD's own probe commands whose TEXT contains `rm -rf`/`git branch -D`/`DROP TABLE` — write probes to a script file and run the file; `br close -r "…rm -rf…"` is not a scrubbed carrier (only `br create/update -d`) → rephrase; a stale local `grimoires/loa/a2a/sprint-1/` from cycle-122 existed (gitignored) and its `COMPLETED` + `auditor-sprint-feedback.md` would have satisfied the gates — cycle-124/125 use GLOBAL ids (`a2a/sprint-241`), the stale files were deleted; bats lives at `~/.config/nvm/versions/node/v24.11.1/lib/node_modules/bats/bin/bats` (not on the background shell's PATH); `git show REF:path > file` silently produced nothing once (re-run; check `wc -c` before trusting a probe against it); relative hook paths break when a probe script `cd`s — pass absolute paths.

## Decision Log — 2026-09-23 (cycle-125 "Friction floor" — planning, unattended after "proceed as you suggest")

- **Evidence base**: `grimoires/loa/reports/usage-mining-2026-09-23.md` (tracked, `62dd75d3`) + raw aggregates in `.run/usage-mining/` (untracked). F5 was CORRECTED during planning: the ledger field is `cost_micro_usd`, not `cost_usd`; legacy ledgers were priced (≈$59 across 16 mounts) and the real defect is that current-path rows are 79 % `pricing_source: unknown` (dated OpenAI ids, `gemini-2.5-pro`, hop names) — FR-5 rewritten accordingly.
- **Cycle bookkeeping**: cycle-124 archived on main (`7073b9a9`, archive dir `grimoires/loa/archive/2026-09-23-cycle-124-model-generation-floor`, `archive-cycle.sh --retention 0` then `ledger-lib archive_cycle`; the script alone does not touch the ledger and default retention 5 would prune older archives); cycle-125 created via `create_cycle` (lib numbered it `cycle-086`, renamed to `cycle-125-friction-floor`); `next_sprint_number` bumped to 241 before `add_sprint` because the bug skill had left it equal to the already-used 240 → sprints 241–244 = sprint-1..4.
- **[ASSUMPTION] cached `/ride` reality (2026-05-04) used; no re-run** — the PRD is grounded in the usage report and direct code citations. Interview phases answered from the brief (`grimoires/loa/context/cycle-125-brief.md`), no operator questions (autonomous).
- **Flatline PRD review**: 2 voices (codex-headless, claude-headless), scoring DEGRADED (cross-scoring rejected), 0 consensus items, 9 skeptic blockers — integrated: no remote-payload scrub (SKP-001), no bare project-dir allowance / `rm -rf src` stays blocked (SKP-002), real `$TMPDIR` + single mktemp assignment (SKP-004), no network in the hook — `gh` probe lives in `git-branch-prune.sh` (SKP-005), corpus lint for reproducible sanitisation (SKP-004b), atomic + versioned checkpoint with beads as recovery truth (SKP-010), pricing-snapshot governance recorded as SDD open question (IMP-001). Output file: `.run/`-local scratch (`flatline-prd.json`); the orchestrator did not write `grimoires/loa/a2a/flatline/prd-review.json`.
- **Scope**: FR-1..FR-5 only; F6 surface diet, F7 fleet tooling, F8 abandoned-cycle sweep deferred. Merge lands as `2.0.0-rc.2` (untagged rc heading rule) — no CHANGELOG heading authored; entries go under `[Unreleased]`.

## Decision Log — 2026-09-23 (release 2.0.0-rc.1 prep on PR #1266 — unattended, prompt `a2a/prompts/release-2.0.0-rc1-2026-09-23.md`)

- **Phase A answer**: the pipeline had no formal pre-release path — `semver-bump.sh` computed `2.0.0` (release → prerelease entry did not exist), `phase_release` never set `prerelease`, `sync-readme-version.sh` rejected an rc `framework_version` (would have failed `README version sync` on the PR). Built as `/bug` **sprint-bug-240** (`a2a/bug-20260923-27d899/`, commit `2018c320`, bead bd-t1fm closed): the topmost **untagged** CHANGELOG heading is the operator signal (enter `2.0.0 → 2.0.0-rc.1`, promote `2.0.0-rc.N → 2.0.0`; anything else ignored, a mismatched rc heading warns; no new flag/config key); candidate carries `prerelease`, publisher ties it to the tag, release POST + read-back verify the flag; README sync takes SemVer §9 prereleases with the shields `--` badge. Review APPROVED (dissent gpt-5.5-pro: 1 advisory → §9 grammar tightened in-round), audit APPROVED (dissent clean), 13 tests first, 220 green across the release-pipeline suites.
- **Phase C landed** (`53ea2168`): CHANGELOG `[2.0.0-rc.1]` (Breaking first, Unreleased empty), `docs/migration/v2.0-model-generation-floor.md`, `docs/architecture/ADR-004-model-generation-floor.md`, README what's-new + badges/pin at the rc, markers at `2.0.0-rc.1` (`update-loa-bump-version.sh --target`), `grimoires/loa/reports/{release-notes-2.0.0-rc.1,breaking-surface-audit-2.0.0-rc.1,release-2.0.0-rc.1-operator-steps}.md`. Evidence after the splice: `semver-bump.sh --from-tag` → `next 2.0.0-rc.1`, `prerelease_transition.kind enter`; `classify-pr-type.sh --title` → `cycle`.
- **Decisions taken**: README pin example moved to `v2.0.0-rc.1` (resolves once published; the block says stable users stay on `v1.202.1`); the "Release" badge points at the rc release page; the bug's CHANGELOG line went into the rc rollup; the a2a bug dir is force-added (gitignored by pattern, allowed on main); trajectory JSONL not committed.
- **Operator-only (not performed)**: rotate the 401 `.env` key; create the `live-floor` environment (branch policy `main`/`release/*`, key); decide bd-c7ma; resolve v1.203.0/#1251 (recommend supersede); merge → inspect → `--publish --approve-sha256` → confirm Pre-release → `gh release edit --notes-file`; soak; promote via a `## [2.0.0]` heading. Full list in `reports/release-2.0.0-rc.1-operator-steps.md` and the PR body section.
- **Gotchas this session**: the FR-SZ2-COPY fence blocks `cp` into ANY path containing `.claude/` (even a scratch fixture) until the framework marker exists; FR-2 blocks `rm -rf "$VAR"` — use `mktemp -d` per run; FR-MERGE-2 matched the literal text "git tag" inside a `br create` title (rephrase); `post-merge-publication.bats` is red on this host only because the global git config forces annotated tags → KF-034 (`GIT_CONFIG_GLOBAL=/dev/null`); `git add` of a gitignored bug dir aborts the `&&` chain silently (use `-f`, check `git log -1`); `adversarial-review.sh` writes to `a2a/<sprint-id>/` — move the JSON into the bug dir before the COMPLETED write (gate looks there); Explore teammates' reports were not delivered as notifications — recovered from `~/.claude/projects/<proj>/<session>/subagents/agent-*.jsonl`; `date -u` on this host is 2026-09-22T22:xxZ while the local date is 09-23 — use UTC in trailers.
- Follow-up beads (`cycle-124-followup`): bd-vp9x multi-changelog parity, bd-fpt3 hermetic publication suite, bd-w8xd CHANGELOG-section release body, bd-bsth skip-template-guard ineffective on PRs, bd-cyti LOA_FORCE_LEGACY_ALIASES stale snapshot, bd-n8dm MODELINV schema writer_version, bd-g4zd dissent voice label mismatch, bd-kvnf bats-tests.yml `always()` controls (BB rc-pass F-003).
- **Bridgebuilder rc pass** (`bridgebuilder-20260922T223420-d989` on `4dd2982e`, OpenAI voice only — Anthropic 401, Google no `agy`; `entry.sh --pr 1266` posts only the `[2/4]` comment when the other voices error): 5 findings, 0 consensus/blocker. F-001/F-002 MEDIUM fixed in `b93aa93e` (live-floor artifact gate: whole-file JSONL-of-objects predicate `jq -e -n 'reduce inputs …'`; credential-indicator scan on the ledgers only — junit.xml's skip reason names `ANTHROPIC_API_KEY`, so it stays on the key-shape scan; test c124-1.8-6). Zone marker deleted after the last `.claude/` commit.
- **Phase E on `b93aa93e`**: CI 65 pass / 2 skipping / 0 fail; `tools/check-prompt-budget.sh` ok (kernel 10,225 B); `repo-map-gen.sh --validate` consistent; full `tests/unit/` 5515 ok / 34 not ok — 22 publication + 3 semver-evidence (KF-034, host git config; green isolated), 1 template-safety ISO, 8 licence grace-period fixtures — no new red; ledgers changed only by the BB rows (22:34–22:35Z) → KF-033 not triggered; `git status` clean; `semver-bump.sh --from-tag` → `2.0.0-rc.1` enter; `classify-pr-type.sh --title` → cycle. STOP: operator-only steps remain (see `reports/release-2.0.0-rc.1-operator-steps.md`).

## Decision Log — 2026-09-23 (2.0.0-rc.1 PUBLISHED — maintainer said "proceed with all the next steps")

- **Executed**: dead `.env` Anthropic key confirmed HTTP 401 against `/v1/models` and removed (backup `~/.env.loa-backup-20260923`, mode 600 — delete it once a new key exists); `live-floor` GitHub environment created (required reviewer deep-name, deployment branches `main` + `release/*`; **no secret yet** — needs a valid `ANTHROPIC_API_KEY`); Aleph tree decision recorded on bd-c7ma (keep inert through the rc, removal via post-rc `/bug`); parked v1.203.0 candidate superseded by the rc (framework-review §9 item 4 — its 21 commits are in the `[2.0.0-rc.1]` rollup); PR #1266 retitled (still `cycle`), marked ready, merged with a merge commit (`--admin`, review requirement bypassed as the sole maintainer) → `867cbc37`; post-merge run 35798553439 prepared `release-candidate-867cbc37…` with `tag v2.0.0-rc.1`, `prerelease: true`, `prerelease_transition.kind: enter`, target `21af972b` (one ledger-archive commit over the merge); recovered per runbook in an isolated clone (bundle verified, tree matched, digest `3d511113…` approved) and published with `--publish … --approve-sha256` → tag `v2.0.0-rc.1` (object `08dec65c` → `21af972b`), release id 394193250 `prerelease: true`, PR comment 5786297663, state `DONE`; rich notes installed via `gh release edit --notes-file`; repo "latest" release stays `v1.200.0`; label `2.0.0-rc` created.
- **Observed**: `git checkout main` in this clone landed on a stale local `main` (1 unique doc commit, 109 behind) — preserved as `main-local-stale-20260923`, `main` repointed to `origin/main` (`867cbc37`); the generated ledger-archive commit lives only on the tag, so `main`'s ledger still says cycle-124 active → bd-b4ji. `semver-bump.sh --from-tag` on main: "No commits since v2.0.0-rc.1" (expected; the next merge prepares `-rc.2`).
- **Remaining (operator)**: obtain a valid Anthropic key → store as `ANTHROPIC_API_KEY` in the `live-floor` environment → `gh workflow run live-floor-check.yml --ref main` (a recorded pass is an rc exit criterion); soak ≥ 2 weeks with `2.0.0-rc` issues; promotion PR with a `## [2.0.0]` heading when the exit criteria hold.

## Decision Log — 2026-09-21 (cycle-124 Sprint 3 — prompt audit, Task 3.6)

### 2026-09-22 — PR #1266 post-close: Aleph opt-in `/bug`, record branch, Bridgebuilder triage

- **Aleph is opt-in** (bug 20260922-13a3d1 / sprint-bug-239, `ae4cf1df`): maintainer directive — Aleph was not authored by the Loa maintainer and must never constrain Loa. `aleph.enabled` (default false) read by `lib/aleph-opt-in.sh` (POSIX awk, no yq) in front of mount/update, check-loa, the three aleph suites and both workflows (integrity gate reads the BASE ref's config on PRs). Residual: the vendored tree is unverified while opted out → bd-c7ma (remove/quarantine).
- **a2a record placement** (maintainer): the per-sprint record lives on the never-merged branch `record/cycle-124-a2a` (`e0ccc783`); this template repo forbids `a2a/sprint-*` and `a2a/trajectory/` as new files on main; bug dirs stay. Keep-list companion moved to `tools/prompt-keeplist.md`.
- **Release policy** (maintainer): major goes out as `2.0.0-rc.1` pre-release first, promoted after a soak.
- **Bridgebuilder on #1266** (six runs, five posted; openai voice only — anthropic 401 from the local `.env` key, bd-pr59; google needs `agy`): iter-1 F-001..F-004 fixed (`1e28a955`); iter-2 F-002 HIGH ref/fork guard + F-003 fixed, F-004 kept, F-001 accepted (`adffe433`); iter-3 F-001/F-002 HIGH → trusted refs only + artifact validation, F-003 → bd-7g2d (`df0cd2ec`); iter-4 F-002 HIGH → no raw test output in the log, release-precondition reframe, non-empty JSONL (`098d766f`); iter-5 plateau (no HIGH) → validation step without the key, missing key fails loud, sentinels on success() (`a20fffeb`); iter-6 completed but BB posted nothing and dry-run prints no body (bead filed). CI on `a20fffeb`: 64 pass / 2 skipping / 0 fail. Live-floor is now a release precondition (main / release/* only; the `live-floor` environment needs a deployment-branch policy before the key is stored).

### 2026-09-22 — cycle-124 close: Sprint 4 (global 238) approved; cycle deliverables complete

- **Sprint 4 gates**: review APPROVED round 1 (0/0/1/3; dissent clean), audit APPROVED (0/0/1/2; dissent clean). COMPLETED marker written; ledger 238 completed; epic bd-ianm closed.
- **G-6 (ledger identity across the unit suite)** failed twice and was fixed at the test seam (two suites; DS-1 tightened; KF-033); ledgers rotated per the runbook — archives `.run/archive/model-invoke-20260922T085656Z.jsonl`, `.run/archive/cost-ledger-20260922T085656Z.jsonl` (rollback data; untracked).
- **Guardrails finding (pre-existing)**: `guardrails-orchestrator.sh` passes `--mode run` but the danger-level enforcer accepts only `interactive|autonomous` → every run-mode input-guardrail check is an error and falls open (bd-rk9o, P2). Not fixed in this cycle (outside the four sprints' scope); flagged in the PR body.
- **Cycle close-out**: framework-review §9 status column filled; draft PR to `main` requesting @deep-name; `.run/zone-guard-authorization.json` deleted after the last `.claude/` edit (NFR-8); run state → JACKED_OUT once the PR URL is recorded.

### 2026-09-22 — Sprint 4 (global 238) FR-10 memory gate: implementation readings

- **`--delta` rule**: `check --delta N` refuses only when `N > 0` and `size + N ≥ 204,800` — one rule that covers "already over the line and growing" and "this write would cross the line"; shrinking and zero-delta writes always pass (compaction is never locked out).
- **Undated blocks**: dated `## Session Continuity` / `## Decision Log` headings rank by date (newest first); blocks with no parseable date rank after them by position with EARLIER first, because the live file prepends its newest entries. The generated fixture pins the dated rule with a mixed-order file.
- **rotate retains** the same selection as `read` (capped at 69,632 B) without the `read --full` footer, plus an `## Archive pointers` section naming the archive; archive first, `sync -d`, then tmp + `mv` in the same directory.
- **Hook denial shape**: stderr message + exit 2 (the zone-write-guard convention), not a JSON decision object; the hook is fail-open on any parse problem and hook-guard.sh allows on a syntax error.
- **No new knob**: `update-notes-learnings.sh` reuses the existing `LOA_GRIMOIRE_DIR` contract to find NOTES.md (needed by NG-11); thresholds stay literals in `notes-guard.sh` per the PRD.
- **Template wiring**: a hook wired only in `settings.json` fails `hook-wiring.bats` W4 and `lint-invariants.sh --hooks-wiring-only`; the entry must also sit in `.claude/hooks/settings.hooks.json` (the copy template).
- **Test payloads travel by file**: a 256 KB `Write` payload exceeds the 128 KiB single-argument limit (`jq --arg`, `bash -c '… "$1"'` both fail with E2BIG); the hook tests build payloads with `--rawfile` and feed them on stdin, exactly as the real hook input arrives.
- **Protocol byte budget**: the FR-10 doc edits left the protocol total at 199,525 B of 200,000 (warn line 143,360 still exceeded — bd-72fq).

### 2026-09-22 — Sprint 3 (global 237) audit round 1: CHANGES_REQUIRED on the eval executor

- **Decision**: audit tally 0/1/9/15; the HIGH (executor confinement) is fixed inside the sprint (failing tests first: EA-3 re-pin, EA-10, ID-10), the nine mediums and fifteen lows are beads (bd-vq7v bd-sk1t bd-tc3i bd-zklv bd-kqz4 bd-1ju5 bd-tjkx bd-2a9g bd-tdtr) plus rows in sprint.md's Sprint 3 follow-ups table; the A/B arms are NOT re-run (hours of wall-clock) and the record carries an environment caveat instead.
- **Why**: `--allowed-tools` only adds allow rules — the operator's `~/.claude/settings.local.json` `Bash(...)` rules applied inside the /tmp sandbox (Bash calls in both surviving transcripts, 30/181 trials wrote to /tmp). `--restricted --tools` is the CLI's own confinement (probe: out-of-cwd Write refused, in-cwd Write allowed). `HOME`/`PATH` stay for the CLI's credentials and toolchain; `sanitize_env` in sandbox.sh moves HOME and is not source-safe, so the env hygiene is four inline lines.
- **Severity reasoning written down**: the dissenter's rejected HIGH on guardrails fail-open is tallied MEDIUM (pre-existing design, operator-authored input, fences independent); the prompt-regression auditor's "pinned cheaper model" and "agent-network read-before-touch" drops are MEDIUM (rule loss without a mechanical backstop, no exploit path).
- **Operator note — host residue from the unconfined arms** (not deleted; generic names may collide with your own files): `/tmp/test_regex.sh`, `/tmp/test_regex2.sh`, `/tmp/test_ajv_probe.sh`, `/tmp/test_heuristic.sh`, `/tmp/test_bdb.sh`, `/tmp/test_scrub.sh`, `/tmp/test_grep.sh`, `/tmp/test_find_regex.sh`, `/tmp/test_find.sh`, `/tmp/test_continue.sh`, `/tmp/probe_test.sh`, `/tmp/stubbin_ajv_v5.sh`, `/tmp/run_test.sh`, `/tmp/audit_test.sh`, `/tmp/echotest.sh`, `/tmp/t.sh`, `/tmp/t.md`, `/tmp/line.txt`, `/tmp/l3.txt`, `/tmp/testline.txt`, `/tmp/multidoc.json`, `/tmp/route_test.txt`, `/tmp/route_test2.txt`, `/tmp/f.txt`, `/tmp/jqtest/`, `/tmp/ajvcheck/`, `/tmp/ajvtest/`, `/tmp/symtest/`; plus the four surviving sandboxes `/tmp/loa-eval-run-20260922-0015*`, `/tmp/loa-eval-smoke1-t1-*`.
- **KF-004** recurrence 31 (audit dissent dropped 2+2 findings for `missing-or-empty-failure_mode`; chunk 1's payloads lost to the per-run sidecar truncation — bd-tdtr).

- **Deliverable recovery**: the six `prompt-auditor` reports arrived as teammate messages whose idle notifications were truncated at 16,000 chars; the full text was recovered from the session JSONL (`message.content[].text` of the `<teammate-message>` line) and split into `units/<slug>.{report,proposed,resource-NAME}.md` — no re-run.
- **Auditor agent type (deviation from the plan's literal text)**: the plan asked for a read-only agent type with no Write/Edit tools; the fan-out ran `prompt-auditor` (Read/Grep/Glob) and, when in-conversation delivery proved lossy, `prompt-auditor-io` (adds Bash, contractually limited to saving its own files under the audit dir). The mechanical write boundary is the lead gate (`lead-gate.sh`: generated-block splice, keep-list grep, protected-string re-read, history-token scan, byte target) — the io agent's boundary is prompt-level, and every proposal went through the gate before touching `.claude/`.
- **Second passes are lead work**: every auditor landed 1.4–7.8 KB above the 16,384 target (their byte estimates ran 3–5 KB low). The lead cut the rest itself — deduplicating against CLAUDE.loa.md (Karpathy, precedence, agent-ergonomics pointers), moving conditional material behind guarded `resources/*.md` pointers, collapsing tables to prose — rather than dispatching a second agent round.
- **Test expectation changes**: `validation-skill-contracts.bats` now requires ≥1 (was exactly 2) `validate-ac-verification.sh` invocations in implementing-tasks — the count was structural, the invariant (each carries `--sprint "$SPRINT_FILE"`) is kept; `tests/fixtures/golden-{never,always}-table.md` regenerated from the registry after the constraints history-token cleanup.
- **Audit dir**: `grimoires/loa/a2a/sprint-237/` (ledger-resolved global id), not the plan's literal `sprint-3/`.
- **Protocol residuals accepted so far**: `helper-scripts.md` (16,830; script man page — contract material only, clean) and `constructs-integration.md` (14,434; loader reference, returned unchanged). The 200 KB aggregate must come from the remaining units; `helper-scripts.md` archival is the fallback if they fall short.
- **Pre-existing red, not this cycle's**: `tests/integration/test_process_compliance.bats` 6/7/10 grep CLAUDE.loa.md and autonomous-agent wording that already differed at the branch base 80be4b0f.
- **Task 3.6 closed 2026-09-22 (`7e8b8345`)**: all 13 skills ≤ 16,384 B, `CLAUDE.loa.md` 10,222 B, protocols 199,313 B (fail line 200,000 met; warn line 143,360 not — residual per file in the report, bead bd-72fq); 49 unit reports. Lead restored two auditor deletions before applying: the riding-codebase qmd advisory step (its integration test reads it in the skill body) and autonomous-agent's `### Implementation Guard` / `NEVER: Write application code directly` rule (fence-class rule; test_process_compliance greps it). `### Coverage` heading form aligned in auditing-security and both Flatline personas to the AC-9.1 wording. helper-scripts/constructs-integration were NOT archived to make byte room — the method forbids byte-count-only deletion; the aggregate was met by the remaining units.
- **Arm B incident (2026-09-22T00:28Z)**: one implement-05 trial recorded `prompt_tree_dirty: true` because a verification-matrix check (`gen-bb-registry --check` / `regen-model-artifacts.sh --check`) transiently rewrote files under `.claude/` while the trial ran; `compare-ab.py` correctly refused the arm as invalid. The row and its artifacts were discarded and the slot re-queued. Rule for the rest of arm B: nothing that writes under `.claude/` — including `--check` modes that rebuild and restore — runs while a trial is in flight.
- **Discipline metric is vacuous at 0/0**: `implement-discipline.sh` requires test-first (first write to `tests/` before the first write to `src/`); Sonnet 5 at medium wrote the source file first in 15/15 arm-A trials, so the composite pass rate is 0.0 in both arms and the `B ≥ A` gate is met trivially. The report breaks the metric down per check (surgical, zone, tests-pass, test-first) so the reader sees what the arms actually did; strengthening the implement prompt for test-first is out of scope mid-A/B (bead to file at close).
- **Arm B2 result (`f19dd6d0`, tree `397d9036`, review-recall only, 2026-09-22T05:40Z)**: anchored recall 0.944 (gate B ≥ A − 0.1 PASSES; −0.014 vs A), clean-PR critical+high 0.167 (one of six clean trials rates a different finding HIGH — a test fixture route that the validator's regex never extracts, `butterfreezone-validate-route-false-positive.bats:39`; the dotted-route heuristic is back to MEDIUM in all three pr-09 trials; gate B ≤ 0 FAILS by the letter), tokens/call B2/A 0.97, no dirty rows. Decision: stop at one iteration — a second tweak aimed at the one remaining HIGH would be hill-climbing on a 10-PR corpus. AC-9.2 is reported as partially met (review recall ✓ after iteration, review FP ✗ 0.17 vs 0, audit recall/FP ✓, audit tokens ✗ 0.76 vs ≤ 0.5, discipline vacuous ✓) and left to the review/audit gates; the audit and implement arms stand on `4f13f15c` (their prompts did not change).
- **A/B result (arm B on `734f3837`, tree `4f13f15c`, 2026-09-22T02:35Z)**: audit-recall recall 0.972 = A, clean-PR FP 0 = A, tokens/call B/A 0.758 (gate ≤ 0.5 FAILS — cache reads of the diff dominate; bead bd-gvxy); review-recall recall 0.958 → 0.820 (gate B ≥ A − 0.1 FAILS by 0.039) and clean-PR critical+high 0 → 0.333 (FAILS); implement-discipline 0.0 = 0.0 (vacuous pass). Per-defect and per-trial reading: the misses are citation granularity (B cites the neighbouring statement or a single line; A's ranges swallow the ±3 anchor — D04 462 vs 468, D06 79 vs 83) and the FPs are a severity shift (both arms found the same dotted-route heuristic on the clean PR; A said MEDIUM, B said HIGH). Decision: ONE diagnosis-driven iteration on reviewing-code only (cite the failing statement as a range; `high` needs a nameable failing input) and a review-recall re-run as arm B2; audit/implement arms stand. Both B and B2 are reported. Grader-side follow-up (credit must_match within the cited range) filed as bd-a4td — the grader is not changed after seeing results.
- **Arm B restarted from zero on `734f3837` (2026-09-22T00:5xZ)**: the final unit run found `simstim-flatline-mode.bats` red — the simstim auditor had pointed the three planning-gate reviews at the shared resource and the test greps SKILL.md for the literal `flatline-orchestrator.sh --doc … --phase … --json` invocations. Restored inline (16,368 B). Because `compare-ab.py` refuses an arm that mixes prompt trees, the 17 arm-B rows captured on `a424391c` were set aside and arm B restarted so it measures the tree the PR ships. Lesson recorded: run the full unit suite BEFORE starting arm B.
- **Second dirty row (01:02Z)**: the full unit suite I ran during arm B includes `cycle-124-anthropic-catalog.bats` c124-1.3-12, which executes `tools/regen-model-artifacts.sh` against the live tree (idempotence check) and leaves `.claude/` momentarily modified — one implement-04 trial recorded `prompt_tree_dirty: true`. Row discarded, slot requeued. Standing rule until arm B is captured: no unit suite, no generator, nothing under `.claude/` — the tree is an instrument.
- **A/B early signal (implement-discipline, 15 vs 15 trials)**: composite pass rate 0.0 in both arms because no trial wrote a test before its source file (`test_first` 0/15 each); per-check, arm B fell to 13/15 on `surgical` (two trials created `_verify.py` / `_verify_dedupe.py` scratch files at the workspace root) where arm A was 15/15. Small sample, but it is the kind of regression the diet could cause (the Karpathy "Surgical Changes" text moved from CLAUDE.loa.md to the protocol). Reported as-is; follow-up bead filed; no prompt change mid-A/B.
- **Arm B cap**: the review/audit runners hit the usage cap at 00:36Z (resets 14:40 Melbourne = 04:40Z); `session-limit-capture.sh` armed; the supervisor retries every 10 min and resumes automatically.
- **Arm B started 2026-09-22T01:3xZ on `7e8b8345`** (three suites, Sonnet 5 medium, same runner as arm A); no `.claude/` edit may land until both arms are captured and compared.
- **Sprint 1 fallout found by the first full unit run since 2026-09-17 (fixed under Sprint 3 as late input)**: (1) T1.8 (`fff40901`) overwrote `tools/ceiling-probe.py` — the cycle-109 fixture-backend probe that `loa-substrate-recalibrate.sh` and 10 bats depend on — with the new live binary-search probe; the cycle-109 tool is restored from `80be4b0f` and the live probe lives at `tools/ceiling-probe-live.py` (references in the live scaffold bats/pytest, workflow, CHANGELOG, catalog comment updated). (2) `bug-881-headless-context-window.bats` pinned `claude-headless context_window=200000`; the Sprint 1 catalog (`f5ea53f4`) set 1,000,000 deliberately — test expectations updated to the catalog. (3) `ledger-transactions.bats` #1248 paused workers at the literal pre-cycle jq filter text; the pause now matches the type-guarded filter from `a1337ed4`. (4) License-validator grace-period tests fail once the gitignored fixtures age past 24 h — regenerated via `ensure_license_fixtures.sh` after deleting them; fixture aging, not code.

### Active Context (2026-09-22, cycle-124 closed)
- **Cycle**: cycle-124 "model-generation floor" — sprints 235–238 all COMPLETED; draft PR open to `main`: https://github.com/0xHoneyJar/loa/pull/1266; review requested from @deep-name. Do not merge.
- **Next**: address PR review; schedule bd-rk9o (guardrails run-mode mismatch) and bd-vq7v (confined A/B re-measure) first among the `cycle-124-followup` beads; re-arm the zone marker before any `.claude/` edit.
- **Recovery read**: `.claude/scripts/notes-guard.sh read` (this file is bounded by FR-10 now).

## Decision Log — 2026-09-17 (cycle-124 kickoff — model-generation floor)

- **Requirements source, no interview**: PRD/SDD/sprint plan are authored from the operator-issued orchestration prompt (`grimoires/loa/a2a/prompts/modernization-cycle-2026-09-17.md`) and `framework-review-2026-09-17.md` §9, which already carry evidence + acceptance criteria per item. Session is unattended (Karpathy §1 unattended rule); the discovering-requirements backpressure interview cannot run without a user. Precedent: cycle-114 PRD (same "no fresh interview per operator direction" header). Open questions are recorded here, not asked.
- **Branch**: `feature/cycle-124-model-generation-floor` cut from `origin/main` 80be4b0f (the local `feature/cycle-123-bug-burndown` branch is superseded per the prompt). The local KF-018/KF-022 recurrence edit to `known-failures.md` was carried over as a patch (`git apply --3way`) because `origin/main` had moved that file.
- **Ledger**: cycle-122 archived via `ledger-lib.sh archive_cycle` (NOT `archive-cycle.sh` — its `--retention 5` default would have deleted 57 historical archive dirs; rec 3 state-hygiene candidate). cycle-124 created via `create_cycle` (which mints `cycle-085` from `.cycles|length`) and renamed with `update_cycle_field` to `cycle-124-model-generation-floor` to match the repo's id convention. `validate_ledger` reports "Missing required field: version" — pre-existing (ledger carries `schema_version`, validator wants `version`).
- **Zone marker**: `.run/zone-guard-authorization.json` rewritten (previous marker expired 2026-07-30 and was still present — rec 3(b) evidence); `expires_at` set 14 h into the run; delete at cycle end.
- **Credential path for live-call ACs (T1.1 thinking blocks, T1.4 cache_read_input_tokens > 0)**: `ANTHROPIC_API_KEY` unset, `ant` CLI absent, no `~/.config/anthropic` profile; the AWS/Bedrock credential probe was declined by the operator. The claude-headless path works (live PONG probe OK) but is not the HTTP adapter under test. Decision: implement + unit-test everything with request-body fixtures; treat the live ACs as an operator blocker unless a key appears (`ANTHROPIC_API_KEY` in `env.d`, or `ant auth login`). Do not borrow the Claude Code OAuth token for API calls (KF-020 class).
- **Flatline cohort for planning docs**: readiness DEGRADED (no anthropic key) → opus voice via claude-headless fallback chain; gemini tier-blocked (KF-018) under `hounfour.headless.mode: cli-only`; effective cohort = opus(headless) + codex. Accepted per the prompt. **Follow-up (PRD rounds 1–2)**: with a tertiary configured the orchestrator plans 3 voices, gets 2, marks `DEGRADED` and skips Phase 2 (no scoring/consensus, exit 6). The tertiary is set in TWO places — `flatline_protocol.models.tertiary` (line ~315) and `hounfour.flatline_tertiary_model` (line ~103), and the hounfour key wins in `get_model_tertiary()`. Both commented out (dated notes in the file) so the cohort is a true 2-voice plan (fleet baseline §10: no tertiary unless `agy` is installed). Rounds 1–2 raw reviews (28 + 25 improvements) were integrated by hand; see prd.md §11.
- **Planning decisions that deviate from the prompt's literal text (all recorded in prd.md §9)**: (1) `effective_input_ceiling` stays at the probed 180K on every Anthropic entry and the 36K non-streaming wall becomes a constant — the KF-002 wall is cheval HTTP-transport, not model capacity, so 1M context does not move it; raising it is a credentialed `ceiling-probe.py` step. (2) The KF-004 repair loop is kept at current depth (flag removed, always on) on the unenforced branch and deleted only where schema enforcement replaces it — Flatline skeptic CRITICAL + recurrence 28; retirement keyed to the measured `schema_enforced` ratio. (3) `claude-headless` forwards `--json-schema` (probed: `structured_output` returned) so the operator's live path is enforced. (4) The OpenAI `text.format` json_schema pass-through lands as an isolated, droppable commit (multi-provider fence exception, flagged). (5) Protocols byte budget enforced at 200 KB this cycle (warn 140 KB) — only 17 KB archives cleanly; residual reported per file. (6) No MODELINV `writer_version` bump (rollup pins 1.2). (7) PR opens as a draft; `live-floor-check.yml` (secret-gated) is the merge precondition for the adapter units.
- **Session cap hit by subagents (2026-09-17T01:43Z)**: the design-panel workflow's opus designers stopped with "You've hit your session limit · resets 3pm (Australia/Melbourne)" while the main session kept working. Captured via `session-limit-capture.sh` (`.run/session-limit-state.json`, reset 05:00Z). Workflow stopped and resumed sequentially (3 designers at a time) per operator instruction; completed agents replay from cache.

## Decision Log — 2026-09-17 (cycle-124 Sprint 1 — implementation)

- **Task 1.7 exit-2 semantics (PRD FR-5 vs SDD §3.4 drift)**: PRD text discriminates `verdict-derive.sh` exit 2 on `.trailer_found == false` → prose fallback; SDD §3.4 (and the implementation) deny on ANY non-zero rc because the golden-path call site already guards on the `<!-- LOA-VERDICT ` marker, so the helper never sees a marker-less file. SDD version shipped (fail-closed is the loud direction). The audit cross-check requires `excluded_confirmed` to EQUAL the review's `excluded` (greater also denies).
- **`tests/integration/test_golden_path.bats` was 47/47 red on main** (setup copied golden-path.sh but not compat-lib.sh, sourced since PR #1028; not in CI) — one-line fix applied in Sprint 1 because the suite is golden-path's own integration coverage. Not a fence change.
- **Catalog evidence file location**: SDD §2.5 said `grimoires/loa/a2a/sprint-N/catalog-evidence.md`, but `grimoires/loa/a2a/*` is gitignored (sprint-scoped agent exchange). The evidence must outlive the sprint (operator flips `reference → probed`), so it lives at `grimoires/loa/reports/2026-09-17-cycle-124-catalog-evidence.md` and the catalog comments point there.
- **AC-3.5 matrix vehicle**: `cheval --dry-run` returns before the pre-flight gate and `--mock-fixture-dir` bypasses both input gates (verified 700K-char input passes in mock mode), so the cheval leg of `tests/integration/input-size-consumers.bats` runs the in-process harness (`cheval.cmd_invoke` with a fake adapter, the pattern of `test_preflight_preempt_modelinv.py`) via pytest; the BB leg is a node one-liner over the generated twins.
- **Sonnet 5 pricing**: the cycle-120 entry carried $3/$15 as the post-2026-08-31 rate; the claude-api reference table (cached 2026-06-24) lists $2/$10 and the PRD/SDD chose the reference. Metering-only; flagged in the evidence file for the probe to settle.
- **`LOA_CHEVAL_LEGACY_WIRE` also restores the 4096 default** (not only the thinking/cache/format emission): a "legacy wire" that still sent 64K would not be the pre-cycle body, and the golden-body test is only meaningful if the whole request is byte-identical.
- **FR-6 discovery allowlist**: the Task 1.1 agent proposed a shrink-only `KNOWN_UNCOVERED` list for 8 remaining spawners; the lead added the two export lines to the 4 bats suites and a `tests/replay/conftest.py` autouse fixture for the 4 replay pytests instead, so the list is empty and AC-6.4 holds literally.
- **Pre-existing failures found while adding suites to CI** (re-baselined, not fixed in code): `cheval-input-gate.bats` G1/G8 pinned the legacy `[input-gate]` marker that the cycle-109 preflight superseded on a CLI override, and G7 pinned the pre-split `max_input_tokens == 24000` for gpt-5.5-pro (live: streaming 200K / legacy 27K). BB `__tests__/persona.test.ts` fails on HEAD without `ANTHROPIC_API_KEY` (a subtest reaches the CLI's fatal credential check) — env-dependent, untouched.
- **`validate-ac-verification.sh --sprint-id`** (framework change outside the sprint task list): the gate extracted every `### Acceptance Criteria` block in `sprint.md`, so with a four-sprint plan the Sprint 1 report failed for not quoting Sprints 2–4's bullets. Added an optional `--sprint-id sprint-N` that restricts extraction to the `## Sprint N` section (usage error when the section is absent), one bats case, and the two SKILL.md invocation lines. Without it the only ways through were quoting other sprints' criteria as "deferred" (false) or feeding the validator a hand-cut extract (gaming the gate).
- **Session cap (again)**: the Task 1.1 subagent stopped at 06:02Z with "You've hit your session limit · resets 8:20pm (Australia/Melbourne)" after finishing ~90% of FR-6; the lead completed the remainder inline (`session-limit-capture.sh` armed).

## Late Sprint 2 review input folded in — 2026-09-21T01:20Z (cycle-124, Sprint 3)

- The three read-only reviewer subagents (adapters / shell / evidence) delivered on 2026-09-19 after sprint-236 closed. Verified and fixed under Sprint 3: slice B in `a1337ed4`, slices A/C in the next commit; the Sprint 2 report's measurement block and the KF-004 row were corrected from the archive (claude-headless 3/3 enforced; codex-headless 10/10 unenforced, not "2/2"). Disposition table: `a2a/sprint-236/engineer-feedback.md` §Addendum; trailers re-derived (APPROVED 0/0/0/2 unchanged).
- Decision: late panel input is folded in, never discarded — same handling as the Sprint 1 slice-C report. Follow-up bead: cache the claude-headless `--help` capability probe across processes.
- Sprint 3 mechanics: arm-A baseline runs on the pinned worktree `72bdb7d2` with `EVAL_MAX_TURNS=80` (40 truncated a 41-turn review), Sonnet 5 at fixed `medium` effort; a usage-credit outage on 2026-09-19 killed the first six auditors and stalled the arms; resumed 2026-09-21.

## Sprint 2 CLOSED — 2026-09-20T00:45Z (cycle-124, global sprint-236)

- Gates: review round 1 APPROVED (0/0/0/2; dissent DISS-001 — FR7-10's shared sidecar assertion — fixed in `037dee75`, then three clean passes on the final diff); audit APPROVED (0/0/0/2; audit dissent `reviewed`, 0 findings, sidecar empty). Two read-only reviewer trios were dispatched; both died on the session cap / ran 45 min without delivering and were stopped — recorded in the feedback rather than hidden. COMPLETED marker through the gate hook; ledger 236 `completed`; run state → sprint-3 IMPLEMENT.
- Head `012d0c5e`, 18 commits since `087fc39e`; adapter pytest 2256/0; Sprint 2 bats 75/75; gate suites green; tripwires OK. Live measurement: claude-headless 3/3 `schema_enforced`, codex-headless 2/2 unenforced.
- Follow-up beads to create at cycle end: codex/gemini `--output-schema` forwarding; mock-dispatch marker in the MODELINV envelope for the hygiene scanner; OpenAI strict `anyOf` live check; repair-loop retirement keyed to the ratio; SDD §2.3 `format` wording.

## Late Sprint 1 audit input — slice C (2026-09-18T23:2xZ)

- The shell-gates audit subagent's report arrived AFTER the Sprint 1 verdict/COMPLETED (first delivery truncated at 16 K chars, second killed by the session cap). Verified and fixed in the Sprint 2 tree; the Sprint 1 audit file carries an addendum table (tally unchanged: everything closed). Headline: the golden-path trailer guard matched only the exact byte string `<!-- LOA-VERDICT ` while `_gp_trailer_int` was whitespace-tolerant — a tab/NBSP/U+2010 marker fell through to the prose heuristic, whose `grep -q APPROVED` matches "NOT APPROVED" (reproduced). Fix philosophy: DETECT loosely (any HTML comment reading LOA…VERDICT) and ACCEPT strictly (`verdict-derive.sh` rejects any marker that is not exactly `<!-- LOA-VERDICT {json} -->`), so a malformed marker can only deny. Bash integer wrap at 2^64 closed with a six-digit cap in both scripts. The `bash -c 'source …; ${MODEL_IDS[$2]}'` lookup in `adversarial-review.sh` evaluated command substitutions in a model id when the maps file failed to source (reproduced): allowlist + `declare -A` pre-declaration + fatal source failure.
- Lesson for the harness: a subagent that hits the session cap mid-report loses everything not yet sent; ask for ≤ 10 K-char reports and treat "no report" as "audit that slice by hand" (done for slice C in Sprint 1 — the by-hand pass missed the marker-variant class; the subagent's reproduction is what found it).

## Decision Log — 2026-09-18 (cycle-124 Sprint 2 — structured outputs)

- **Wire enums follow the PROMPT, validator stays wide (SDD §2.3 parity refined)**: the dissent prompts advertise 11 (review) / 12 (audit) categories while `validate_finding` accepts 20 for both types. The parity test asserts wire == prompt (what the model is asked for) and wire ⊆ validator (a schema-valid finding is validator-valid by construction); shrinking the validator to the prompt list would start rejecting broader tags from unenforced voices — the KF-004 class. Prompt enums are label-anchored (`CATEGORY (required, one of):` … `OUTPUT:`) because the prompts are single-quoted bash strings; the validator list is marker-anchored (`# wire-enums:validate:start/end`).
- **`$schema` meta-URI removed from the wire schemas (live finding)**: the first measurement run failed every claude-headless voice — `claude -p --json-schema` rejects a schema declaring `https://json-schema.org/draft/2020-12/schema` ("no schema with key or ref"). Neither provider needs the meta-schema; W3 now forbids `$schema`/`$id`/`$comment`/`$anchor`/`$dynamic*`/`$vocabulary`; a re-probe with the corrected scorer schema returns `structured_output` (stop_reason `tool_use`). The headless adapter also retries a CLI-side schema rejection once unenforced so a voice never drops out over a schema/CLI mismatch.
- **`stop_reason` added to the CLI JSON and `translate_output`**: SDD §3 wanted a `stop_reason ∉ {end_turn}` guard on the enforced branch, but the adapter boundary dropped the field; cheval's CLI JSON now carries `stop_reason` and the dissent treats `max_tokens` as a truncated (malformed) enforced payload.
- **Repair loop flag removed, loop kept**: `repair_loop:` gone from both configs and `CONF_REPAIR_LOOP` from the script; the loop runs unconditionally on the unenforced branch and never on the enforced one; the DEGRADED trajectory record on `rejected_count > 0` is unconditional (`_emit_rejection_degraded`).
- **Measured ratio (AC-7.4)**: one Flatline spec run (`ledger-hygiene-rotation.md`, 4/4 Phase-1 calls, 2/2 voices) + one review dissent on the Sprint 2 diff: `claude-headless` 3/3 `schema_enforced=true` (review, skeptic, score), `codex-headless` 2/2 `false` (schema not forwarded to codex/gemini headless — follow-up bead, multi-provider), i.e. 100 % on the only hop that can enforce on this host. Recorded rows archived at the Sprint 2 rotation.
- **Ledger pollution found and fixed (FR-6 gap)**: `adversarial-review-e2e.bats` and `adversarial-review.bats` run the real script in `FLATLINE_MOCK_MODE`; cheval's mock path still appends MODELINV + cost rows, and the discovery test only knew the DIRECT spawners — 2 mock cost rows and 6 mock MODELINV rows (real model ids, mock fixture values) landed in the operator's `.run/`. Both suites redirect now; the discovery pre-filter/ENTRY_RE cover `adversarial-review.sh` and `flatline-orchestrator.sh`; ledgers rotated per the runbook. The MODELINV hygiene scanner cannot see mock rows that carry real model ids — follow-up bead: stamp mock dispatches in the envelope.
- **Framework fix outside the task list**: `ledger-lib.sh` jq walks over `.cycles[].sprints[]` errored on bug-fix cycles' label-string entries, so `update_sprint_status` had returned LEDGER_ERROR all sprint (235 stayed `planned`); guarded with `select(type == "object")`, bats repro, ledger corrected.
- **`grimoires/loa/ledger.json.lock`** (ledger-lib's flock target) is now gitignored — it appeared untracked after every ledger transaction.

## Sprint 1 CLOSED — 2026-09-18T22:50Z (cycle-124, global sprint-235)

- Gates: review round 1 CHANGES_REQUIRED (0/5/9/9) → round 2 APPROVED (dissent DISS-001 fixed, four dissent passes clean) → audit APPROVED (0/0/2/1; dissent sidecar HIGH fixed in `44e1feab`, slices A/B/D fixes in `2b43baf4 1991390f 4670b22f`, residuals AUD-R1/R2/R3 recorded). COMPLETED marker written through the gate hook; ledger 235 `completed`; beads 1.5/1.6/1.8/1.9/1.10 closed; run state → sprint-2 IMPLEMENT.
- Head `087fc39e`, 25 commits since `80be4b0f`; adapter pytest 2203/0; bats verification set 142/142 + 58 schema + 17 cost-rollup; drift gates OK; hygiene tripwire OK.
- Open follow-up beads to create at cycle end: exit 7 vs 12 duality; 120 s orchestrator/BB timeouts vs 16K answers; `MODE_TO_AGENT["dissent"]` skill dir; `thinking_default_on` catalog flag (later cycle); operator: configure the `live-floor` environment protection before storing the key.

## Decision Log — 2026-09-17 (cycle-124 Sprint 1 — review round 1)

- **Round-1 review verdict CHANGES_REQUIRED (0 critical / 5 high / 9 medium / 9 low)** from the five-lens independent panel (Workflow `wf_eee49f5a-2c9`; verifiers mostly failed on usage credits, so the lead confirmed each finding against the tree by hand) on top of a clean cross-model dissent (gpt-5.5-pro, 0 findings — a weak positive on a 24K-token slice of a 400 KB diff). Every finding was accepted; none demoted.
- **PRD FR-4 vs SDD §2.2 drift resolved in favour of the PRD**: the persona-less `--system` payload is the stable prefix (Bridgebuilder: `--agent reviewing-code` has no persona.md; its 9 KB `INJECTION_HARDENING` + `bridgebuilder-persona.md` file was sent unmarked, so the PRD's named primary beneficiary never cached). The SDD sentence, the eligibility table and the pinned test all said "unmarked"; corrected (SDD-R1-1). A call with neither persona nor `--system` still sends no system block.
- **Cost-ledger anchoring**: relative config/default paths now anchor at the project root like the MODELINV twin; a relative `LOA_COST_LEDGER_PATH` stays CWD-relative because it is the test/operator redirect (the conftest sets absolute tmp paths). Existing non-regular targets (directory, `/dev/null`) are `INVALID_CONFIG` at resolve time rather than an exception inside `BudgetEnforcer.post_call` after the billed call (SDD-R1-2). `rollup.default_ledger_path()` and `cost-report.sh` now run the writer's resolver over the merged config — the old reader read the system-defaults yaml only and pointed at `grimoires/loa/a2a/cost-ledger.jsonl`, a file the writer (project overlay: `.run/cost-ledger.jsonl`) never wrote.
- **`FLATLINE_SCORE_MAX_TOKENS` 4 000 → 16 000** (SDD-R1-3): the scorer now runs on the retargeted `opus` voice with adaptive thinking on; a 4K budget is spent on reasoning before the JSON array. B3 re-pinned; the report's cost table moves the scorer to the 16K column.
- **`tools/regen-model-artifacts.sh` exit 3 = toolchain missing** (npm/node/BB node_modules), never 1 = drift; the two catalog bats cases that need the toolchain skip without it. Exit 2 stays usage.
- **Kept as-is, disclosed in the PR body**: the explicit `--max-tokens` clamp applies on every provider hop, not only Anthropic (a value above a fallback hop's catalog cap would otherwise 400 on the rescue hop). Pre-flight exit 7 vs legacy-wall exit 12 for one refusal class, and the 120 s orchestrator/BB timeouts against 16K answers, are follow-up beads, not Sprint 1 changes.
- **Live scaffold AC-4.3 rebuilt through the production path** (`cheval._persona_messages` + `AnthropicAdapter._transform_messages` with the real BB prefix): the hand-built body could not have observed the FR-4 defect. `tools/ceiling-probe.py` counts a stream that reaches `message_stop` as OK (default-on thinking can spend a 16-token budget on reasoning — a budget outcome, not a size failure), asks for 1024 output tokens and records `stop_reason`.
- **Round-2 dissent DISS-001 accepted (BLOCKING → fixed `12b6067e`)**: the round-1 reader fix ran the writer's resolver but swallowed its `ConfigError` and fell back to a literal `.run/cost-ledger.jsonl` — a refused path became a silent read of another file. Readers now fail closed like the writer; an explicit `--ledger` never consults the resolver; the literal fallback survives only for an unavailable Python substrate. Round-2 dissents run on codex-headless (CLI, $0 metered); the audit dissent's `budget_exceeded` (estimate 310 ¢ > 200 ¢ default on the 900 KB diff) was lifted with `--budget 400` because the estimate prices API tokens the CLI hop does not bill — real spend today per the cost ledger: $0.
- **Audit dissent finding recovered from the KF-004 sidecar (HIGH, accepted)**: the audit dissent's envelope said `reviewed`, 0 findings, but `adversarial-rejected-audit.jsonl` held one payload rejected for `missing-or-non-string-id`: `live-floor-check.yml` ran on `pull_request` and passed `ANTHROPIC_API_KEY` to a pytest step over PR-controlled code (`HAS_KEY` only proves the secret exists). Fixed: `workflow_dispatch` only + `environment: live-floor` (key stored as an environment secret behind required reviewers), operator line in the report updated, bats lock c124-1.8-5. The recovery path itself is the KF-004 class (recurrence +1, attempt row): the lead reads the sidecar after every dissent, and Sprint 2's structured outputs (FR-7) are the fix for the omission.
- **Audit dissent, final pass — residual accepted (MEDIUM, operator control)**: with the workflow manual-only, the dissenter's sidecar still (correctly) notes that a dispatched run executes the candidate branch's test code with the environment secret. No checkout arrangement removes that from a live check of the branch's own adapter; the control is GitHub's environment protection — required reviewers + a deployment-branch rule on `live-floor` before the key is stored, never a repository-level secret. Recorded in the workflow header and the report's operator item; not a code change.
- **Audit slices (four read-only subagents, all four hit the session cap mid-report; three reports recovered in full or in part)**: accepted and fixed — unconditional ledger isolation in the conftests (an operator export must not collect test rows), sidecar `O_NOFOLLOW`, readers refuse on an unloadable merged config, `cost-report.sh` without `cd`, budget-aware non-streaming read timeout (lengthen only, cap 600 s), `ModelNotFoundError` bypasses the breaker, Bridgebuilder retry from the clamped budget via one `effectiveInputBudget` helper, typed pricing schema, three tautological/over-mirrored tests made falsifiable. Accepted with documentation only: `LOA_CHEVAL_LEGACY_WIRE` cannot turn thinking off on 5.x (server default-on) — a `thinking_default_on` catalog flag would be a new config surface the operator prompt excludes; the stop-reason warning covers the truncation case. Known/follow-up: pre-flight vs legacy-wall exit codes. Slice C (shell gates) never delivered its report; the lead re-audited that slice by hand (temp-file hygiene, `bash -c` subscripts, trailer shapes, CI secrets) and the two dissent passes covered `golden-path.sh` and `adversarial-review.sh`.
- **RED-first figure corrected in the report**: the committed `test_anthropic_catalog_floor.py` is a collection error on 80be4b0f (it imports `_LEGACY_TRANSPORT_INPUT_WALL`, which did not exist); the earlier "24/26 failed" came from a draft that stubbed the import. A collection error is red, but the report should say what was actually run.

## Decision Log — 2026-07-29 (cycle-121 sprint-1 — context diet)

### Verification-integrity correction (cycle-121/122 merge gate)
- **My earlier "full unit suite exit 0" claims were observation errors**: the bats invocations were piped to `tail`, so the recorded exit code was tail's, and failures at scroll-back positions were invisible. CI caught 5 real failures the masked runs missed (dead-recall-relabel tests asserting markers on files sprint-3 deleted; R-006 capabilities drift from an incomplete de-advertising sweep; README memory-query link). All five fixed; suite re-run WITHOUT pipes on the bats invocation (exit captured directly). Lesson: never pipe a verification command's output at the invocation — write to file, inspect after. Same swallowed-exit class as the stash/jq fences this very cycle shipped.

### Read-before-Write row decision (cycle-122, operator-delegated)
- **Decision: KEEP** the C-PROC-018 ALWAYS row. The operator's condition ("remove this error type without degrading quality") cannot be met with available evidence: the error class is already loss-proof (the Write tool's harness rejection), so the row's job is turn-waste prevention, and the only measurement that could justify removal is FLEET telemetry (the ~570/month figure from #1177-F) re-run under Claude-5 models — not accessible from this repo.
- **Flip evidence** (tracked in bead swm-rbw-remeasure): fleet telemetry showing the read-before-write rejection class collapsed (>90% reduction) under Opus 5 / Fable 5 sessions → delete the row via registry (C-PROC-018 claude-loa-md layer), keeping the AGENTS.md projection decision explicit for non-Claude harnesses that have no mechanical rejection.

### checksums.json hand-edit rationale (NC-2, review round 1)
- Removed 15 deleted-file rows from `.claude/checksums.json` by hand instead of regenerating: integrity enforcement is unset in this repo, the manifest has been stale since 2026-01-17 (pre-existing), and running `generate_checksums()` inside the dev repo would hash 44MB of local untracked `node_modules` under `.claude/skills/bridgebuilder-review/`. Full regeneration is a mount/update-time operation (`mount-loa.sh` / `update.sh`). Do NOT "fix" by regenerating here.
- evals fixture `loa-skill-dir` is a plain snapshot (no hash pinning in fixture.yaml); its embedded mount-loa.sh comment was synced when url-registry.md was deleted.

## Session Continuity — 2026-07-29 (cycle-121 context diet, run-20260729-68d1b75c)


### Post-PR Validation Checkpoint
- **ID:** post-pr-20260729-2ab3b23b
- **PR:** [#1238](https://github.com/0xHoneyJar/loa/pull/1238)
- **State:** CONTEXT_CLEAR
- **Timestamp:** 2026-07-29T07:56:33Z
- **Next Phase:** E2E_TESTING
- **Resume:** Run `/clear` then `/simstim --resume` or `post-pr-orchestrator.sh --resume --pr-url https://github.com/0xHoneyJar/loa/pull/1238`
- **Current state**: /run sprint-plan executing cycle-121 (5 sprints). Sprints 1-2 COMPLETE (review+audit approved). Sprint 3 in progress.
- **Run state**: `.run/sprint-plan-state.json` (RUNNING) + `.run/state.json`; branch `feat/cycle-121-context-diet`.
- **Recovery**: read `grimoires/loa/sprint.md` (plan + Scope Contract), then `.run/sprint-plan-state.json` for position; resume autonomously per Run Mode.

## Blockers

None. (KF-005/KF-022 beads degradations at kickoff were routed around per their Reading guides — see KF ledger evidence rows.)

## Archive pointers

- Pre-cycle-121 working notes (cycles 052-054, 092-094 Decision Logs, rollback plan, legacy Decisions table incl. the 106 cache-junk rows): `grimoires/loa/archive/2026-07-29-notes-pre-cycle-121.md`
- Pre-cycle-121 a2a sprint dirs: `grimoires/loa/archive/2026-07-29-pre-cycle-121-a2a/`
- Cycle-116/119/120 ship records: same archive file (deferred work lives in beads: bd-ge58, bd-x0rd, bd-toag2)

## Observation — 2026-07-05 (skill-loop pass 4, settings-cleanup latent defect)

- **Finding**: `.claude/hooks/hygiene/settings-cleanup.sh` post-cleanup secret scan could NEVER detect the `'-----BEGIN .* PRIVATE KEY'` pattern: `grep -qP "$pat" || grep -qE "$pat"` passes the pattern as the first argument without `--`, so the leading-dash pattern is parsed as grep OPTIONS → rc=2 (silenced by 2>/dev/null) → reported as no-match on GNU and BSD grep alike. Found by pass-4 differential testing (staging/pass4/tools/run-diff.sh, case sc-privkey-in-deny-preserved-defect).
- **Status**: DELIBERATELY PRESERVED in the pass-4 jq rewrite (leading-dash patterns report no-match) — the perf pass is behavior-identical by contract.
- **Follow-up**: needs its own test-first /bug cycle: make the scan actually warn on remaining private-key material (the jq scan can simply drop the leading-dash guard; a grep-based fix would be `grep -qP -e "$pat"`). Note the main filter (which REMOVES such entries from permissions.allow) was never affected — only the post-rewrite warning scan.

## Observation — 2026-07-05 (skill-loop pass 5, beads-health missing-owner detection latent defect)

- **Finding**: `.claude/scripts/beads/beads-health.sh` `check_schema()` can NEVER report `missing_owner` / MIGRATION_NEEDED: `has_owner=$(sqlite3 … "PRAGMA table_info(issues);" | grep -c "owner" || echo "0")` — when the owner column is absent, `grep -c` PRINTS "0" AND exits 1, so `echo "0"` appends a second zero and `has_owner` becomes the two-line string `0\n0`. The follow-up `[[ "${has_owner}" -eq 0 ]]` is then a bash arithmetic syntax error (stderr: `[[: 0\n0: syntax error in expression`) which evaluates FALSE, so the code falls through to `schema=compatible` and a no-owner database reports `"status": "HEALTHY"`, exit 0. Found by pass-5 differential testing (staging/pass5/tools/run-diff-pass5.sh, case bh-noowner-quick-json). Same double-zero shape also fires when sqlite3 itself fails.
- **Status**: DELIBERATELY PRESERVED in the pass-5 spawn-reduction rewrite (the `grep -c … || echo "0"` line is untouched) — the perf pass is behavior-identical by contract.
- **Follow-up**: needs its own test-first /bug cycle: `grep -c` already prints the count on no-match, so the `|| echo "0"` is the bug (`|| true` or dropping the guard entirely fixes it); exit-3 schema-migration detection for missing owner would then actually fire for pre-owner beads databases.

## Observation — 2026-07-06 (cycle-116 D3: advisor_strategy dormant + `agents.flatline-scorer.model: cheap` is a dead comment)

- **Finding (dormancy)**: `advisor_strategy.resolve()` is structurally unexercised in production. It only fires from `cheval.py:1069` (`if getattr(args,"role",None) and not args.model:`), and every production dispatch site passes `--model` explicitly with no `--role` (`flatline-orchestrator.sh` `call_model`, `model-adapter.sh`, `hitl-jury-panel-lib.sh`, `red-team-model-adapter.sh`). So `defaults`/`per_skill_overrides`/`tier_aliases` load + validate but never route a real call. D3 (bd-c116-d3-tiering) wires exactly one stage (flatline-scorer, score-mode) into this path behind a default-OFF flag `advisor_strategy.stage_routing.flatline_scorer`.
- **Finding (dead comment)**: `.claude/defaults/model-config.yaml:911-914` binds `flatline-scorer` to the `cheap` tier (Sonnet 4.6) with a cycle-114 FR-13 comment claiming scoring is cost-demoted to Sonnet. It is NEVER live: the scorer's real cross-scoring calls (`flatline-orchestrator.sh:1498-1535`, `call_model "$secondary_model"/"$primary_model" score …`) pin `--model` explicitly to the advisor-class primary/secondary (today opus/gpt-5.5). `--dry-run --agent flatline-scorer` resolves `claude-sonnet-4-6`, but the orchestrator never uses that path. The advertised demotion has never executed. Documentation-vs-behavior drift, not a behavioral bug → recorded here + in `grimoires/loa/runbooks/model-tiering-per-stage.md`, deliberately NOT a KF entry (per cycle-116 lead decision).
- **A/B result (scoring ≠ BB review)**: PR #885's "executor UNSAFE (−60% findings)" tested Bridgebuilder REVIEW, not scoring. D3's live gold-set A/B (`tests/replay/test_d3_flatline_scorer_tier_ab.py`, `LOA_RUN_LIVE_TESTS=1`, 10 trials) measured scoring directly: advisor(opus-4-8) mean would_integrate agreement 0.90 vs executor(sonnet-4-6) 0.90; paired bootstrap mean_delta 0.0, 95% CI [−0.10,+0.10] → INCONCLUSIVE (promising, not proven; n=5×6 too small). Verdict: flag STAYS false; recorded for a future operator decision. Results under `grimoires/loa/cycles/cycle-116-quality-per-token/d3-flatline-scorer-ab/`.
- **Gotcha (tier_aliases ≠ cheval aliases)**: `advisor_strategy.tier_aliases.<tier>.<provider>` stores bare model-ids. cheval `--model` wants an alias OR `provider:model-id`. `claude-opus-4-8` happens to be a registered alias (advisor passed), but `claude-sonnet-4-6` is NOT (its alias is `cheap`) → `INVALID_CONFIG: Unknown alias` on the executor tier. Fix in both the A/B test and `tools/stage-tier-benchmark.sh`: emit/pass `provider:model-id` form. Only affects benchmark tooling that passes `--model` with a raw tier_alias; the real flag-on dispatch path uses `--role` (no `--model`) so cheval resolves the provider correctly.


## Learnings

*Extracted learnings will be added here.*


No new learnings to add.
