# Legacy Documentation Inventory

> Generated by `/ride --enriched` 2026-05-04. Inventory of pre-existing documentation in the framework repo.
> Total `.md` / `.rst` / `.adoc` files (excluding `node_modules/`, `.git/`, `archive/`, `a2a/`, `audit-archive/`, vendored deps): **1,147**

This inventory does NOT mark these docs as deprecated — Loa is itself a documentation-heavy framework, and most "docs" here are operational (skill READMEs, protocol specs, fixture data). Phase 8 deprecation is therefore **skipped** for this ride; legacy docs in the framework repo are NOT replaced by the freshly-generated PRD/SDD.

## Distribution by area

| Area | File count | Notes |
|------|-----------:|-------|
| `evals/fixtures/` | 310 | Eval test fixtures — operational, not legacy |
| `grimoires/loa/` | 151 | Mix of cycle PRD/SDD/sprint planning docs (current + historical) |
| `skills/legba/` | 139 | Vendored skill package — operational |
| `.claude/skills/` | 124 | Skill definitions and resources — current |
| `.run/bridge-reviews/` | 95 | Auto-generated bridge review artifacts |
| `.claude/protocols/` | 56 | Process protocols (each `.md` is a spec) |
| `.claude/commands/` | 53 | Slash command definitions |
| `.loa/venv/` | 37 | Python virtualenv-shipped docs |
| `.claude/constructs/` | 36 | Construct registry definitions |
| `.run/red-team/` | 27 | Red-team scenario archives |
| `tests/fixtures/` | 23 | Test data |
| `.claude/loa/` | 14 | Framework-managed CLAUDE.loa.md + reference docs |

## Key top-level docs

| File | Lines | Purpose | Quality assessment |
|------|------:|---------|---------|
| `README.md` | 246 | Public-facing project README | Comprehensive, current (v1.110.1) |
| `INSTALLATION.md` | 1,029 | Detailed install guide | Long-form; canonical install instructions |
| `CHANGELOG.md` | 6,636 | Version history (very large) | Active — last entry 2026-04-15 |
| `CLAUDE.md` | 35 | Project-specific Claude instructions | Thin (35 lines); imports `.claude/loa/CLAUDE.loa.md` (338 lines) |
| `.claude/loa/CLAUDE.loa.md` | 338 | Framework-managed Claude instructions | Comprehensive — protocols, rules, MAY/MUST/NEVER tables |
| `PROCESS.md` | (large) | Workflow documentation | Power-user reference |
| `SECURITY.md` | — | Security disclosure policy | Required governance file (present) |
| `CONTRIBUTING.md` | — | Contribution guide | Required governance file (present) |
| `LICENSE.md` | — | AGPL-3.0 license | Required governance file (present) |
| `BUTTERFREEZONE.md` | — | Token-efficient project summary | Auto-generated by `/butterfreezone` |

## CLAUDE.md quality score

The instructions are split between a thin `CLAUDE.md` (35 lines) and a thick framework-managed import `.claude/loa/CLAUDE.loa.md` (338 lines).

| Criterion | Pass? | Evidence |
|-----------|:-----:|----------|
| Length > 50 lines | **Yes** (combined 373 lines) | wc -l |
| Tech stack mentioned | Partial | `bash`, `python` referenced via protocols, but not enumerated explicitly in CLAUDE.md |
| Pattern / convention guidance | **Yes** (70 hits) | NEVER/ALWAYS/MUST tables in CLAUDE.loa.md |
| Warnings | Indirect | Routed via `.claude/rules/*.md` references; no inline warnings |

**Combined score**: 5 / 7. The combination is comprehensive but the user-facing `CLAUDE.md` itself is sparse — would benefit from a one-paragraph summary of project lineage + active cycle context (currently provided only via `auto-memory`).

## Cycle-scoped artifacts in `grimoires/loa/`

The grimoires root contains historical `prd-*.md`, `sdd-*.md`, `sprint-*.md` files from prior cycles (autonomous-flatline, post-pr-integration, configurable-grimoires, etc.). These are NOT legacy in the deprecation sense — they're cycle archives that didn't migrate into `cycles/`. Worth a future hygiene pass to relocate them under `archive/` or `cycles/` for consistency, but no action this ride.

The current `prd.md` and `sdd.md` (in `grimoires/loa/`) describe **cycle-098 Agent-Network Operation Primitives**, NOT the framework as a whole.

## Phase 8 (Legacy Deprecation) decision

**Skipped** for this ride. Reasoning:
1. The framework repo's docs are mostly operational specifications, not stale narrative docs about a long-gone implementation.
2. The current `prd.md` / `sdd.md` are cycle-scoped (cycle-098), not framework-wide; the ride's framework-wide PRD/SDD would not "replace" them.
3. Auto-prepending deprecation notices to 1,147 .md files would be destructive and inappropriate.

Operator MAY decide later to relocate `grimoires/loa/prd-*.md`, `sdd-*.md`, `sprint-*.md` to a cycle-archive directory for consistency.
